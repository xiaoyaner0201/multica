{
  "schema_version": "behavior-inventory.v1",
  "subject": {
    "repo": "https://github.com/multica-ai/multica",
    "base_commit": "1ef1d65bef2e2ecc2a2c04b2dcc99bc6a10f9e9c",
    "base_tree": "bfb2d1ef8bf8861cdadc3bdd6e1ce35f6bf48cee"
  },
  "generator": {
    "name": "multica-coder-run/02cc6075-02d4-448e-8225-25158762e98b",
    "version": "hd48-discovery-1",
    "mode": "go/ast name-resolved call-site + sink + literal enumeration over server/**, cross-checked by structured manual source reading of every reported site and of the two HTTP entry handlers",
    "maturity": "D1",
    "produced_by_run_id": "02cc6075-02d4-448e-8225-25158762e98b",
    "produced_by_context_id": "25158762e98b",
    "produced_at": "2026-08-26T07:05:48Z",
    "command": "cd multica-ledger/feat-hd-48-email-template-override/coder/tools && go run . -root ../../../../server -prefix ../../../.. > ../discovery-ast-raw.json",
    "coverage_note": "Deterministic and reproducible, but NOT a completeness upper bound. The walker parses all 1555 non-vendor .go files under server/ with go/parser and matches call targets by SELECTOR NAME, not by a resolved method set: golang.org/x/tools is absent from this environment's module cache, so go/packages type-checked analysis is unavailable offline. Therefore this is D1, not D2, despite the reproducible command — the skill defines D2 as cross-file resolution of calls, which name matching does not provide. Known blind spots, each also filed as an UNVERIFIED candidate: (a) dispatch through an interface value, a stored func value, or reflect.MethodByName is invisible to it — a targeted grep found only reflect.DeepEqual in handler/ and service/ and no MethodByName, and handler.Handler holds EmailService as a CONCRETE *service.EmailService rather than an interface, which lowers but does not eliminate this risk; (b) non-Go surfaces (docker-compose.selfhost.yml, deploy/helm, .env.example, apps/docs/**) are not parsed by the tool and were enumerated by hand instead; (c) third-party behavior inside the Resend SDK and its server side is opaque to any static pass. Sink/escape/literal sections are scoped to server/internal/service/email.go by the -focus flag; call-site enumeration is repo-wide. Per the dispatch, FRONTEND_ORIGIN and s.fromEmail are registered only as required_states and are deliberately NOT expanded into candidates — the sender address is HD-47's write set and a non-goal here."
  },
  "candidates": [
    {
      "candidate_id": "VC-SINK-SMTP",
      "consumer": "(*EmailService).SendVerificationCode SMTP branch — hands the hardcoded subject literal and the inline HTML body to sendSMTP",
      "relationship_source": "Direct call s.sendSMTP(to, \"Your Multica verification code\", body) taken when s.smtpHost != \"\"; SMTP has absolute priority, there is no fallback to Resend or DEV when it fails",
      "eligibility_boundary": "SMTP_HOST non-empty. Note s.fromEmail must also be non-empty or sendSMTP returns before any wire byte is written",
      "sinks": [
        "SMTP DATA payload written by fmt.Fprintf(w, \"%s%s\", headers, bodyBytes) — recipient-visible subject and body",
        "SMTP envelope: c.Mail(s.fromEmail), c.Rcpt(to)"
      ],
      "affected_invariants": [
        "Unset override => byte-identical rendered body, including the exact leading tabs and newlines of the Go raw string literal (a parsed template file will not reproduce them by accident)",
        "Subject rendered on this path must equal the subject rendered on the Resend path — today they are two independent copies of the same literal",
        "The one-time login credential is inside this body; a render defect on this path is an authentication-path defect, not a cosmetic one"
      ],
      "required_states": [
        "SMTP_HOST set",
        "s.fromEmail non-empty (HD-47 write set — required_state only, not expanded here)",
        "code is exactly six ASCII digits, server-generated by generateCode()"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/service/email.go",
        "symbol": "(*EmailService).SendVerificationCode",
        "note": "Byte-identity goldens for this candidate are captured FROM the pre-change tree and therefore must PASS on it; must_fail_old is false by construction. What must RED is the override-applied behavior, filed under OVERRIDE-* candidates."
      }
    },
    {
      "candidate_id": "VC-SINK-RESEND",
      "consumer": "(*EmailService).SendVerificationCode Resend branch — builds resend.SendEmailRequest inline with a SECOND, independent copy of the subject literal",
      "relationship_source": "s.client.Emails.Send(params) with Subject: \"Your Multica verification code\" written a second time, not shared with the SMTP branch",
      "eligibility_boundary": "SMTP_HOST empty AND RESEND_API_KEY non-empty (s.client != nil)",
      "sinks": [
        "resend.Emails.Send — Subject and Html fields leave the process to a third-party API"
      ],
      "affected_invariants": [
        "Duplicated subject literal: any change that updates one copy and not the other is a defect per Issue invariant 3",
        "Unset override => byte-identical Subject and Html on this path too",
        "Unlike the SMTP path, the body here is handed over WITHOUT quoted-printable or 8bit encoding — the transfer encoding decision belongs to Resend, so a CJK body behaves differently on the two paths by construction"
      ],
      "required_states": [
        "SMTP_HOST empty",
        "RESEND_API_KEY set",
        "s.fromEmail used as From (HD-47 write set — required_state only)"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/service/email.go",
        "symbol": "(*EmailService).SendVerificationCode",
        "note": "No existing test constructs an EmailService with a non-nil resend client; this branch has zero automated coverage on the base tree."
      }
    },
    {
      "candidate_id": "VC-SINK-DEV",
      "consumer": "(*EmailService).SendVerificationCode DEV branch — fmt.Printf(\"[DEV] Verification code for %s: %s\\n\", to, code)",
      "relationship_source": "Taken when s.smtpHost == \"\" AND s.client == nil. It does NOT render the subject or the HTML body at all: it prints its own third format string",
      "eligibility_boundary": "SMTP_HOST empty AND RESEND_API_KEY empty",
      "sinks": [
        "process stdout — the operator's terminal and container logs are the effective recipient"
      ],
      "affected_invariants": [
        "This sink structurally bypasses the template surface: whatever template mechanism is introduced, the Planner must decide explicitly whether DEV starts rendering it or keeps its own literal. Silently changing this changes what appears in every developer's log",
        "The one-time login credential is written to stdout in cleartext here. Any change that makes DEV render the operator template must not widen what is logged"
      ],
      "required_states": [
        "SMTP_HOST empty",
        "RESEND_API_KEY empty",
        "APP_ENV irrelevant on this path — the DEV sink is selected purely by absent transport config, NOT by APP_ENV; a production deployment that forgets both SMTP_HOST and RESEND_API_KEY prints login codes to its logs today"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/service/email.go",
        "symbol": "(*EmailService).SendVerificationCode",
        "note": "Distinct from MULTICA_DEV_VERIFICATION_CODE, which lives in the handler (isDevVerificationCode) and gates VERIFICATION, not delivery. The two are unrelated mechanisms with confusingly similar names."
      }
    },
    {
      "candidate_id": "INV-SINK-SMTP",
      "consumer": "(*EmailService).SendInvitationEmail SMTP branch — calls buildInvitationParams then sendSMTP(to, params.Subject, params.Html), discarding params.From",
      "relationship_source": "buildInvitationParams(s.fromEmail, to, inviterName, workspaceName, inviteURL) at email.go:376, then s.sendSMTP at email.go:377. The params.From field built one line earlier is thrown away; sendSMTP uses s.fromEmail directly",
      "eligibility_boundary": "SMTP_HOST non-empty. Reached only if the caller's guard h.EmailService != nil && workspaceName != \"\" held",
      "sinks": [
        "SMTP DATA payload — recipient-visible subject and body delivered to an unauthenticated off-site address",
        "SMTP envelope c.Mail/c.Rcpt"
      ],
      "affected_invariants": [
        "Unset override => byte-identical subject and body",
        "Cross-workspace exposure: inviterName and workspaceName originate in workspace A and are delivered to an address that has not authenticated to anything",
        "Subject on this path is single-sourced through buildInvitationParams — unlike the verification-code producer, so the two producers have ASYMMETRIC subject-duplication risk and must not be modelled with one rule"
      ],
      "required_states": [
        "SMTP_HOST set",
        "s.fromEmail non-empty (HD-47 write set — required_state only)",
        "FRONTEND_ORIGIN set or absent — it selects the inviteURL host embedded in the body (HD-47-adjacent; required_state only, not expanded)",
        "invitationID as produced by uuidToString(inv.ID)"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/service/email.go",
        "symbol": "(*EmailService).SendInvitationEmail"
      }
    },
    {
      "candidate_id": "INV-SINK-RESEND",
      "consumer": "(*EmailService).SendInvitationEmail Resend branch — calls buildInvitationParams a second time and passes the whole request to Emails.Send",
      "relationship_source": "Second, independent call to buildInvitationParams at email.go:383, then s.client.Emails.Send(params) at email.go:384",
      "eligibility_boundary": "SMTP_HOST empty AND s.client != nil AND the caller guard held",
      "sinks": [
        "resend.Emails.Send — From, To, Subject, Html all leave the process"
      ],
      "affected_invariants": [
        "Must render identically to INV-SINK-SMTP (Issue invariant 3)",
        "This is the only invitation path where params.From is actually used; the SMTP path drops it. A template change that touches the params struct shape affects the two paths differently"
      ],
      "required_states": [
        "SMTP_HOST empty",
        "RESEND_API_KEY set",
        "FRONTEND_ORIGIN (required_state only)"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/service/email.go",
        "symbol": "buildInvitationParams"
      }
    },
    {
      "candidate_id": "INV-SINK-DEV",
      "consumer": "(*EmailService).SendInvitationEmail DEV branch — fmt.Printf(\"[DEV] Invitation email to %s: %s invited you to %s — %s\\n\", to, inviterName, workspaceName, inviteURL)",
      "relationship_source": "Taken when s.smtpHost == \"\" AND s.client == nil. It never calls buildInvitationParams",
      "eligibility_boundary": "SMTP_HOST empty AND RESEND_API_KEY empty AND caller guard held",
      "sinks": [
        "process stdout / container logs"
      ],
      "affected_invariants": [
        "THIS SINK APPLIES NEITHER ESCAPE MODEL. It prints inviterName and workspaceName RAW — not html.EscapeString, not sanitizeSubjectField. Control characters, 200-character names and markup all reach the log verbatim today. Any redesign must state deliberately whether that stays true; both keeping it and fixing it are behavior changes with evidence obligations",
        "It is also a third independent copy of the invitation copy text (\"invited you to\"), so the phrase now exists in three places: DEV literal, subject format, body literal"
      ],
      "required_states": [
        "SMTP_HOST empty",
        "RESEND_API_KEY empty",
        "inviterName and/or workspaceName containing control characters or markup"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/service/email.go",
        "symbol": "(*EmailService).SendInvitationEmail",
        "note": "Discovered by the literal scan (email.go:380), not by reading the changed lines — it is outside both functions named in the Issue's line table."
      }
    },
    {
      "candidate_id": "SMTP-HEADER-SUBJECT-QENCODE",
      "consumer": "sendSMTP subject header assembly — encodedSubject := mime.QEncoding.Encode(\"utf-8\", subject), emitted as \"Subject: \" + encodedSubject",
      "relationship_source": "Every SMTP send of BOTH producers funnels through this single line; it is the only place a subject becomes wire bytes",
      "eligibility_boundary": "Any SMTP send. Unconditional — no extension probe gates it",
      "sinks": [
        "the Subject: header inside the SMTP DATA payload"
      ],
      "affected_invariants": [
        "The Issue's CJK acceptance criterion (\"Non-ASCII template content renders correctly end-to-end, including subject encoding\") lands HERE and nowhere else. A CJK subject must emerge as RFC 2047 encoded-words, not raw UTF-8 bytes",
        "QEncoding.Encode returns the input UNCHANGED when it is pure ASCII. So an ASCII byte-identity golden cannot detect a regression in the non-ASCII path, and a CJK probe cannot be substituted by an ASCII one",
        "sanitizeSubjectField's 60-rune cap is applied BEFORE encoding; encoded-word expansion of CJK is roughly 3-4x, so the cap does not bound the encoded header length linearly"
      ],
      "required_states": [
        "subject containing CJK",
        "subject that is pure ASCII",
        "subject at exactly the maxSubjectFieldRunes boundary"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/service/email.go",
        "symbol": "(*EmailService).sendSMTP",
        "note": "NO existing test asserts anything about this header. The mock SMTP server in email_test.go reads DATA lines and discards them (see TEST-GAP-WIRE-BYTES)."
      }
    },
    {
      "candidate_id": "SMTP-HEADER-CTE-8BIT",
      "consumer": "sendSMTP 8BITMIME branch — bodyBytes = []byte(htmlBody) raw, cte = \"8bit\"",
      "relationship_source": "has8Bit, _ := c.Extension(\"8BITMIME\") probed AFTER any STARTTLS upgrade, so the branch depends on post-upgrade server capability",
      "eligibility_boundary": "Relay advertises 8BITMIME",
      "sinks": [
        "raw body bytes in the SMTP DATA payload",
        "the Content-Transfer-Encoding: 8bit header"
      ],
      "affected_invariants": [
        "A CJK body is transmitted as raw UTF-8 on this branch and as quoted-printable on the other. The SAME template therefore produces two different wire byte streams depending on relay capability; byte-identity assertions must be stated per branch, not once",
        "This branch is NOT exercised by any existing test: the mock server in email_test.go never advertises 8BITMIME, so every current sendSMTP test runs the quoted-printable branch only"
      ],
      "required_states": [
        "relay advertising 8BITMIME",
        "body containing non-ASCII",
        "body containing a line longer than 76 characters (the two branches diverge on soft line breaks)"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/service/email.go",
        "symbol": "(*EmailService).sendSMTP"
      }
    },
    {
      "candidate_id": "SMTP-HEADER-CTE-QP",
      "consumer": "sendSMTP quoted-printable branch — quotedprintable.NewWriter over htmlBody, cte = \"quoted-printable\"",
      "relationship_source": "Fallback when 8BITMIME is not advertised",
      "eligibility_boundary": "Relay does not advertise 8BITMIME",
      "sinks": [
        "quoted-printable-encoded body in the SMTP DATA payload",
        "the Content-Transfer-Encoding: quoted-printable header"
      ],
      "affected_invariants": [
        "quotedprintable inserts soft line breaks (=\\r\\n) at column ~76. The current body is a Go raw string literal whose lines are indented with TABS; a template file that changes indentation by one character can shift every soft break and change the wire bytes without changing the rendered text. This is the concrete mechanism behind the Issue's warning that byte-identity 'is easy to get wrong'",
        "Trailing whitespace is encoded as =20 — so a template file with a trailing space or a final newline the literal does not have is directly observable on the wire"
      ],
      "required_states": [
        "relay without 8BITMIME",
        "body with tab indentation preserved exactly",
        "body containing non-ASCII"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/service/email.go",
        "symbol": "(*EmailService).sendSMTP"
      }
    },
    {
      "candidate_id": "SMTP-HEADER-CONTENT-TYPE",
      "consumer": "sendSMTP hardcoded header \"Content-Type: text/html; charset=UTF-8\\r\\n\"",
      "relationship_source": "String literal in the header block, applied to every SMTP send of both producers regardless of what the body actually is",
      "eligibility_boundary": "Any SMTP send. Unconditional",
      "sinks": [
        "the Content-Type header in the SMTP DATA payload"
      ],
      "affected_invariants": [
        "An operator-supplied template is a NEW way for the body to stop being HTML while this header keeps asserting text/html. Today that mismatch is impossible because the bodies are compiled-in HTML literals",
        "Also fixed here: From:, To:, Date:, Message-ID:, MIME-Version:. Message-ID embeds s.smtpHost and a nanosecond timestamp, so the DATA payload is NOT byte-stable across runs — a golden fixture must normalise Date and Message-ID or assert on the body and subject separately from the full payload"
      ],
      "required_states": [
        "any SMTP send",
        "an override template whose content is not HTML"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/service/email.go",
        "symbol": "(*EmailService).sendSMTP"
      }
    },
    {
      "candidate_id": "ESCAPE-MODEL-BODY-HTMLESCAPESTRING",
      "consumer": "buildInvitationParams body escaping — safeWorkspace := html.EscapeString(workspaceName), safeInviter := html.EscapeString(inviterName), interpolated with %s into the HTML literal",
      "relationship_source": "Escape model #1. It runs BEFORE the values reach the literal, so the values arriving at the body are already-escaped strings",
      "eligibility_boundary": "Invitation body on the SMTP and Resend paths only. The DEV path (INV-SINK-DEV) does not use it",
      "sinks": [
        "SMTP DATA body",
        "resend Emails.Send Html field"
      ],
      "affected_invariants": [
        "Migrating to html/template introduces escape model #3, which auto-escapes at render time. Feeding it values that html.EscapeString already processed yields DOUBLE ESCAPING (&amp;lt;script&amp;gt; in the inbox); dropping html.EscapeString and mis-declaring the value as template.HTML yields ESCAPE LOSS (stored XSS in a mail client). These are symmetric failures and one probe cannot cover both — the Planner must model each direction separately",
        "safeWorkspace is interpolated TWICE (h2 heading and the strong tag) while safeInviter is interpolated once. Note the argument order in the Sprintf is (safeWorkspace, safeInviter, safeWorkspace, inviteURL) — not left-to-right by name. Any template rewrite that reorders placeholders swaps inviter and workspace silently, and the existing substring tests would still pass because both strings appear somewhere in the body",
        "inviteURL is interpolated into an href WITHOUT any escaping. It is derived from FRONTEND_ORIGIN plus a server-generated UUID, so it is not attacker-controlled today; under html/template it would be subject to URL-context filtering instead, a different model again"
      ],
      "required_states": [
        "workspaceName containing < > & \" '",
        "inviterName containing an attribute-break payload",
        "benign ASCII names (the no-change control)",
        "names containing characters that html.EscapeString does NOT touch"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/service/email.go",
        "symbol": "buildInvitationParams"
      }
    },
    {
      "candidate_id": "ESCAPE-MODEL-SUBJECT-SANITIZE",
      "consumer": "sanitizeSubjectField — strips unicode.IsControl runes, then caps at maxSubjectFieldRunes (60) replacing the tail with a single U+2026 ellipsis",
      "relationship_source": "Escape model #2, applied only to the subject, only for the invitation producer. Its doc comment states explicitly that HTML-escaping here would leak literal entities into the inbox, so NOT escaping is the intended behavior, not an oversight",
      "eligibility_boundary": "Invitation subject on the SMTP and Resend paths. The verification-code subject is a constant and never passes through it",
      "sinks": [
        "the Subject: header (after QEncoding) on the SMTP path",
        "the resend Subject field"
      ],
      "affected_invariants": [
        "MUST remain distinct from the body model. If a template engine applies one uniform auto-escape to both subject and body, the subject regresses to literal &lt;script&gt; text — a user-visible defect the current code deliberately avoids",
        "The truncation is at 59 runes plus U+2026, i.e. the result is 60 runes; it counts RUNES not bytes, so CJK names are not truncated early",
        "Control stripping is defence-in-depth against header injection. On the SMTP path a surviving CR/LF in the subject would be folded into raw headers by the fmt.Fprintf header block — there is no second CR/LF filter between sanitizeSubjectField and the wire. Removing or reordering this step is an INVARIANT_VIOLATION, not a refactor"
      ],
      "required_states": [
        "inviterName containing \\r\\n",
        "workspaceName containing \\t",
        "workspaceName longer than 60 runes",
        "workspaceName containing a literal ampersand",
        "CJK names at the rune boundary"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/service/email.go",
        "symbol": "sanitizeSubjectField"
      }
    },
    {
      "candidate_id": "ENTRY-SENDCODE-HANDLER",
      "consumer": "(*Handler).SendCode — the only production caller of SendVerificationCode",
      "relationship_source": "h.EmailService.SendVerificationCode(email, code) at handler/auth.go:356, routed as r.With(authRL).Post(\"/auth/send-code\", h.SendCode) in cmd/server/router.go",
      "eligibility_boundary": "UNAUTHENTICATED endpoint, rate-limited by authRL plus an application rule of one code per 60s per email (GetLatestCodeByEmail). Signup restrictions and the temporarily-disabled-user check run before it",
      "sinks": [
        "whichever of the three SendVerificationCode sinks is configured",
        "HTTP 500 'failed to send verification code' to the caller",
        "slog.Error 'failed to send verification code'"
      ],
      "affected_invariants": [
        "SYNCHRONOUS: a send error DOES reach the user as a 500. A loud failure is structurally possible on this path — unlike the invitation path",
        "The verification code row is persisted BEFORE the send. A send failure therefore leaves a valid unusable code in the database AND consumes the 60-second rate-limit window, locking the user out of retrying. A template-load failure that surfaces at send time rather than at startup converts a config typo into a full login outage with a 60s retry floor per user",
        "No user-controlled text reaches this body: code is %06d from crypto/rand. That is the current guarantee; introducing template variables must not widen it"
      ],
      "required_states": [
        "new user vs existing user",
        "within the 60s rate-limit window",
        "EmailService returning an error"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/handler/auth.go",
        "symbol": "(*Handler).SendCode"
      }
    },
    {
      "candidate_id": "ENTRY-CREATEINVITATION-HANDLER",
      "consumer": "(*Handler).CreateInvitation — the only production caller of SendInvitationEmail, invoked inside a bare goroutine",
      "relationship_source": "go func() { h.EmailService.SendInvitationEmail(...) }() at handler/invitation.go:239-243. The AST pass flags this site with in_go_stmt=true; it is routed as r.Post(\"/members\", h.CreateInvitation) under the workspace-scoped group",
      "eligibility_boundary": "Guarded by h.EmailService != nil && workspaceName != \"\". workspaceName is only set if GetWorkspace succeeded — so a workspace lookup failure silently suppresses the invitation email entirely while still returning HTTP 201",
      "sinks": [
        "whichever of the three SendInvitationEmail sinks is configured",
        "slog.Warn 'failed to send invitation email' — and nothing else"
      ],
      "affected_invariants": [
        "FIRE-AND-FORGET, DETACHED FROM THE REQUEST. The error is only logged; HTTP 201 has already been written. Issue invariant 4 (a malformed template must fail loudly and early, not silently) is therefore UNSATISFIABLE at send time on this path — the only place it can be made loud is process startup. This is the single most consequential structural constraint this discovery found and the Planner must dispose of it explicitly",
        "The goroutine captures email, inviterName, workspaceName, invID by value before launching, so it does not race the handler's locals — but it also does not inherit r.Context(), so it has no deadline and no cancellation",
        "inviterName defaults to the INVITEE'S OWN EMAIL ADDRESS when GetUser fails (inviterName := email is the declared fallback). A template rendering 'X invited you to Y' can therefore legitimately render the recipient's own address as the inviter"
      ],
      "required_states": [
        "GetWorkspace failing (email suppressed entirely, 201 still returned)",
        "GetUser failing (inviter name falls back to the invitee's email)",
        "seat-capacity enabled and disabled transaction shapes",
        "EmailService returning an error (log-only)"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/handler/invitation.go",
        "symbol": "(*Handler).CreateInvitation"
      }
    },
    {
      "candidate_id": "INPUT-INVITERNAME-PROVENANCE",
      "consumer": "inviterName — the upstream source of one of the two user-controlled template variables",
      "relationship_source": "h.Queries.GetUser(r.Context(), requester.UserID).Name, written by (*Handler).UpdateMe and by the Google login name-adoption path in GoogleLogin",
      "eligibility_boundary": "Any authenticated user can set their own display name. UpdateMe applies strings.TrimSpace and rejects only the empty string — there is NO length cap and NO character-class restriction, so arbitrary UTF-8 of arbitrary length reaches the email",
      "sinks": [
        "invitation subject via sanitizeSubjectField",
        "invitation body via html.EscapeString",
        "DEV stdout RAW, unescaped"
      ],
      "affected_invariants": [
        "Attacker-controlled by any workspace member with invite rights, delivered to an address that has never authenticated. This is the cross-workspace trust boundary that makes this Issue HIGH_RISK",
        "Unbounded at the input layer: the only length bound anywhere is sanitizeSubjectField's 60-rune subject cap. The BODY is not length-capped at all — a megabyte display name lands in the HTML body today",
        "MaxProfileDescriptionLen exists for profile_description but there is no analogous cap for Name"
      ],
      "required_states": [
        "display name containing markup",
        "display name containing CR/LF",
        "display name of several thousand runes",
        "display name that is pure CJK",
        "GetUser failure => inviterName is the invitee's email"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/handler/auth.go",
        "symbol": "(*Handler).UpdateMe"
      }
    },
    {
      "candidate_id": "INPUT-WORKSPACENAME-PROVENANCE",
      "consumer": "workspaceName — the second user-controlled template variable, interpolated into the body TWICE and into the subject once",
      "relationship_source": "h.Queries.GetWorkspace(r.Context(), requester.WorkspaceID).Name, written by CreateWorkspace and UpdateWorkspace",
      "eligibility_boundary": "Workspace admins. Both handlers apply only strings.TrimSpace and an empty-string rejection — no length cap, no character-class restriction. (The slug IS pattern-restricted and reserved-word checked; the NAME is not)",
      "sinks": [
        "invitation subject via sanitizeSubjectField",
        "invitation body via html.EscapeString, at two separate interpolation points",
        "DEV stdout RAW, unescaped",
        "the workspace_name field of the EventInvitationCreated realtime payload (a pre-existing sibling sink, unchanged by this Issue but sharing the same source value)"
      ],
      "affected_invariants": [
        "Same cross-workspace, unauthenticated-recipient exposure as the inviter name",
        "The double interpolation means a template rewrite must place it in both the heading and the body sentence; omitting one is a silent content regression that substring assertions cannot catch"
      ],
      "required_states": [
        "workspace name containing markup",
        "workspace name longer than 60 runes",
        "workspace name that is pure CJK",
        "workspace name containing a literal ampersand"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/handler/workspace.go",
        "symbol": "(*Handler).UpdateWorkspace"
      }
    },
    {
      "candidate_id": "CTOR-NEWEMAILSERVICE-STARTUP",
      "consumer": "service.NewEmailService() — the single process-wide construction point and the only place a startup-time template load could hook",
      "relationship_source": "Called once at cmd/server/router.go:421 inside NewRouterWithOptions, and once at handler/handler_test.go:66 in TestMain",
      "eligibility_boundary": "Process startup. Runs unconditionally",
      "sinks": [
        "the *EmailService instance held as a CONCRETE *service.EmailService on handler.Handler",
        "startup stdout banner: one of three fmt.Printf/Println lines announcing SMTP relay / Resend API / DEV mode"
      ],
      "affected_invariants": [
        "NewEmailService RETURNS NO ERROR — its signature is func NewEmailService() *EmailService. There is today no channel through which a template-load failure can abort startup. Satisfying Issue invariant 4 ('fail loudly and early') therefore REQUIRES a signature or lifecycle change at this call site, which is a change to cmd/server/router.go, a file outside the Issue's line table. The Planner must decide and justify: change the signature, panic, or log-and-degrade — and log-and-degrade is exactly the silent fallback the invariant forbids",
        "Its current error-handling precedent is log-and-continue: an os.Hostname() failure and an unrecognised SMTP_TLS value both only Printf a warning",
        "The startup banner currently prints from=<sender>. Any new banner line must not print operator template contents or paths that could leak secrets into logs",
        "Because the field is a concrete pointer and not an interface, handler tests cannot substitute a fake sender — which is why there is no handler-level email test today"
      ],
      "required_states": [
        "SMTP mode",
        "Resend mode",
        "DEV mode",
        "s.fromEmail resolution via resolveFromEmail (HD-47 write set — required_state only, NOT expanded)",
        "override directory missing / unreadable / malformed"
      ],
      "must_fail_old": true,
      "evidence": {
        "path": "server/cmd/server/router.go",
        "symbol": "NewRouterWithOptions",
        "note": "must_fail_old is true here: a probe asserting that a malformed override aborts or reports at startup has no old behavior to match and must RED on the base tree, where the mechanism does not exist."
      }
    },
    {
      "candidate_id": "OVERRIDE-NEW-INPUT-SURFACE",
      "consumer": "the operator-supplied template directory itself — a new semi-trusted input surface that does not exist on the base tree",
      "relationship_source": "Introduced by this Issue's chosen Option B. It has no producer, no caller and no test on the base tree; it is listed so the Planner cannot treat it as out of the behavior graph",
      "eligibility_boundary": "Whatever env var the Planner chooses, read at startup by whatever mechanism CTOR-NEWEMAILSERVICE-STARTUP grows",
      "sinks": [
        "every sink of both producers — six delivery branches plus the SMTP header block"
      ],
      "affected_invariants": [
        "Template content is operator-controlled, i.e. more trusted than a workspace name but LESS trusted than compiled-in source: a self-hosted operator is not necessarily the same principal as the person who reviewed the Go code",
        "Path handling: a directory value read from the environment and joined with a filename is a path-traversal and symlink surface that does not exist today",
        "Undefined-variable behavior: text/template renders <no value> by default while html/template with missingkey=error fails. The Issue requires this be 'defined, tested behavior' — it is currently undefined because the mechanism is absent",
        "An override template is also the first way the two producers' subjects can DIVERGE between SMTP and Resend at runtime rather than at edit time"
      ],
      "required_states": [
        "override unset (must reproduce base bytes exactly)",
        "override directory absent",
        "override directory present but unreadable",
        "template with malformed syntax",
        "template referencing an undefined variable",
        "template containing CJK",
        "template that is not HTML"
      ],
      "must_fail_old": true,
      "evidence": {
        "path": "server/internal/service/email.go",
        "symbol": "(*EmailService).SendVerificationCode",
        "note": "No base-tree evidence exists by definition; the evidence anchor is the producer this surface will feed. Every probe for this candidate must RED on the base tree."
      }
    },
    {
      "candidate_id": "TEST-EXISTING-INVITATION-SANITIZATION",
      "consumer": "the five existing buildInvitationParams tests, which the function's own doc comment says it was extracted to enable",
      "relationship_source": "TestBuildInvitationParams_EscapesHTMLInBody, _SubjectStripsControls, _SubjectNotHTMLEscaped, _SubjectTruncated, _ToAndFromPassedThrough in server/internal/service/email_test.go",
      "eligibility_boundary": "Pure unit tests on the helper. They never construct an EmailService and never touch a sink",
      "sinks": [
        "none — they assert on the returned *resend.SendEmailRequest struct, one call short of every real sink"
      ],
      "affected_invariants": [
        "RED SENSITIVITY IS WEAK AND QA MUST RE-ASSESS IT, not inherit it. _EscapesHTMLInBody asserts substring presence/absence only; a body that double-escapes to &amp;lt;script&amp;gt; still satisfies 'does not contain raw <script>' AND, for the &lt;script&gt; needle, still contains it as a substring of the double-escaped form. This suite would stay GREEN through a double-escaping regression",
        "_SubjectStripsControls is the strongest of the five: it asserts full subject equality ('Alice invited you to Acme on Multica')",
        "None of the five asserts byte-exact BODY output; there is no golden fixture anywhere in the tree",
        "They pin the helper's signature. A refactor that moves rendering out of buildInvitationParams silently orphans all five without any of them failing"
      ],
      "required_states": [
        "base tree, go test ./internal/service/"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/service/email_test.go",
        "symbol": "TestBuildInvitationParams_EscapesHTMLInBody",
        "note": "Baseline verified green on the base tree: GOTMPDIR=/workspaces/HD-48/gotmp go test ./internal/service/ -run 'TestSanitizeSubjectField|TestBuildInvitationParams|TestSendSMTP|TestNewEmailService|TestLoginAuth|TestSMTPAuth' => ok github.com/multica-ai/multica/server/internal/service 0.026s"
      }
    },
    {
      "candidate_id": "TEST-GAP-WIRE-BYTES",
      "consumer": "the mock SMTP server used by the sendSMTP flow tests — testSMTPServer.handleConn",
      "relationship_source": "Its DATA handler reads lines until a lone '.' and DISCARDS every one of them; it never records the payload. Consequently no test in the tree asserts on any rendered header or body byte",
      "eligibility_boundary": "All four TestSendSMTP_* flow tests",
      "sinks": [
        "none captured — the wire bytes are read and dropped"
      ],
      "affected_invariants": [
        "There is currently ZERO coverage of: the Subject: header, mime.QEncoding output, Content-Type, Content-Transfer-Encoding, the 8bit branch, quoted-printable output, and the rendered body on the wire. Every SMTP-side acceptance criterion in this Issue lands on untested ground",
        "The server also never advertises 8BITMIME, so SMTP-HEADER-CTE-8BIT has no coverage at all while SMTP-HEADER-CTE-QP is only implicitly executed",
        "SendVerificationCode has NO test of any kind — the AST scan finds zero call sites for it in any _test.go file across all 1555 parsed files. The producer that carries the login credential is the one with no coverage",
        "The Resend branch of both producers has no test: no test constructs an EmailService with a non-nil resend client"
      ],
      "required_states": [
        "base tree"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/service/email_test.go",
        "symbol": "testSMTPServer.handleConn",
        "note": "Registered as a gap, not a defect. QA owns whether to close it; the Planner owns whether the acceptance criteria can be met without closing it. They cannot: the Issue's CJK-subject criterion is unobservable through the current harness."
      }
    },
    {
      "candidate_id": "DEPLOY-ENV-PLUMBING",
      "consumer": "the deployment surfaces that must forward any new env var into the running container",
      "relationship_source": "Enumerated by hand, not by the AST tool. docker-compose.selfhost.yml forwards eight SMTP_* vars plus RESEND_* into the backend service environment block; deploy/helm/multica/templates/configmap.yaml references RESEND_FROM_EMAIL but forwards NO SMTP_* var at all; .env.example documents the email block; apps/docs/content/docs/{environment-variables,auth-setup,troubleshooting}.mdx exist in four languages each",
      "eligibility_boundary": "Self-hosted deployments only. The Issue's entire premise",
      "sinks": [
        "the backend container's environment",
        "operator-facing documentation"
      ],
      "affected_invariants": [
        "An override env var that is not added to docker-compose.selfhost.yml is INERT for exactly the deployments this Issue exists to serve — the feature would be untestable by its intended user",
        "The backend service declares only one volume, backend_uploads:/app/data/uploads. A directory-based override needs a NEW bind mount; there is no existing host path an operator could drop templates into",
        "The Helm chart already omits SMTP_* entirely — a pre-existing gap, NOT this Issue's to fix, but the Planner should state whether Helm is in or out of scope rather than leave it ambiguous",
        "Docs exist in en/zh/ja/ko. Documenting only the English file leaves three translations describing a stale contract"
      ],
      "required_states": [
        "docker compose self-host deployment",
        "Helm deployment",
        "bare binary deployment"
      ],
      "must_fail_old": true,
      "evidence": {
        "path": "docker-compose.selfhost.yml",
        "symbol": "services.backend.environment",
        "note": "must_fail_old is true in the sense that no probe of the override plumbing can pass on the base tree."
      }
    },
    {
      "candidate_id": "UNVERIFIED-DYNAMIC-DISPATCH",
      "consumer": "any caller of the two producers that this discovery could not resolve",
      "relationship_source": "UNVERIFIED BY CONSTRUCTION. The AST walker matches call targets by selector name without go/types, because golang.org/x/tools is not in this environment's module cache and go/packages is unavailable offline. A call through an interface value, a stored func value, or reflect.MethodByName whose selector text differs from the target name would not be reported",
      "eligibility_boundary": "Unknown — that is the point of the entry",
      "sinks": [
        "unknown; potentially any of the six delivery branches"
      ],
      "affected_invariants": [
        "Every invariant in this inventory, if such a path exists",
        "Mitigating facts, offered as evidence and NOT as proof: handler.Handler holds EmailService as a concrete *service.EmailService, not an interface, so no interface indirection exists at the only known injection point; a targeted grep across server/internal/handler and server/internal/service found only reflect.DeepEqual in project_resource.go and no MethodByName anywhere; the two methods are exported from package service so any external caller must spell service.EmailService, and a repo-wide scan of 1555 files found exactly two production call sites",
        "These facts lower the residual risk but do not discharge it. Per the dispatch this is registered as UNVERIFIED rather than assumed safe, and the Planner must dispose of it explicitly"
      ],
      "required_states": [
        "N/A — this candidate records a method limitation, not a runtime state"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "multica-ledger/feat-hd-48-email-template-override/coder/tools/discover_email_behavior.go",
        "symbol": "calleeName",
        "note": "The tool emits this same limitation in its own unresolved_notes section, so the artifact is self-describing."
      }
    },
    {
      "candidate_id": "UNVERIFIED-RESEND-SDK-BEHAVIOR",
      "consumer": "github.com/resend/resend-go/v2 Emails.Send and the Resend service behind it",
      "relationship_source": "Third-party dependency. Two of the six delivery branches terminate inside it and neither this tree nor any static pass can observe what it does to Subject and Html",
      "eligibility_boundary": "Resend mode",
      "sinks": [
        "the actual email delivered by Resend — outside this repository entirely"
      ],
      "affected_invariants": [
        "sanitizeSubjectField's doc comment ASSERTS that 'Resend also filters CR/LF'. That claim is not verified anywhere in this tree and cannot be verified by this discovery. If it is false, control stripping is the only CR/LF defence on the Resend path as well as the SMTP path",
        "Whether Resend applies its own encoding, length limits or sanitisation to a CJK subject is unknown, so the Issue's end-to-end CJK criterion is only mechanically verifiable on the SMTP path",
        "Byte-identity on the Resend path can only be asserted at the SendEmailRequest struct boundary, never on the wire"
      ],
      "required_states": [
        "RESEND_API_KEY set",
        "subject containing CR/LF",
        "subject containing CJK"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "server/internal/service/email.go",
        "symbol": "(*EmailService).SendInvitationEmail",
        "note": "Registered as UNVERIFIED, not excluded. Do not let the doc comment's claim stand in as evidence."
      }
    },
    {
      "candidate_id": "UNVERIFIED-NONGO-SURFACES",
      "consumer": "every non-Go file that participates in email behavior",
      "relationship_source": "The AST tool parses .go files only. The deployment, packaging and documentation surfaces were enumerated by hand and that enumeration is not mechanically complete",
      "eligibility_boundary": "Build and deploy time",
      "sinks": [
        "container image contents",
        "container environment",
        "operator documentation"
      ],
      "affected_invariants": [
        "The Issue states as a source-confirmed fact that the image contains no template files — /app holds only binaries, migrations/ and LICENSE. This discovery did NOT re-verify that claim against a built image; there is no server Dockerfile in this tree (only Dockerfile and Dockerfile.web at the root), so where the backend image is built from was not established",
        "Any embed.FS approach changes what the Go build includes; any directory approach changes what the image or a mount must provide. Neither was mechanically checked",
        "Documentation exists in four languages for three relevant pages; the set of pages needing updates was not exhaustively determined"
      ],
      "required_states": [
        "built backend image",
        "compose deployment",
        "Helm deployment"
      ],
      "must_fail_old": false,
      "evidence": {
        "path": "docker-compose.selfhost.yml",
        "symbol": "services.backend",
        "note": "Registered as UNVERIFIED per the dispatch instruction not to default to safe. The Planner must either verify the image claim or carry it as an explicit assumption."
      }
    }
  ]
}
