---
ts: 20260826-164100
agent: coder
action: RED→GREEN
tree: ecc74e58
verdict: —
---

# 实现：U1 邮件模板自定义

## 1. 依据

- 冻结方案：`planner/20260826-154500-plan-r4.md`，sha256
  `4e7fef61b821b1ab015d0ab2802766384ef1eb22d98f8829f8c7db8b9fc4ce0e`
- 人工闸门：`leader/20260826-163000-gate1-approved-r4.md`，含 PG16 / PG20 与追加约束三至五
- BASE：commit `f74a71060fcdd45318cae6fb7caf7c0d7e71cdfa` / tree
  `57e81be90cc49effdc55aa2f06f16aef35689aba`
- 本次覆盖的 unit：U1（`work_partition` 唯一单元）
- 候选代码：commit `0a37dbada5bd9a6cd9f955252fec38631a7bf1cd` / tree
  `ecc74e58c87472d9e9aa1722fdcae955428bab99`

## 2. RED → GREEN → REFACTOR

### 2.1 mock relay 观测准备

先给既有 `testSMTPServer` 增加 `CapturedEnvelope` / `CapturedData` 与 `Advertise8BITMIME`，DATA 用
`textproto.Reader.ReadDotBytes` 读取。这个步骤只让最终副作用可观测，不算被测生产行为。

```
$ GOTMPDIR=/workspaces/hd53-go-tmp.52fb424651d1 /usr/local/go/bin/go test ./internal/service -run 'TestSendSMTP_(FallbackReconnectsAndAuthsWithLOGIN|PlainAuthSucceedsWithoutFallback|NoAuthWhenUsernameEmpty)$' -count=1 -v
=== RUN   TestSendSMTP_FallbackReconnectsAndAuthsWithLOGIN
--- PASS: TestSendSMTP_FallbackReconnectsAndAuthsWithLOGIN (0.01s)
=== RUN   TestSendSMTP_PlainAuthSucceedsWithoutFallback
--- PASS: TestSendSMTP_PlainAuthSucceedsWithoutFallback (0.00s)
=== RUN   TestSendSMTP_NoAuthWhenUsernameEmpty
--- PASS: TestSendSMTP_NoAuthWhenUsernameEmpty (0.00s)
PASS
ok  github.com/multica-ai/multica/server/internal/service  0.014s
```

### 2.2 数据契约、加载器与 Subject 边界

**RED** — 测试先引用冻结的数据结构、四个文件名、加载后模板槽位、
`sanitizeRenderedSubject` 与 `subjectLine`；生产代码尚不存在，编译按目标原因失败。

```
$ GOTMPDIR=/workspaces/hd53-go-tmp.52fb424651d1 /usr/local/go/bin/go test ./internal/service -run 'Test(EmailTemplateDataContract|SanitizeRenderedSubject|SubjectSanitizers|NewEmailServiceLoads)' -count=1 -v
# github.com/multica-ai/multica/server/internal/service [github.com/multica-ai/multica/server/internal/service.test]
internal/service/email_test.go:765:16: undefined: verificationTemplateData
internal/service/email_test.go:770:16: undefined: invitationTemplateData
internal/service/email_test.go:806:12: undefined: sanitizeRenderedSubject
internal/service/email_test.go:814:12: undefined: subjectLine
internal/service/email_test.go:834:14: undefined: sanitizeRenderedSubject
FAIL github.com/multica-ai/multica/server/internal/service [build failed]
FAIL
```

失败原因命中目标：是。测试从编译期证明冻结契约与主题管线尚未实现，不是测试自身错误。

**GREEN** — 增加两个精确字段结构体、四个独立可选模板槽位、启动时加载/解析/零值诊断、
`unicode.IsControl` → 200 rune → 998 octet 的 Subject 管线。T24a 最终扩成五类字符集共 20000 条
确定性随机输入的前缀/极大/幂等性质测试；T24b 运行期枚举得到 198 rune 的精确 998 边界。

```
$ GOTMPDIR=/workspaces/hd53-go-tmp.52fb424651d1 /usr/local/go/bin/go test ./internal/service -run 'TestSanitizeRenderedSubject(Properties|ExactBoundary|Bounds)$' -count=1 -v
=== RUN   TestSanitizeRenderedSubjectBounds
--- PASS: TestSanitizeRenderedSubjectBounds (0.00s)
=== RUN   TestSanitizeRenderedSubjectProperties
--- PASS: TestSanitizeRenderedSubjectProperties (2.17s)
=== RUN   TestSanitizeRenderedSubjectExactBoundary
    email_test.go:897: derived exact boundary with 198 runes
--- PASS: TestSanitizeRenderedSubjectExactBoundary (0.03s)
PASS
ok  github.com/multica-ai/multica/server/internal/service  2.206s
```

测试层：结构体闭包与纯函数单元层；另外由 §2.3 的 SMTP/Resend 入口测试证明它们进入真实发送路径。

### 2.3 SMTP / Resend / DEV 生产入口

**RED** — 从 `SendVerificationCode` / `SendInvitationEmail` 发信，分别在 Resend 请求体和 mock relay
最终 DATA 断言自定义 Subject/HTML、上下文转义、控制字符清洗、QP/8BITMIME 与 dot-stuffing。

```
$ GOTMPDIR=/workspaces/hd53-go-tmp.52fb424651d1 /usr/local/go/bin/go test ./internal/service -run 'Test(SendVerificationCodeCustom|SendInvitationEmailCustom|SendInvitationEmailTemplate|DEVModeDoesNot|CustomTemplateSMTP)' -count=1 -v
=== RUN   TestSendVerificationCodeCustomTemplatesReachResend
    email_test.go:1011: Subject = "Your Multica verification code"
--- FAIL: TestSendVerificationCodeCustomTemplatesReachResend (0.00s)
=== RUN   TestSendInvitationEmailCustomTemplatesReachResend
    email_test.go:1037: Subject = "Alice invited you to A&B <img src=x> on Multica"
--- FAIL: TestSendInvitationEmailCustomTemplatesReachResend (0.00s)
=== RUN   TestSendInvitationEmailTemplateComponentsAreIndependent
    email_test.go:1064: custom Html = "<div style=...>"
--- FAIL: TestSendInvitationEmailTemplateComponentsAreIndependent (0.00s)
=== RUN   TestDEVModeDoesNotRenderTemplates
--- PASS: TestDEVModeDoesNotRenderTemplates (0.00s)
=== RUN   TestCustomTemplateSMTPWireSafetyAndTransferEncoding
    email_test.go:1107: Subject = "Your Multica verification code"
--- FAIL: TestCustomTemplateSMTPWireSafetyAndTransferEncoding (0.01s)
FAIL
FAIL github.com/multica-ai/multica/server/internal/service 0.013s
FAIL
```

失败原因命中目标：是。真实入口仍发送内置内容；DEV 用例先绿也证明其 BASE 早返回行为可作为兼容基线。

**GREEN** — DEV 在渲染前返回；两条真实通道在分叉前各渲染一次到 `bytes.Buffer`；每个组件独立回落；
Resend 改用最小 `emailSender` 接缝捕获真实 `SendEmailRequest`；邀请模板走平行
`buildInvitationParamsFromTemplate`，没有改既有 `buildInvitationParams`。

```
$ GOTMPDIR=/workspaces/hd53-go-tmp.52fb424651d1 /usr/local/go/bin/go test ./internal/service -run 'Test(SendVerificationCodeCustom|SendInvitationEmailCustom|SendInvitationEmailTemplate|DEVModeDoesNot|CustomTemplateSMTP)' -count=1 -v
=== RUN   TestSendVerificationCodeCustomTemplatesReachResend
--- PASS: TestSendVerificationCodeCustomTemplatesReachResend (0.00s)
=== RUN   TestSendInvitationEmailCustomTemplatesReachResend
--- PASS: TestSendInvitationEmailCustomTemplatesReachResend (0.00s)
=== RUN   TestSendInvitationEmailTemplateComponentsAreIndependent
--- PASS: TestSendInvitationEmailTemplateComponentsAreIndependent (0.00s)
=== RUN   TestDEVModeDoesNotRenderTemplates
--- PASS: TestDEVModeDoesNotRenderTemplates (0.00s)
=== RUN   TestCustomTemplateSMTPWireSafetyAndTransferEncoding
--- PASS: TestCustomTemplateSMTPWireSafetyAndTransferEncoding (0.00s)
PASS
ok  github.com/multica-ai/multica/server/internal/service  0.010s
```

测试层：最终生产入口 + SMTP wire DATA / envelope + Resend 请求体副作用落点。正文经 DotReader 反
stuffing、按 CTE 解 quoted-printable 后断言；Subject 经 `net/mail` 与 `mime.WordDecoder` 断言。

### 2.4 失败语义、兼容与并发

覆盖：目录不存在一条告警、缺文件静默、parse 失败隔离、未知字段的零值诊断仍保持模板启用、零值
假阳性/假阴性、运行期半截 Execute 回落、哨兵验证码不进告警、SMTP/Resend sink 一致、50 并发与
`-race`。内置验证码 wire 在掩蔽并单独校验 `Date` / `Message-ID` 后与 BASE 期望逐字节相等；内置
邀请仍保持原转义。细分失败语义用例是在共同加载/渲染骨架 GREEN 后补齐，首次即绿，未单独取得
每个子场景的 RED；这是本轮 TDD 证据的已知薄弱处，不把它写成先红。

```
$ GOTMPDIR=/workspaces/hd53-go-tmp.52fb424651d1 /usr/local/go/bin/go test -race ./internal/service -count=1
ok  github.com/multica-ai/multica/server/internal/service  23.481s

$ GOTMPDIR=/workspaces/hd53-go-tmp.52fb424651d1 /usr/local/go/bin/go vet ./internal/service
<空输出，退出码 0>
```

### 2.5 文档与部署面

**RED** — 13 个冻结业务文件均无新变量、文件名、品牌固定说明与验证码 Subject 警告。

```
$ rg -n 'EMAIL_TEMPLATE_DIR|verification_code\.subject\.tmpl|AppName.*Multica|\.Code.*(Subject|subject|主题|件名|제목)' .env.example docker-compose.selfhost.yml apps/docs/content/docs/environment-variables{,.ja,.ko,.zh}.mdx apps/docs/content/docs/auth-setup{,.ja,.ko,.zh}.mdx SELF_HOSTING_ADVANCED.md
<无输出，退出码 1>
```

**GREEN** — `.env.example` 登记变量，官方 self-host compose 透传，四语言变量表与 auth 文档同步，
高级自建文档给出挂载、四文件、字段、重启加载与失败回落。新增用户可见数字集合机械核对只含
`91 / 99 / 200 / 67`；四语言均写明 `{{.AppName}} == Multica` 与 `{{.Code}}` Subject 风险。

```
$ git diff --unified=0 -- apps/docs/content/docs/auth-setup.mdx apps/docs/content/docs/auth-setup.ja.mdx apps/docs/content/docs/auth-setup.ko.mdx apps/docs/content/docs/auth-setup.zh.mdx SELF_HOSTING_ADVANCED.md | rg '^\+[^+]' | rg -o '[0-9]+' | sort -u
200
67
91
99

$ for f in apps/docs/content/docs/environment-variables.mdx apps/docs/content/docs/environment-variables.ja.mdx apps/docs/content/docs/environment-variables.ko.mdx apps/docs/content/docs/environment-variables.zh.mdx apps/docs/content/docs/auth-setup.mdx apps/docs/content/docs/auth-setup.ja.mdx apps/docs/content/docs/auth-setup.ko.mdx apps/docs/content/docs/auth-setup.zh.mdx SELF_HOSTING_ADVANCED.md; do rg -c 'EMAIL_TEMPLATE_DIR' "$f"; done
1
1
1
1
1
1
1
1
2
```

`pnpm --dir apps/docs typecheck` 未能执行：运行镜像无 `node_modules`，修正 noexec pnpm shim 后原始终止为
`sh: 1: fumadocs-mdx: not found` 与 `Local package.json exists, but node_modules missing`。未为本卡片
下载整套 monorepo Node 依赖；交给 QA/CI 补跑。

### 2.6 REFACTOR 与机械防漂移

- `sanitizeSubjectField` 与 `buildInvitationParams` 从 BASE 提取后 `diff -u` 均为空。
- `email_test.go` 的 diff hunk 只在 import、BASE 505 之后的 harness、BASE 730 之后的新测试；
  `email_test.go:270-415` 无 hunk、零 diff。
- `verificationTemplateData` 与 `invitationTemplateData` 用反射锁死字段顺序/集合、全部 `string`，并显式
  排除 `template.HTML/URL/JS/CSS/Srcset`。
- 写集为冻结方案声明的 13 个业务文件，未动 `handler/auth.go`，未捎带 #7577。

## 3. 单元与整体回归

| unit_id | worktree/分支 | 写到的文件 | 单元测试结果 |
|---|---|---|---|
| U1 | 自动本地分支 `agent/agent/52fb424651d1`，远端 `feature/email-template-customization` | r4 §10 的 13 个业务文件 | `go test -race ./internal/service` PASS；`go vet ./internal/service` PASS |

完整命令 `PATH=/usr/local/go/bin:$PATH GOTMPDIR=... bash scripts/test-go.sh --race` 已运行，但本 agent
容器的全仓回归不绿，失败与 U1 写集无关且可复现为运行环境耦合：

1. `/workspaces/.multica/daemon_task_context.json` 是平台 marker，导致普通 CLI 用例被判成 agent task；
2. 容器 umask 为 `0077`，使 storage 的 `0644` 用例得到 `0600`；
3. 可执行临时目录只有 `/workspaces`，而其祖先必含 marker；`/tmp`、`/home/agent`、`/dev/shm` 均
   `noexec`。把测试临时目录移到 `/dev/shm` 可消掉 marker，但所有构造假 CLI 的用例会反过来报
   `permission denied`。

原始代表性输出与最终相关包事实：

```
--- FAIL: TestAgentCopySameRuntimeCopiesPortableFields
    runAgentCopy: agent execution context requires MULTICA_TOKEN to be a task-scoped mat_ token
--- FAIL: TestLocalStorageUploadsAreWorldReadable
    buffered mode = 600, want 0644
ok   github.com/multica-ai/multica/server/internal/service  23.763s
FAIL
```

因此不把 T19 写成 PASS；QA 应在无 task marker、umask 022、可执行临时目录的 CI 等价环境补跑完整
`bash scripts/test-go.sh --race`。本卡片直接受影响的完整 `internal/service` race 包已绿。

写集实际与方案声明一致，无并行单元合并。

## 4. 与方案的偏差

| 位置 | 方案说 | 实际是 | 处理 |
|---|---|---|---|
| TDD 细粒度 | 每组生产行为先看到 RED | 加载/发送主链先红；失败语义细分用例在共同骨架后补、首次即绿 | 如实列为验证薄弱处，不伪造 RED；QA 可重点变异失败回落与日志泄漏断言 |
| T19 | `bash scripts/test-go.sh --race` 全绿 | 平台 marker、umask 与 noexec 三者使全仓无可同时满足的本地环境 | 保留原始失败；相关 service 包 race 全绿；交 QA/CI 补跑 |
| docs typecheck | 文档检查 | Node 依赖未安装 | 未擅自安装整套依赖；机械内容核对已做，交 QA/CI 补跑 |

无方案层冲突；未命中新的 `PLAN_GAP` 或 `RISK_ROUTE_GAP`。

## 5. 交付事实

```
$ git rev-parse 0a37dbada5bd9a6cd9f955252fec38631a7bf1cd 0a37dbada5bd9a6cd9f955252fec38631a7bf1cd^{tree} 0a37dbada5bd9a6cd9f955252fec38631a7bf1cd:server
0a37dbada5bd9a6cd9f955252fec38631a7bf1cd
ecc74e58c87472d9e9aa1722fdcae955428bab99
dfd11c9a1efdfb4e8bf6c7a13321cd7f4d2d75e2

$ git push origin HEAD:refs/heads/feature/email-template-customization
To https://github.com/xiaoyaner0201/multica
   35687f4f5..0a37dbada  HEAD -> feature/email-template-customization

$ git ls-remote origin refs/heads/feature/email-template-customization
0a37dbada5bd9a6cd9f955252fec38631a7bf1cd refs/heads/feature/email-template-customization
```

- 分支：远端 `feature/email-template-customization`，已推 fork：是
- 冻结 commit / tree：`0a37dbada5bd9a6cd9f955252fec38631a7bf1cd` /
  `ecc74e58c87472d9e9aa1722fdcae955428bab99`
- publication notes：`coder/20260826-164006-U1-publication-notes.md`

## 6. 交接给验证

验证需重点补查：

- 在干净 CI 等价环境跑全仓 `scripts/test-go.sh --race` 与 docs typecheck；
- 变异运行期 Execute 告警，确认哨兵验证码断言真能捕获 `data=%+v` 类泄漏；
- 真实 MTA 对超长 Subject 行的处置、真实部署挂载方式、真实零值诊断假阳性率仍未覆盖；
- `handler/auth.go` 的 TTL 与 service 常量仍是 BASE 既有的两处耦合，本次没有跨包修；
- U1 上游维护者取向、U3 #7577 合入时点、U5 真实生产并发量、U6 四语言翻译一致性 CI 均保持
  `UNVERIFIED`。

本卡片保持 `in_progress`，`done` 由验证实测后写。

