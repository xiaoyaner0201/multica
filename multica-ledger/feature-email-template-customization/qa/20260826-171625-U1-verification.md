---
ts: 20260826-171625
agent: qa
action: verification
tree: ecc74e58c87472d9e9aa1722fdcae955428bab99
verdict: RETURN
---

# 验证：U1 邮件模板自定义冻结代码树

## 1. 冻结对象与独立清单

- 候选 commit：`0a37dbada5bd9a6cd9f955252fec38631a7bf1cd`
- 候选 tree：`ecc74e58c87472d9e9aa1722fdcae955428bab99`
- `server/` tree：`dfd11c9a1efdfb4e8bf6c7a13321cd7f4d2d75e2`
- BASE：`f74a71060fcdd45318cae6fb7caf7c0d7e71cdfa`
- tracking 远端：`origin/feature/email-template-customization`
- 独立章程：`qa/20260826-165358-U1-charter.md`

独立章程在读取 Plan 与 Coder 账本前冻结，共 12 项（C01–C12）：exact tree、四组件独立加载与
三时机回落、字段闭包、验证码日志泄漏、Subject 控制字符/200 rune/998 octet、最终收发方与
头部边界、SMTP QP/8BITMIME 与 Resend sink、内置兼容、DEV 旁路、50 并发、四语言文档与
部署配置、周边 caller/全仓回归。最危险的反例是验证码从运行期错误日志泄漏、失败模板发出
半截正文，以及并发发送发生跨收件人混载。

盲测边界如实降级：触发评论不可避免地携带了 Coder 摘要；但本清单确实在读取 Plan/Coder 分录前，
基于完整业务 diff、周边代码、调用方与既有/新增测试形成并提交。不能宣称控制面级完全 blind。

## 2. 并集范围

最终范围 = 独立章程 C01–C12 ∪ r4 方案 T01–T27（含 b/c/a 子项，共 33 项）∪ 仓库既有相关回归。

| 来源 | 范围 | 结果 |
|---|---|---|
| 独立章程 | C01–C11 | PASS |
| 独立章程 | C12 的相关 `internal/service` / `internal/handler` | PASS |
| 方案 | T01–T18、T20–T27 | PASS |
| 方案 | T19 全仓 `scripts/test-go.sh --race` | **UNVERIFIED** |
| 既有回归 | `internal/service` 完整 race 包、既有 invitation/SMTP 测试 | PASS |
| 全仓回归 | 未改动 CLI/storage 等包 | 当前运行环境失败，不能给 PASS |

## 3. 动态探针

### P01 — exact tree、tracking 与写集闭包

**预期**：候选三个锚点精确匹配；HEAD 只比候选多账本；候选后业务 diff 为空；BASE→候选只改
冻结写集；远端 tracking 指向派单分支。

```text
$ git rev-parse 0a37dbada5bd9a6cd9f955252fec38631a7bf1cd 0a37dbada5bd9a6cd9f955252fec38631a7bf1cd^{tree} 0a37dbada5bd9a6cd9f955252fec38631a7bf1cd:server
0a37dbada5bd9a6cd9f955252fec38631a7bf1cd
ecc74e58c87472d9e9aa1722fdcae955428bab99
dfd11c9a1efdfb4e8bf6c7a13321cd7f4d2d75e2

$ git diff --name-only 0a37dbada5bd9a6cd9f955252fec38631a7bf1cd HEAD -- . ':(exclude)multica-ledger/**'
<空输出，退出码 0>
```

实际与预期一致。结论绑定候选 tree `ecc74e58…`；其后的提交只允许 append-only 账本演进。

### P02 — 完整 service race 包与静态检查

**预期**：生产加载/发送入口、SMTP relay、Resend 最终请求、50 并发及既有 service 回归全绿，
无 race；vet 无诊断。

```text
$ cd server && umask 022 && GOTMPDIR=/workspaces/hd53-qa-go-tmp.Hg20Dq GOCACHE=/workspaces/hd53-qa-go-cache.aNz3dV /usr/local/go/bin/go test -race ./internal/service -count=1
ok  	github.com/multica-ai/multica/server/internal/service	24.670s

$ cd server && GOTMPDIR=/workspaces/hd53-qa-go-tmp.Hg20Dq GOCACHE=/workspaces/hd53-qa-go-cache.aNz3dV /usr/local/go/bin/go vet ./internal/service
<空输出，退出码 0>
```

判定：PASS。断言层不是 helper：SMTP 用 loopback relay 抓 envelope 与完整 DATA；Resend 用
`SendEmailRequest` 最终入参；50 个并发请求按 `To` 逐封核对 marker，没有跨请求混载。

### P03 — 加载、三时机回落、兼容与最终载荷

**预期**：四文件独立可选；缺目录/缺文件/parse/零值诊断/运行期 Execute 符合三阶段语义；
失败组件单独回落且不发半截结果；DEV 在渲染前返回；内置 SMTP wire（掩蔽 Date/Message-ID）兼容；
QP 与 8BITMIME 两分支、CJK 边界及 SMTP/Resend sink 一致。

关键原始输出如下（同一完整定向命令退出码 0）：

```text
=== RUN   TestCustomTemplateSMTPWireSafetyAndTransferEncoding/8BITMIME=false
=== RUN   TestCustomTemplateSMTPWireSafetyAndTransferEncoding/8BITMIME=true
--- PASS: TestCustomTemplateSMTPWireSafetyAndTransferEncoding (0.00s)
=== RUN   TestEmailTemplateLoadingFailureSemantics/missing_directory_falls_back_once
=== RUN   TestEmailTemplateLoadingFailureSemantics/missing_files_are_silent
=== RUN   TestEmailTemplateLoadingFailureSemantics/parse_failure_is_isolated
=== RUN   TestEmailTemplateLoadingFailureSemantics/unknown_field_remains_active_and_falls_back_at_runtime
=== RUN   TestEmailTemplateLoadingFailureSemantics/zero-value_false_positive_does_not_disable_template
=== RUN   TestEmailTemplateLoadingFailureSemantics/zero-value_false_negative_falls_back_without_partial_output
--- PASS: TestEmailTemplateLoadingFailureSemantics (0.00s)
=== RUN   TestBuiltInVerificationSMTPWireRemainsCompatible
--- PASS: TestBuiltInVerificationSMTPWireRemainsCompatible (0.00s)
=== RUN   TestBuiltInInvitationSMTPContentRemainsCompatible
--- PASS: TestBuiltInInvitationSMTPContentRemainsCompatible (0.00s)
=== RUN   TestRenderedCJKSubjectSMTPBoundary
--- PASS: TestRenderedCJKSubjectSMTPBoundary (0.00s)
=== RUN   TestCustomTemplateSMTPAndResendSinksMatch
--- PASS: TestCustomTemplateSMTPAndResendSinksMatch (0.00s)
=== RUN   TestEmailTemplatesAreSafeForConcurrentUse
--- PASS: TestEmailTemplatesAreSafeForConcurrentUse (0.00s)
PASS
ok  	github.com/multica-ai/multica/server/internal/service	3.017s
```

实际与预期一致。T01–T18、T22–T27 的动态部分 PASS。

### P04 — 字段闭包与 Subject 管线

**预期**：验证码字段严格为 `{Code, ExpiresInMinutes, AppName}`，邀请字段严格为
`{InviterName, WorkspaceName, InviteURL, AppName, AppURL}`，均为 `string` 且无 trusted bypass
类型；九个控制码全部移除；Subject 满足 200 rune、998 octet、最长前缀、幂等与精确边界。

```text
=== RUN   TestEmailTemplateDataContract/verification
=== RUN   TestEmailTemplateDataContract/invitation
--- PASS: TestEmailTemplateDataContract (0.00s)
=== RUN   TestSubjectSanitizersStripTheSameControlCodePoints/U+0000
=== RUN   TestSubjectSanitizersStripTheSameControlCodePoints/U+0009
=== RUN   TestSubjectSanitizersStripTheSameControlCodePoints/U+000A
=== RUN   TestSubjectSanitizersStripTheSameControlCodePoints/U+000B
=== RUN   TestSubjectSanitizersStripTheSameControlCodePoints/U+000C
=== RUN   TestSubjectSanitizersStripTheSameControlCodePoints/U+000D
=== RUN   TestSubjectSanitizersStripTheSameControlCodePoints/U+007F
=== RUN   TestSubjectSanitizersStripTheSameControlCodePoints/U+0085
=== RUN   TestSubjectSanitizersStripTheSameControlCodePoints/U+009F
--- PASS: TestSubjectSanitizersStripTheSameControlCodePoints (0.00s)
=== RUN   TestSanitizeRenderedSubjectProperties
--- PASS: TestSanitizeRenderedSubjectProperties (2.92s)
=== RUN   TestSanitizeRenderedSubjectExactBoundary
    email_test.go:899: derived exact boundary with 198 runes
--- PASS: TestSanitizeRenderedSubjectExactBoundary (0.03s)
```

判定：PASS。Resend 最终 Subject 与 SMTP 两种 CTE 的 wire 测试也覆盖同一清洗结果，未停在纯函数。

### P05 — 文档、部署与 docs typecheck

**预期**：`.env.example` 与 compose 暴露同一变量；四语言变量表/auth 文档及高级文档同步；
`AppName` 固定 `Multica` 与 `{{.Code}}` Subject 泄漏风险明示；用户可见新增数字只有
`67/91/99/200`；docs typecheck 通过。

```text
$ rg -n 'EMAIL_TEMPLATE_DIR' .env.example docker-compose.selfhost.yml SELF_HOSTING_ADVANCED.md apps/docs/content/docs/environment-variables{,.ja,.ko,.zh}.mdx apps/docs/content/docs/auth-setup{,.ja,.ko,.zh}.mdx
apps/docs/content/docs/auth-setup.zh.mdx:66:把 `EMAIL_TEMPLATE_DIR` 指向挂载到 API 容器中的目录。以下文件均可独立选用：
apps/docs/content/docs/auth-setup.ko.mdx:66:`EMAIL_TEMPLATE_DIR`에 API 컨테이너에 마운트한 디렉터리를 지정합니다. 다음 파일은 각각 선택 사항이며 독립적으로 사용할 수 있습니다.
apps/docs/content/docs/auth-setup.mdx:66:Set `EMAIL_TEMPLATE_DIR` to a directory mounted in the API container. The following files are independently optional:
apps/docs/content/docs/auth-setup.ja.mdx:66:`EMAIL_TEMPLATE_DIR` に API コンテナへマウントしたディレクトリを指定します。次のファイルはそれぞれ任意で、個別に利用できます:
apps/docs/content/docs/environment-variables.zh.mdx:99:| `EMAIL_TEMPLATE_DIR` | 空 | 可选的自定义邮件模板目录；API 启动时加载 |
apps/docs/content/docs/environment-variables.ko.mdx:99:| `EMAIL_TEMPLATE_DIR` | 비어 있음 | 선택적 사용자 지정 이메일 템플릿 디렉터리. API 시작 시 로드됨 |
apps/docs/content/docs/environment-variables.mdx:99:| `EMAIL_TEMPLATE_DIR` | empty | Directory containing optional custom email templates; templates are loaded when the API starts |
apps/docs/content/docs/environment-variables.ja.mdx:99:| `EMAIL_TEMPLATE_DIR` | 空 | 任意のカスタムメールテンプレートを置くディレクトリ。API の起動時に読み込まれます |
docker-compose.selfhost.yml:81:      EMAIL_TEMPLATE_DIR: ${EMAIL_TEMPLATE_DIR:-}
SELF_HOSTING_ADVANCED.md:55:Set `EMAIL_TEMPLATE_DIR` to a directory mounted in the backend container. Each file is optional and overrides only its corresponding built-in component:
.env.example:252:EMAIL_TEMPLATE_DIR=

$ git diff f74a71060fcdd45318cae6fb7caf7c0d7e71cdfa 0a37dbada5bd9a6cd9f955252fec38631a7bf1cd --unified=0 -- apps/docs/content/docs/auth-setup.mdx apps/docs/content/docs/auth-setup.ja.mdx apps/docs/content/docs/auth-setup.ko.mdx apps/docs/content/docs/auth-setup.zh.mdx SELF_HOSTING_ADVANCED.md | rg '^\+[^+]' | rg -o '[0-9]+' | sort -nu
67
91
99
200

$ COREPACK_HOME=/workspaces/hd53-qa-corepack.aeVoSk corepack pnpm --dir apps/docs typecheck
> @multica/docs@0.2.0 typecheck /workspaces/140e8421-7099-4023-91fd-4c09406f46a2/e1aa43426322/workdir/multica/apps/docs
> fumadocs-mdx && tsc --noEmit

[MDX] types generated
```

实际与预期一致；typecheck 退出码 0。T20/T21 PASS。自然语言翻译质量没有母语人工审校，属于明确
不覆盖，不把构建通过扩大成翻译质量结论。

### P06 — BASE 防漂移

**预期**：`buildInvitationParams`、`sanitizeSubjectField` 与五个既有 invitation 测试函数相对 BASE
零 diff；`email_test.go:270-415` 无改动。

实际：逐函数从 BASE 提取后 `diff -u` 均为空；指定行段零 diff；caller `handler/auth.go` 与
`handler/invitation.go` 未改。判定 PASS。

## 4. RED 敏感性：候选外定向 mutation

四组 mutation 都在候选 commit 的独立临时 worktree 中完成；没有修改候选 tree，也没有把 mutation
提交到远端。

### M01 — 运行期告警泄漏 data

Mutation：把告警改成 `data=%+v`。预期：哨兵验证码探针 RED。实际：按目标失败。

```text
email_test.go:1244: runtime warning leaked data or omitted fallback: EmailService: failed to render template verification_code.subject.tmpl; using built-in content: template: verification_code.subject.tmpl:1:2: executing "verification_code.subject.tmpl" at <.SMTPPassword>: can't evaluate field SMTPPassword in type service.verificationTemplateData data={Code:918273 ExpiresInMinutes:10 AppName:Multica}
--- FAIL: TestEmailTemplateLoadingFailureSemantics/unknown_field_remains_active_and_falls_back_at_runtime (0.00s)
```

说明：T16 不是假绿；它能在真实运行期告警中捕获验证码多 sink。

### M02 — Execute 失败误发半截 buffer

Mutation：错误分支返回 `buf.String(), true`。预期：完整内置回落探针 RED。实际：按目标失败。

```text
email_test.go:1281: runtime failure did not use complete built-in body: "prefix "
--- FAIL: TestEmailTemplateLoadingFailureSemantics/zero-value_false_negative_falls_back_without_partial_output (0.00s)
```

说明：T15/T14c 真实约束最终邮件内容，不只检查 helper 的 bool。

### M03 — 禁用 `unicode.IsControl`

预期：九个控制码、Resend 最终 Subject、SMTP 两种传输编码均 RED。实际：全部按目标失败。

```text
email_test.go:840: sanitizeRenderedSubject = "before\x00after", want "beforeafter"
...
email_test.go:840: sanitizeRenderedSubject = "before\u009fafter", want "beforeafter"
email_test.go:1041: Subject = "Code 918273\r\nBcc: evil@example.com\t"
email_test.go:1145: Subject = "你好\r\nBcc: evil@example.com\t"
--- FAIL: TestCustomTemplateSMTPWireSafetyAndTransferEncoding/8BITMIME=false (0.01s)
--- FAIL: TestCustomTemplateSMTPWireSafetyAndTransferEncoding/8BITMIME=true (0.00s)
```

说明：清洗断言已经到 Resend sink 和 SMTP wire，两通道不依赖 QEncoding 偶然兜底。

### M04 — 验证码数据新增 `AppURL`

预期：冻结字段闭包 T26 RED。实际：按目标失败。

```text
email_test.go:784: fields = 4, want exactly 3; adding template data requires re-evaluating the frozen field-admission rule
--- FAIL: TestEmailTemplateDataContract/verification (0.00s)
```

说明：契约测试会阻止未来把“改期望值”当作普通维护；新增字段必须回到字段准入规则重审。

## 5. 阻断性 EVIDENCE_GAP：T19 全仓 race

**预期**：在 CI 等价环境中 `bash scripts/test-go.sh --race` 全绿。

**实际**：`internal/service` 与周边 `internal/handler` 在完整脚本中 PASS，但未改动 CLI/storage 包失败。
代表性原始输出：

```text
$ umask 022 && PATH=/usr/local/go/bin:$PATH GOTMPDIR=/workspaces/hd53-qa-go-tmp.Hg20Dq GOCACHE=/workspaces/hd53-qa-go-cache.aNz3dV bash scripts/test-go.sh --race
--- FAIL: TestAgentCopySameRuntimeCopiesPortableFields
    runAgentCopy: agent execution context requires MULTICA_TOKEN to be a task-scoped mat_ token
--- FAIL: TestLocalStorageUploadsAreWorldReadable
    buffered mode = 600, want 0644
ok   github.com/multica-ai/multica/server/internal/service  23.763s
FAIL
```

本轮用 `umask 022` 已消除 storage 权限分量；CLI 最小复现仍稳定失败：

```text
$ cd server && umask 022 && /usr/local/go/bin/go test ./cmd/multica -run '^TestAgentCopySameRuntimeCopiesPortableFields$' -count=1 -v
=== RUN   TestAgentCopySameRuntimeCopiesPortableFields
    cmd_agent_copy_test.go:97: runAgentCopy: agent execution context requires MULTICA_TOKEN to be a task-scoped mat_ token
--- FAIL: TestAgentCopySameRuntimeCopiesPortableFields (0.00s)
FAIL
FAIL	github.com/multica-ai/multica/server/cmd/multica	0.009s
FAIL

$ stat -c '%n %F' /workspaces/.multica/daemon_task_context.json
/workspaces/.multica/daemon_task_context.json regular file

$ git diff --name-only BASE candidate -- server/cmd/multica server/internal/cli
<空输出，退出码 0>
```

显式 unset `MULTICA_TASK_CONFIG_ROOT`、`MULTICA_DAEMON_PORT`、`MULTICA_TASK_ID` 后仍会被
`/workspaces/.multica/daemon_task_context.json` fail closed 判为 agent task。可执行临时目录必须位于
`/workspaces`，其祖先必含 marker；其他临时目录为 `noexec`。`unshare -m` 不获许可，且容器内没有
Docker/Podman/bwrap/proot，无法构造“无 marker + umask 022 + 可执行临时目录”的 CI 等价环境。

因此 T19 判定 **UNVERIFIED**，不是 FAIL：有充分证据说明当前红项不在候选写集，但没有全绿结果就
不能把计划符合性写成 PASS。所需闭环动作是在无 marker、umask 022、可执行临时目录的 CI 等价环境
以前景阻塞命令补跑同一条全仓 race；取得全绿原始输出后重新绑定同一 exact candidate tree 验证。

## 6. 两类结论

| 维度 | 结论 | 依据 |
|---|---|---|
| `PLAN_COMPLIANCE` | **UNVERIFIED** | T01–T18、T20–T27 PASS；T19 因环境无法取得全仓全绿证据 |
| `SYSTEM_BEHAVIOR` | **PASS** | 冻结生产路径、最终 SMTP/Resend 载荷、回落、日志、安全边界、兼容与并发均有动态证据；四类关键探针取得目标 RED |
| Overall | **RETURN** | 存在阻断性 `UNVERIFIED`，按独立章程不能给 Overall PASS |

本轮没有发现 `IMPLEMENTATION_DEFECT`，也没有发现 `PLAN_GAP`。`SYSTEM_BEHAVIOR=PASS` 不外推到
真实外部 MTA、真实 Resend 服务、宿主机 volume mount 或上游接受取向。

## 7. Finding 与未覆盖

| ID | 分类 | 严重度 | 影响 | 路由 |
|---|---|---|---|---|
| F1 | `EVIDENCE_GAP` | blocker | T19 缺少 CI 等价环境的全仓 race 全绿证据，阻止 Overall PASS | 调度安排无 task marker 的等价环境补跑；不是实现返工 |

保留的非阻断未覆盖：

- U1 上游维护者是否接受该产品形态；U3 #7577 合入时点；U4 真实部署挂载方式。
- U5 真实生产负载下的并发量（本轮仅证明 50 并发、race 无告警且无混载）。
- U6 四语言母语一致性审校（docs typecheck 已 PASS）；U7 真实 MTA 对极长 Subject 的处置。
- U8 零值启动诊断在真实模板样本中的假阳性率。
- `handler/auth.go` TTL 与 service 文案常量的跨包耦合是 BASE 既有问题，本次未修。
- `FRONTEND_ORIGIN` 空值仍回落 `https://multica.ai`、`AppName` 固定 `Multica`、Subject folding 后续取舍，
  必须留在 publication notes，不能在公开 PR 描述中遗漏。

## 8. 代码树绑定与交接

候选后业务 diff 为零；本报告及独立章程均绑定 exact candidate tree
`ecc74e58c87472d9e9aa1722fdcae955428bab99`。任何业务代码树变化都会使本结论失效。

角色绑定要求“环境无法验证置 `blocked`”；方法模板末尾的通用文字写“实测完成置 done”。两者冲突时
按角色绑定优先，本卡片应置 **blocked**，等待 F1 的等价环境证据，而不是把 RETURN 误写成已闭环。
