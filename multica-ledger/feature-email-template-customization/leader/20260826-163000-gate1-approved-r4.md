---
ts: 20260826-163000
agent: leader
action: gate1-approved-r4
tree: 34631e84
server_tree: df467331
verdict: APPROVED
supersedes: leader/20260826-152000-gate-pg16-route.md §2「r3 解冻」的悬置状态
---

# 人工闸门 1（第二次）：放行。r4 恢复冻结，stage 2 在原卡片继续

放行人：dongsjoa（HD-51 发起人）。放行时间 20260826，评论见 HD-51。
**执行依据自本分录起为 `planner/20260826-154500-plan-r4.md`**
（sha256 `4e7fef61b821b1ab015d0ab2802766384ef1eb22d98f8829f8c7db8b9fc4ce0e`），
r3 自 20260826-152000 解冻后**不再是任何阶段的执行依据**，账本 append-only 原样保留。

## 1. 两项裁决

### PG16：照准——删除 `verificationTemplateData.AppURL`，维持硬规则

放行人独立复核了 BASE（`FRONTEND_ORIGIN` 在 `email.go` 仅 `:369` 一处、位于
`SendInvitationEmail`；`SendVerificationCode` 不读也不渲染任何 URL），确认两条规定无法同真，
并确认「能力增量为零」是决定性理由：`AppURL` 是部署期常量，运维方本就持有；`Code` 是服务端
为每封信现生成，运维方不可能预知，二者不同类。

**代价已被明确接受**：运维方换域名时验证码模板里的链接要自己改，域名在两处维护。
接受理由是凭据类邮件授权面应最小、且方向可逆（将来要加容易，加了再收回难）。

放行人另点明重写后的判据是本轮最有价值的产出：字段 F 准入 ⟺ 存在确定性 `g` 使
`F = g(该封信在改动前已产生的主题与正文字节)`，`g` 只能截取拼接格式化，不得查库、读环境、读文件。
判据落在**每一封信**而非整个代码库，因此能同时正确放行 `InviteURL` 与拒绝验证码侧 `AppURL`。

### PG20：选 (b)——保留 `AppName`，但文档必须写明它固定渲染 `Multica`

放行人复核了 BASE 主题字面量 `Your Multica verification code`（`:350` 与 `:359` 两处），
确认 `AppName` 通过新判据无问题，**同时确认我指出的不对称属实**：
「运维方自己就知道」这条理由对品牌名比对域名更成立，r4 只回答了准入判据、没回答决定性判据。

不选 (a) 删除，理由是删了模板作者会失去一个**保持与内置主题一致**的引用方式。
不选 (c) 新增品牌名配置项，理由是它违反 r4 刚立的判据，而
**「刚立的判据第一次遇到不便就修订，判据就没有约束力了」**——这句话本身是本次交付的方法论结论，
比这个字段的去留重要，记在这里。

## 2. 三条追加约束（与冻结方案同等效力）

r4 已冻结、本次不重走 stage 1，因此以下三条**只存在于本分录**。
**它们不是建议，效力等同 r4 正文**；实现按此执行，评审按此检查，不得以「不在方案里」为由跳过。

| # | 约束 | 落点（必须能被指出的具体产物） | 检查方 |
|---|---|---|---|
| 追加约束三 | 四语言文档必须明确写出：`{{.AppName}}` **固定渲染为 `Multica`**；要用自己的品牌请直接在模板里写字面量 | r4 **T21** 的四语言文档面（含 `.env.example` 同步项） | 评审 |
| 追加约束四 | PR 描述主动交代 PG20 这个取舍（为什么留 `AppName` 却不让它可配） | 实现的 `coder/<ts>-publication-notes.md` | 评审 |
| 追加约束五 | PR 描述用一句话点出：`FRONTEND_ORIGIN` 为空时兜底为硬编码 `https://multica.ai`（`email.go:370-372`），是**既有行为、本次未改** | 同上 | 评审 |

追加约束三的理由（放行人原话性质的转述）：不写清楚，自建方会写 `{{.AppName}}` 然后收到
印着**我们**品牌名的邮件——对一个专为自定义品牌而做的功能，这是陷阱。

追加约束五的理由：这处兜底不在本次改动面内、**不修**，但与本功能高度相关——自建方装完看到
邀请信指向 multica.ai，会以为是本次功能引入的 bug。一句话点明可避免误判。

追加约束四、五与上一轮的追加约束一同理：
**主动交代和被追问后解释，在 review 里是完全不同的两件事。**

## 3. 其余维持，不重开

路线 A、PG1（剥控制字符契约）、PG2（三时机分离）、Q1–Q4 原裁决、Q5a（998 octet 上限）、
Q5b（按 rune 截断，含「纯中日韩主题 200→91 字」写进四语言文档）、
追加约束一（PR 描述交代两条路径 998 处理不一致）、
追加约束二（header folding 记为已知正解并说明为何不做，已落为 r4 §5.2）——**全部维持**。

PG15 非阻塞约束继续有效：文档面只许用 **91 / 99 / 200 / 67** 这几个可复算的数，
不得复活 `71` / `151`。

r4 自查新增的 PG17（主题可放 `{{.Code}}`，允许但 T21 四语言必须警告）、
PG18（`ExpiresInMinutes` 从 `service` 包常量取值 + T27，不修 `auth.go` 跨包耦合，列为 U10）、
PG19（DEV 分支渲染前返回，stdout 与 BASE 逐字相同）三项裁决随 r4 一并生效。

## 4. 流程修订（放行人当场宣布，本分录记录为正本变更）

> 实现期发现的方案缺陷若影响半径是**局部的**（字段取舍、判据歧义、局部矛盾），
> **就地裁决后由规划补修订分录，实现在原卡片继续，不再新建方案卡片重走整轮**。

PG16 正属此类。据此本次**不新建方案卡片**，HD-53 直接恢复，不再走一次 stage 1。

这条修订的代价要写在明处：省下的是一整轮 stage 1 的 Run 与闸门，
付出的是**冻结方案与实际要求之间出现落差**——本次落差就是第 2 节那三条。
所以第 2 节才必须显式绑定落点与检查方；没有那张表，这条流程修订就是在制造无人负责的口头约束。

## 5. 状态与路由

- HD-51 父 Issue：`in_progress`（放行人已置，我不重复写）。
- HD-53【实现】：**已由放行人提升为 `todo` 并唤醒实现**。
  我因此**不再 @ 实现**——同一份工作既提卡片又 @ 人会触发两个并行 Run。
- HD-54【评审】：保持 `backlog`，stage 2 全部 `done` 后由 barrier 唤醒我再提升。
- 未解决：无阻塞项。非阻塞待办 = 追加约束一/三/四/五（文档与 PR 描述面）+ PG15 数字白名单。

## 6. 过程摩擦

**M-L25（新）：闸门追加约束现在漂在冻结方案之外，靠 leader 分录承载，这是新引入的结构性风险。**
上一轮的追加约束一/二能被规划折进 r4，是因为 r4 本来就要重写。这一轮不重走 stage 1，
三条新约束**只存在于本分录**。评审在盲审阶段对照的是**冻结方案**，很可能判定它们「超出方案范围」
而不检查，于是约束静默失效——而且失效得毫无痕迹，没有任何一步会报错。
本分录用第 2 节那张「约束 / 落点 / 检查方」表来对冲，但那是我这次手工补的，不是制度。
**建议正本明确**：闸门追加约束与冻结方案同等效力；每条必须在放行分录里绑定
**可被指出的具体产物**与**检查方**；评审的对照基准定义为「冻结方案 + 全部放行分录的追加约束」，
而不是冻结方案单独一份。第 4 节那条流程修订如果不配这一条，就是净负收益。

**M-L26（新）：放行与提升卡片由放行人一次做完，我到达时实现已被唤醒——约束落账本存在竞态。**
时间线：放行人在同一条评论里宣布放行、提升 HD-53、唤醒实现；我被触发后读到 HD-53 已是 `todo`、
父 Issue 已 `in_progress`。也就是说**实现可能在本分录 push 之前就完成了 `git pull`**，
那它拿到的 r4 里没有第 2 节的三条约束，而它不会知道自己少拿了东西。
这次我一收到触发就先 pull、先写分录、先 push，把窗口压到最小，但窗口不为零。
**这不是要求放行人多做一步**——放行人直接提卡片省掉了一次纯中转的 Run，方向是对的。
**建议正本改的是实现侧**：实现开工的第一步不是「`git pull` 然后照方案做」，
而是「`git pull` → 读 `index.md` 头部确认当前执行依据与状态 → 检查是否存在**晚于冻结方案时间戳**的
leader 分录，有则一并纳入」。把「有没有新约束」变成实现的一次主动检查，竞态就与推进顺序解耦了。

**M-L27（新，记一条正向的）：这一轮我提出的非阻塞 finding 被采纳并当场裁决，代价是一条文档要求。**
PG20 如果我没在核对时提出，最可能的结局是：实现照 r4 做完、评审也照 r4 通过、
到上游 review 时被问「`AppURL` 因为运维方自己能敲所以删了，`AppName` 呢」——
那时改的是已经过了两道闸门的交付物。
把它判为**非阻塞但必须呈报**（而不是「非阻塞所以只记在账本里」）是这次做对的地方：
非阻塞决定的是**要不要停下来**，不决定**要不要说**。这两件事上一轮被我合并处理过，是错的。
