---
ts: 20260826-152000
agent: leader
action: gate-pg16-route
tree: a9bfbabc
verdict: RETURN
supersedes: leader/20260826-150500-gate1-approved.md §7「stage 2 启动」
---

# PG16 路由：复算成立，r3 解冻，退规划出 r4

被核对产物：`coder/20260826-145530-U1-plan-gap-r3.md`（verdict `RETURN`，finding `PG16`）。
所审对象所在 commit：`f28786239`。业务子树 `HEAD:server` = `df467331`，自 BASE 起零变更
（`git diff --stat BASE HEAD -- . ':(exclude)multica-ledger'` 输出为空），与实现「未进入 RED/GREEN、
无候选代码树」的自述一致。

## 1. 我自己复算的结果：PG16 成立，阻断

实现的分录我不采信文字，逐条重算。四项证据全部独立复现：

| # | 断言 | 我的复算 | 结果 |
|---|---|---|---|
| 1 | 方案 §7.1 结构体含 `AppURL string // FRONTEND_ORIGIN` | `awk` 取 r3 `:925-940`，`:933` 逐字为 `    AppURL           string // FRONTEND_ORIGIN` | 成立 |
| 2 | 同节硬规则第 3 条禁止新增既有不可见字段 | `awk` 取 r3 `:952-965`，`:958-960` 逐字为「**绝不把 `*EmailService`、配置对象或 `os.Environ()` 的产物放进数据结构**……验证码模板的数据结构**只能包含内置模板今天已经渲染出来的信息**，一个字段都不多」 | 成立 |
| 3 | BASE 验证码路径不读也不渲染 `FRONTEND_ORIGIN` | `grep -n FRONTEND_ORIGIN server/internal/service/email.go` 全文**仅 1 处**：`:369`，位于 `SendInvitationEmail`。`SendVerificationCode`（`:340-364`）正文只渲染 `code` 与固定文案，主题为字面量 `"Your Multica verification code"`，无应用 URL | 成立 |
| 4 | 方案自己已提出该问题但未裁决 | `grep -n AppURL` 全文仅 3 处：`:933`（验证码结构体）、`:941`（邀请结构体）、`:1244`（L2 `must_answer` 5）。`:1243-1244` 逐字为「验证码模板的数据结构中，每个字段是否都是内置模板今天**已经渲染出来**的信息？`AppURL` / `AppName` 的引入是否带进了内置模板没有的新信息？」——是**提问**，正文无对应裁决 | 成立 |

**裁决：`PLAN_GAP` 成立且阻断。** 这是方案同一节内部的互斥，不是实现口味差异：
按结构体写就违反硬规则，按硬规则写就不实现结构体，实现无论选哪边都在替规划做数据授权面的决定。
实现停在复核阶段、零业务 diff、不自签 PASS，处置正确。

**`AppName` 不在本 finding 范围内**，我同意实现的划界：BASE 主题字面量 `Your Multica verification code`
已含品牌名 `Multica`，`AppName` 有现有落点，通过「今天已经渲染出来」这一检验。

**顺带记一处方案措辞缺陷（不单列 finding，并入 PG16 交规划一并处理）**：硬规则第 3 条前半句
「绝不把……`os.Environ()` 的产物放进数据结构」若按字面读，会连邀请模板的 `InviteURL` 一起禁掉
——而 `InviteURL` 由 `FRONTEND_ORIGIN` 派生（`email.go:369,373`）且**今天确实渲染**，本就该允许。
可见该条真正起作用的判据是后半句「今天已经渲染出来的信息」，前半句是过宽的表述。
规划在 r4 里要把这条判据写成单一、可机械执行的形式，否则下一个实现还会撞上同一处歧义。

## 2. r3 解冻

r3 自 20260826-134000 冻结、20260826-145500 核对 PASS、20260826-150500 经人工闸门 1 放行；
现因 PG16 **解冻**，不再是 stage 2 的执行依据。账本 append-only，r1/r2/r3 三份连同各自的
gate 分录一律原样保留，不修改、不删除。

## 3. 闸门 1 的裁决与两条约束**继续有效**，不重开

这一条要写清楚，否则 r4 会退回去重议已经拍板的事。放行人在 20260826-150500 已明确
「其余裁决我复核后维持——路线 A、PG1 剥控制字符契约、PG2 三时机分离、Q1–Q4 原裁决，全部维持，不重开」。
PG16 是 §7.1 一处内部互斥，**不触及路线选择、不触及 Q1–Q5 的任何一条**，因此：

- 路线 A、PG1、PG2、Q1–Q4 原裁决：**维持**，r4 不得重开。
- Q5a（998 octet 上限）、Q5b（按 rune 截断）：**维持**，含「纯中日韩主题 200→91 字」这一用户可见约束
  必须写进四语言文档。
- 追加约束一（PR 描述主动交代两条路径 998 处理不一致）：**维持**，落点仍是实现的
  `coder/<ts>-publication-notes.md`。
- 追加约束二（写明 folding 是正解、本次截断是范围内取舍、folding 同时能修内置路径 1349 octet 越界）：
  **维持**，落点是方案与 PR 描述——即 r4 必须自带这一节。
- PG15（emoji `71` / `mixed 151` 构造不可复算）：**维持**，非阻塞，文档面只许用 91 / 99 / 200 / 67。

**r4 = r3 + PG16 裁决 + 上述约束就地落地**，不是重写。规划不得借 r4 重开已放行的方向。

## 4. r4 之后是否要再走一次人工闸门 1

**要走，但只走 PG16 这一处。** 判据是：PG16 的两条候选出路里，「保留 `AppURL` 并撤回硬规则」
会**扩大验证码邮件的模板数据授权面**——把一个部署级环境变量新暴露给一个今天不含任何 URL 的
凭据类邮件。HD-51 正文三条安全点里就有数据暴露面一条，这类决定按项目路由不归我裁量。
若规划选另一条出路（删 `AppURL`、维持硬规则），授权面收窄而非放大，闸门可以极短。
两种情况我都不代发起人决定，但闸门正文只呈 PG16，不复述已放行内容。

## 5. 路由

| finding | 判定 | 去向 | 卡片 |
|---|---|---|---|
| `PG16` | 成立、阻断 | 规划（数据授权面裁决，实现不代决） | 新建【方案 r4】卡片，沿用 stage 1 |

HD-53【实现】stage 2 由实现自行置 `blocked`，处置正确，保持 `blocked`，待 r4 过闸门后由我提升。
HD-54【评审】stage 3 保持 `backlog`。HD-56 已 `done`，按正本不复用，r4 另建新卡片。

## 6. 过程摩擦（本次交付要求各角色如实记录）

**M-L18（新）：PG16 是我上一轮 gate 该抓到而没抓到的。**
我在 `…-145500-gate-plan-r3.md` §3 走了七项语义判据，其中就有「数据白名单是否成立」。我当时
核的是白名单**存在且字段全为 `string`、无 `template.HTML`**，也就是核了这份清单的**形式**，
没有拿清单里的**每一个字段**去和 BASE 的实际渲染输出做逐字段回归。四个字段里只要有一个
没有现有落点，同节硬规则就被自己破掉——而这正是实现开工第一步就撞上的。
我的漏检不是疏忽某一行，是**判据本身停在了结构层**。
**建议正本给 gate 增加一条可机械执行的判据**：凡方案声明「数据白名单/最小集/一个字段都不多」
一类的封闭性契约，核对方必须**逐元素**对照 BASE 找出现有落点，找不到落点的元素当场列为 finding；
只核结构不核元素等于没核。这条对不变量矩阵、路径清单、写集同样适用。

**M-L19（新）：方案把一个未决的设计裁决挂成了评审 lens 的 `must_answer`，而我没识别出来。**
r3 `:1243-1244` 把「`AppURL` 是否带进了新信息」写成 L2 的 `must_answer` 5。表面上这是尽责——
问题被写下来了；实质上是**把一个必须在方案阶段拍板的授权面决定，推迟到了评审阶段**。
评审是验证，不是裁决；等到评审才发现要改数据契约，代价是整条实现链返工。
更糟的是它对核对方有伪装性：`must_answer` 越具体越像证据充分，我上一轮正是把它读成了「规划想得很细」。
**建议正本明确两条**：(a) `must_answer` 只能承载**可证伪的验证探针**，不得承载未决的设计选择；
(b) gate 增加一项——逐条扫 `must_answer`，凡问句的答案会**改变方案本身**（而不是确认方案成立）的，
一律判为方案未完成，当场 RETURN。本次若有这条判据，PG16 会在 145500 就被我抓住，
而不是让实现白起一个 Run、让闸门 1 白批一次。

**M-L20（新）：闸门 1 放行与实现开工在同一分钟并发，我的放行分录写「stage 2 启动」时它已经不成立了。**
时间线：14:49 放行人提 HD-53 为 `todo`；14:55:30 实现写下 PG16 分录并 push；15:05 我写放行分录，
其中 §7 写「stage 2 启动」——写的那一刻 stage 2 已经因 PG16 停住，我不知道。
我是在 `git push` 被拒、rebase 撞冲突时才发现远端多了一条分录的。**账本救了这次**：
非 fast-forward 拒绝是一个硬信号，我无法忽略它。但这依赖运气——若我和实现改的不是同一个文件，
push 会直接成功，我的「stage 2 启动」就会作为既成事实留在头部，与实际状态相反。
**建议正本明确**：调度写任何带状态断言的分录前，必须先 `git pull --ff-only` 并重读 `index.md`
表格尾部；分录里的状态断言（「启动」「进行中」「已完成」）只能描述**已 pull 到的事实**，
不能描述自己即将触发的动作。本分录据此 supersede 上一条分录的 §7。

**M-L21（新）：`index.md` 的 tree 列在本次出现了两种读法并存，实证了 M-L17。**
实现的 145530 行写 `dc5a0f97`（写作时 HEAD 的全仓 tree），我的 150500 行写 `51feb441`
（所审对象所在 commit 的 tree）。两条相邻的行用了互斥的定义，账本无从判断哪个是对的。
这不再是我一个人的困惑，而是已经发生的不一致。M-L17 的建议（tree 列定义固定为「所审对象所在
commit 的 tree」，一致性判据绑定业务子树而非全仓 tree）由此**从建议升级为必须**：
纯账本提交会改变全仓 tree，用全仓 tree 做绑定会让每写一条分录就作废上一条的绑定。
本分录 front matter 的 `tree` 字段本身也无法自指——它记录的必然是提交前的状态，
这个自指缺陷正本也要一并处理。

## 7. 交接

- 结论：`RETURN`。r3 解冻，退规划出 r4。
- 分支：`feature/email-template-customization`（intake 定死，全链不变）。
- 下一步：新建【方案 r4】卡片（stage 1，assignee 规划，`todo`）。r4 只裁决 PG16 并就地落地
  §3 列出的全部既有约束，不重开已放行方向。r4 冻结后由我核对，再走一次只呈 PG16 的人工闸门 1。
- 未解决：PG16（阻断）、PG15（非阻塞，带进文档面）。
- 保持状态：HD-53 `blocked`，HD-54 `backlog`。
