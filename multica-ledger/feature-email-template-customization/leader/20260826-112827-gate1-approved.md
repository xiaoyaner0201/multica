---
ts: 20260826-112827
agent: leader
action: gate1-approved
tree: 37fb17c83b3da66a0cf25107afc9785e63e6c29a
verdict: PASS
---

# 人工闸门 1：发起人放行

发起人 dongsjoa（千乘妍）在父 Issue HD-51 上明确放行方案 r1
（`planner/20260826-110423-plan-r1.md`，sha256 `e70497a2…`，subject tree `37fb17c8…`）。
本分录记录放行内容与由此产生的约束，供 stage 2/3 引用。

## 四个待决项的裁决（全部采用方案默认取值）

| | 裁决 | 对下游的效力 |
|---|---|---|
| Q1 首版范围 | 只做验证码 + 邀请两封邮件的 Subject 与 HTML 正文 | 固定 non-goal 边界，实现不得自行扩到第三封邮件或纯文本 part |
| Q2 多语言 | 首版不做。理由被确认成立（验证码邮件发生在用户记录存在之前，无可靠 locale 来源） | 可证伪条件 #9 仍然有效——若上游要求首版就做多语言，仍按 `PLAN_GAP` 退回 |
| Q3 主题长度 | 渲染后 200 rune 上限，与 `maxSubjectFieldRunes = 60` 的逐字段语义**分离** | INV3 的两层语义被确认，不得把 60 复用到整条主题上 |
| Q4 文档面 | 扩到 11 个文件；`docker-compose.selfhost.yml` 属硬需求 | P19 从「规划补入」升格为发起人确认的硬需求，实现不得省略 |

## A1（上游取向）的处置：并行，不阻塞

发起人已在上游 Issue #7569 提交方案摘要征求维护者意见，并裁定**实现与上游问询并行推进**。

同时确认了规划的一个判断：A1 的裁决者是上游维护者而非发起人，因此规划按派单要求未发追问
评论、把它留在闸门 1 是正确处置。**这一条澄清了方法正本「HIGH_RISK 先与发起人追问」的适用
边界**——追问的对象必须是能裁决该问题的人；发起人无法裁决的假设，追问只会产生一轮空转。
记入流程摩擦供协议修订参考。

若维护者回复倾向 DB + 管理界面：按 `PLAN_GAP`（方案遗漏）退回规划重做，已投入的 harness
与测试用例大部分仍可复用。这与方案 §9 可证伪条件 #1 一致，路由不变。

## 实现阶段追加的三条要求（发起人提出，我已复算）

### 1. 先补 mock relay harness

发起人称「BASE 的 `testSMTPServer` 只丢弃 DATA 内容（`:632-640`）」。**独立复算确认为真**：

```
$ sed -n '620,640p' server/internal/service/email_test.go
                dataLine := readLine()
                if dataLine == "." {
                        break
                }
        }
        writeLine("250 OK")
$ grep -n "ReceivedData\|ReceivedMailFrom" server/internal/service/email_test.go
（空）
```

DATA 逐行读入后直接丢弃，无任何捕获通道。T01–T10 全部依赖抓取 `MAIL FROM` 与完整 `DATA`
载荷的能力，而 PR #7577 未合入（见 `leader/20260826-111707-gate1-plan.md` §3），
因此这个 harness 必须自建。

发起人的定性值得原样保留：**它是「让目标行为变得可观测」的 RED 准备，不是被测改动。**
这条区分对 QA 有直接效力——harness 本身不构成 INV4 所说的「既有行为变更」，
但它落在 `email_test.go` 内，与要求 2 的零 diff 区间必须互不重叠。

### 2. `email_test.go` 既有测试区间零 diff

INV4 唯一的机械证据，也是对上 Bohan-J「既有部署零变更」偏好的凭据。
计数以修正后为准：**5 个顶层测试函数**（`:270` / `:343` / `:359` / `:376` / `:396`），
不是方案里笔误的 6 个。

注意要求 1 与要求 2 的张力：harness 要加在 `testSMTPServer`（`:600` 附近）上，
零 diff 区间是 `:270-415`，两者不重叠，但实现必须在交接时用 `git diff` 显式证明这一点，
不能只是声称。

### 3. §14「最容易做错的四处」逐条对照后再交接

- 不给 `buildInvitationParams` 加参数（否则 5 个既有测试的调用点全产生 diff，INV4 的机械证据消失）
- 不把已 `html.EscapeString` 的字符串喂给模板（二次转义，`&amp;` → `&amp;amp;`，INV7）
- 不把 SMTP writer 交给 `Execute`（渲染中途失败会发出半截邮件，INV10）
- 渲染失败的告警不记录数据结构或渲染结果（那里面有验证码，INV1）

## 状态推进

- 父 Issue HD-51：`in_review` → `in_progress`
- HD-53【实现】stage 2：`backlog` → `todo`
- 账本状态：PLAN_GATE_PASS → IMPLEMENTING
- HD-54【评审】stage 3 保持 `backlog`，stage 2 barrier 后由我提升

Council 提名 L1/L2/L3（含反对者 L3）已在方案 Gate 校验通过，发起人确认按提名执行，
在 stage 3 由评审承担，实现阶段不执行。

## 交接

- 状态：IMPLEMENTING
- 产物：冻结方案 `planner/20260826-110423-plan-r1.md`，BASE `f74a71060`，subject tree `37fb17c8…`
- 下一步：HD-53 实现按冻结方案落地（先补 harness），完成后卡片内 @ 验证实测，由验证置 `done`
- 未解决：上游 #7569 维护者取向（U1/A1），并行推进不阻塞；若回复倾向 DB+UI 则按 `PLAN_GAP` 退回规划
- 需要决策：无
