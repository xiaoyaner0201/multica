---
ts: 20260826-183500
agent: leader（调度）
action: 闸门 2 核对（独立复算，不采信报告文字）
tree: ecc74e58c87472d9e9aa1722fdcae955428bab99
verdict: RETURN（存在开放 high F1；四维未全 PASS；不判 READY）
---

# 闸门 2 核对：`ecc74e58` — 不放行 READY

本分录只写**我自己复算出来的**：核对了哪些项、探针的原始输出、本轮新发现的事实。
**不代评审转述结论**——评审的四维结论由它自己在 HD-51 直接汇报，两条并列可读。

## 1. 事实层（自己重算，全部一致）

| 项 | 声明值 | 我复算 | 结论 |
|---|---|---|---|
| 账本 HEAD | `b079019b23e7882cf90c60ed4ecde1c525e06880` | 同 | ✅ |
| HEAD == remote `feature/email-template-customization` | — | `b079019b2`，逐字符相同 | ✅ |
| 工作树 clean | — | `git status --porcelain` 空 | ✅ |
| candidate tree | `ecc74e58c87472d9e9aa1722fdcae955428bab99` | `git rev-parse 0a37dbada^{tree}` 同 | ✅ |
| 候选之后只有账本演进 | — | `git diff --name-only 0a37dbada HEAD -- . ':(exclude)multica-ledger/**'` 输出为空 | ✅ |
| BASE 是 HEAD 祖先 | `f74a7106` | `git merge-base --is-ancestor` 通过 | ✅ |
| Pass 1 产物 sha256 | `0088fc73…913b97` | 同 | ✅ |
| Pass 2 产物 sha256 | 未在分录内自述 | `593b4594f4dd580f2f4a09c0f6a4abbb583c8b44fa89bb94ab94f8ff9e243407` | 记录在此 |
| 评审 §0 引用的 `plan-r4.md` | `4e7fef61` | 同 | ✅ |
| 评审 §0 引用的 `gate1-approved-r4.md` | `17ed99a6` | 同 | ✅ |
| 评审 §0 引用的 `gate-t19-evidence-gap.md` | `59d3c812` | 同 | ✅ |
| qa 章程 / 验证书 sha256 | `bd222b4c…` / `bc73c056…` | 同 | ✅ |

评审引用的每一个 digest 都对得上，且它**确实读了 `gate1-approved-r4.md`**——这一点见 §5。

## 2. F1 的独立复现（不采信报告的三行输出）

评审的 F1 复现走的是 Resend sink。我不复用它的探针，改在仓库外一个独立 Go 程序里
直接考察 `text/template` 与 `html/template` 的语义，再回到 `email.go` 复核调用链。

原始输出（`go1.27.0`，仓库外 `probe-f1/`，跑完即删，`git status --porcelain` 复核为空）：

```
empty         parse=(<nil>,<nil>) diagnose=(<nil>,<nil>) exec=(<nil>,<nil>) subject="" html=""
whitespace    parse=(<nil>,<nil>) diagnose=(<nil>,<nil>) exec=(<nil>,<nil>) subject="   \n\t\n" html="   \n\t\n"
comment_only  parse=(<nil>,<nil>) diagnose=(<nil>,<nil>) exec=(<nil>,<nil>) subject="" html=""
```

**三个环节全部无错**：`Parse` 成功 → 零值 `diagnoseTemplate` 成功（因此不告警）→
运行期 `Execute` 成功（因此不回落）。回到代码：

- `loadTextEmailTemplate` / `loadHTMLEmailTemplate` 只在**三种**情形返回 `nil`：
  `os.IsNotExist`、读取错误、`Parse` 错误。空文件三条全不命中 → 模板被判为「已覆盖」；
- `renderEmailTemplate` 在 `Execute` 返回 `nil` 时**无条件**返回 `(buf.String(), true)`；
- 调用方 `SendVerificationCode` / `buildInvitationParamsFromTemplate` 见 `ok == true` 即
  **覆盖**内置的 `subject` / `body`。

**F1 成立。** 严重度 high 我同意：验证码邮件是自建部署的唯一登录通道。

### 2.1 我把 F1 的影响面比评审记的又扩了两处

**(a) SMTP 通道同样无守卫——评审只在 Resend 侧复现过。**
我通读 `sendSMTP` 全部返回点：守卫只有 `fromEmail` 为空一条，
`subject` 与 `htmlBody` 从入参到 `fmt.Fprintf(w, "%s%s", headers, bodyBytes)` 之间
**没有任何长度或非空判断**。即空渲染在**两条通道上都**产出一封格式合法的空邮件，
不是单通道现象。这一点评审的证据没有覆盖，我补上。

**(b) 更容易踩中的是「只空一个文件」，而不是评审复现的「两个都空」。**
四个组件独立可选，因此 `touch verification_code.html.tmpl` 一条命令就够：
主题仍是内置的 `Your Multica verification code`（正确），正文为空（无验证码）。
**这比两个都空更隐蔽**——运维方看到邮件在发、主题正常，只有收件人知道信里没有码。
评审的三个变体（`empty` / `whitespace` / `comment_only`）都同时写了 subject 与 html
两个文件，没有覆盖这个半空形态。**属 F1 的影响面扩大，不是新 finding。**

## 3. F2 的独立复核（代码事实，非重跑评审探针）

- `maxSubjectFieldRunes = 60`（`email.go:30`），只被 `sanitizeSubjectField` 使用（`:587`/`:591`）；
- `buildInvitationParamsFromTemplate` 装入模板的是**原始** `inviterName` / `workspaceName`，
  未过 `sanitizeSubjectField`；
- 模板路径的 Subject 改由 `sanitizeRenderedSubject` 收口，我逐行读完：它只做
  ①剥 `unicode.IsControl` ②整条截到 `maxRenderedSubjectRunes = 200` rune
  ③按 `maxSubjectLineOctets = 998` 逐 rune 退让。**没有任何逐字段上限。**

**F2 成立。** 逐字段 60 rune 的钓鱼额度在模板主题路径上确实不生效，
且 `workspaceName` 是跨租户最终用户可控字段——§7.2「能力止于运维方」的信任边界论证
对它不适用。评审判 medium、判 `INVARIANT_VIOLATION`，我同意。

## 4. F3 的独立复核

对四语言 `auth-setup*.mdx`、`SELF_HOSTING_ADVANCED.md`、`.env.example` 全量 grep
`trust|Trust|受信|信頼|신뢰|writable|non-admin|同等`：命中 7 处，**逐条读完，无一处与
`EMAIL_TEMPLATE_DIR` 有关**（分别是 SMTP TLS 信任库、`COOKIE_DOMAIN` 同域信任、
XFF 信任白名单）。交付文档面确实没有承载 §7.2 的信任边界结论。**F3 成立。**

## 5. 语义判据

| # | 判据 | 结论 |
|---|---|---|
| a | 四维结论是否完整 | **完整**。四维齐备且各自给出依据，不是打分 |
| b | `SYSTEM_BEHAVIOR` 是否 PASS | 验证侧 `SYSTEM_BEHAVIOR = PASS`、`PLAN_COMPLIANCE = UNVERIFIED`，**未全绿** |
| c | **有无开放的 high 或 blocker** | **有：F1（high，可复现，我已独立复现并扩大影响面）** |
| d | `UNVERIFIED` 是否如实列出 | **如实**。T19 与 DB 面**逐字带到闸门 2 且明写「不得写成 PASS」**，U1 单列并标注不可验原因，另承接 U3–U8/U10 |
| e | 结论是否绑定同一 tree | 是，四维中的 Exact Tree 一项本身即 PASS，我复算一致 |

**判据 c 单独成立即不放行 READY。** 我不投票、不做多数决：
一条可复现的 high 不被其他三维的 PASS 覆盖。

### 5.1 M-L25 的实测结果：预测的失效**没有发生**

上一轮我记下的结构性风险是「追加约束漂在冻结方案之外，评审对照冻结方案时很可能判它们
超出范围而不检查，且失效毫无痕迹」。本轮实测：评审 §0 把
`leader/20260826-163000-gate1-approved-r4.md` §2 列为**必读材料**并注明「追加约束三/四/五
只存在于本分录」，§1.7 逐条给出落点与 PASS。**三条约束全部被检查到。**

但**这不构成对 M-L25 的证伪**：本轮成立是因为派单正文钉死了指针，而不是因为正本规定了
对照基准。M-L25 的建议维持不变——正本应把评审的对照基准定义为
「冻结方案 + 全部放行分录的追加约束」。**一次没塌不等于结构稳**。

我另行机械复核了同样三条（在 stage 2 barrier 时做的，当时刻意不外发以免污染盲审）：
追加约束三在四语言 `auth-setup{,.zh,.ja,.ko}.mdx` 均在；
追加约束一/四/五在 `coder/20260826-164006-U1-publication-notes.md` 均在；
PG17 的 `{{.Code}}` 主题警告四语言均在；PG15 数字白名单干净（只有 91/99/200/67，
各 5 处，无 `71`/`151`）。**与评审 §1.7 独立得出的结论一致。**

## 6. 返工路由裁决

评审给出的去向我复核后**全部照准**，理由独立成立：

| ID | 分类 | 去向 | 我的复核依据 |
|---|---|---|---|
| **F1** | `PLAN_GAP` high | **规划** | 我在 `plan-r4.md` 与 qa 章程全文搜 `空模板/空文件/空输出/产出为空/TrimSpace/仅注释/纯空白/空主题/空正文`，**唯一命中是一行 BASE 代码引用**，与本议题无关。§7.3 降级表实数**恰好五行**，空渲染五行全不命中。`email_test.go` 的 `writeEmailTemplate` **五个调用点写的全是非空内容**。方案侧未建模、验收侧无探针，**实现忠实执行了方案** → 去向是规划 |
| **F2** | `INVARIANT_VIOLATION` | **规划** | 修它要定「跨租户字段 vs 运维方字段」的截断契约，是取舍裁决不是补丁 |
| **F3** | `PLAN_GAP` | **规划** | T21 增一项，纯文档，无代码 |

**Pass 1 → Pass 2 的两条撤销（F4 TTL 双字面量、F5 内置主题 1349 octet）我照准。**
两条在 r4 里都有显式记录（PG18 + T27 + U10；INV11 + non-goal 10 + U7 + §5.2），
是有边界的 out-of-scope，不是遗漏。评审只改分类、不改代码事实判断，做法正确。

**当前不路由到验证。** F1/F2 的判据没定之前补探针，等于把裁决权推给写测试的人。

### 6.1 局部还是整体：我判**增量修订，不是重写**

路线 A、字段准入判据、`work_partition`、INV1–INV13 中新建立的那批、27 项验收里除 F1 面
以外的部分，**均未被本轮 finding 触动**。需要新增的是三块：
①「渲染成功但输出为空」的判据 + 降级表新行 + 不变量 + 验收项；
②模板数据的截断契约；③T21 一项文档要求。按 r3→r4 的 M-L22 做法（先逐字节 `cp` 提交、
再逐处修改）可把下一轮的核对同样降为一条 diff 命令。

**但我不据此自行起卡片**，理由见 §7。

## 7. 为什么本轮进闸门而不是直接派返工

方法正本写的是「核对 RETURN 时按 finding 路由返工，不进闸门」。该条针对的是
**产物或证据有缺**、不该占用决策者时间的情形。本轮不属于：产物齐备、digest 全对、
评审自身质量高，且**有三项裁决权在本分录之外**：

1. **T19 与 DB 面**——我 20260826-174840 那份分录已经写死「在**闸门 2 由决策者裁定**
   是否须补真实 CI 结果后再判 READY，裁决权不在评审，也不在调度」。这项无论 RETURN 与否
   都必须在闸门 2 兑现，否则是我自己毁自己上一轮的裁决；
2. **U1（上游取向）不可验**——方案 §9 第 1 条列它为最大单点风险；评审 L3-2 另指出一条
   方案未讨论的折中（只把 DB+UI 限定在邀请邮件上，方案 C 重新可行）。
   若上游取向另有答案，F1/F2 的修复轮次可能白做；
3. **F2 的范围本身是取舍**——是收紧模板路径的字段额度，还是接受它并写进文档，
   两条都自洽，属决策者的成本判断。

因此本轮：**父 Issue 置 `in_review`，不判 READY，不起返工卡片。**
若我此刻直接建【方案 r5】卡片，等于替决策者预设了「要再做一轮」这个答案——
与「预建卡片带着旧指针活到下一版」是同型错误，只是方向相反：那次是卡片早于裁决而失效，
这次是卡片早于裁决而僭越。放行后由决策者直接改状态并 @ 目标角色，或指示我建卡片。

## 8. 本轮新查出的两条边角事实（均非阻塞，留痕备 Publication）

**(a) 评审 M12 记的误建远端分支属实，且它不是孤例。**
`refs/heads/agent/agent/4f61e9aedfc5` @ `1d45e62c8` 确实存在于 fork；
我复算 `git merge-base --is-ancestor` 确认该 commit **已完整包含在**交付分支
`b079019b2` 中，账本无丢失。但 fork 上同形态的 `agent/agent/*` 分支**共 29 条**——
这是 `repo checkout` 自动分支名 + 裸 `git push origin HEAD` 的**系统性产物**，
不是评审一次失手。建议正本把「账本推送一律写全 refspec `HEAD:<派单声明的远端分支>`」
写成硬规则。**删除远端分支属显式需升级的不可逆动作，本角色不做，留待有权者清理。**

**(b) fork 上另有 `feat/hd-48-email-template-override`，是同一功能的更早一次尝试。**
我拉下来核对：该分支只有三个 commit、**全部是 `multica-ledger/feat-hd-48-…/` 下的账本文件**，
生产代码零改动，链路停在 BEHAVIOR_DISCOVERY 之后未进入方案阶段。
**不是竞品实现，不影响 Publication 查重**，但它与本次同名同题，
记在这里以免将来有人误当作重复交付。

## 9. 过程摩擦

> 编号勘误：`leader/20260826-174840` 那份分录的 M-L22/23/24 与 `…-161500` 的 M-L22/23/24
> **号段撞车**，两组内容不同。append-only 不改历史，此处留痕；本分录从 **M-L28** 起编，
> 引用 174840 那三条时请连分录路径一起写。

**M-L28（新）：「核对 RETURN 就不进闸门」这条正本判据，与「某些 `UNVERIFIED` 的裁决权
在闸门 2 决策者」这条，在本轮直接冲突。**
两条都是正本，且都不是错的，但没有规定谁优先。我按「谁的裁决权更靠上」解开——
决策者的裁决权优先于我的路由权——但这是我临场的解释，不是正本写明的。
**建议正本补一句**：核对 RETURN 时若存在只有闸门决策者能裁决的事项，仍进闸门，
在同一条评论内并列呈报「RETURN 的 finding 路由」与「待裁决事项」，不拆成两次。
不补这一句，下一个调度遇到同样情形会二选一，而两种选法都能从正本找到依据。

**M-L29（新）：F1 是被「两遍评审、第一遍不读方案」这一条机制逮住的，
不是被任何一份检查表逮住的。**
方案、QA 章程、我的三次 Gate 判据表，**三方都漏了同一类失败**——因为三方都在
「加载失败分三时机」这个心智模型里。QA 章程虽然先于读方案冻结，却已采用「三时机」这个
r4 的专有词汇（评审 M11 已如实记下）。**先冻结不足以隔离模型继承。**
本轮真正起作用的是「第一遍拿不到方案」这个**信息隔离**，而不是「先冻结」这个**时序隔离**。
建议正本把这条差别写明：要真隔离，盲审角色必须**拿不到**上游产物，
而不只是**先于**上游产物冻结自己的清单。

**M-L30（新，记一条针对我自己的）：我这三轮 Gate 的判据表在 F1 这一类上一次都没触发过。**
我的八项语义判据里有「封闭性契约逐元素验证」「具体数字回到管线复算」，
它们防的是**声明了什么却做不到**；F1 是**没声明所以没人检查**——
判据表对「表上没有的那一类」结构性失明。我自己的身份提示里写着
「判据要随轮次演进，且不能与被检查方的自查表完全同构」，这一轮暴露的是更弱的一环：
**不同构还不够，还得有一条判据专门问「这张表本身漏了哪一类」。**
下一轮 Gate 我加一条：对每张穷举表（降级表、不变量集、验收集），
要求作者写出**该表的完备性论证或反例**，而不只是表本身。
