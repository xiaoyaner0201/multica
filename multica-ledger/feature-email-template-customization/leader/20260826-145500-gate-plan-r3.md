---
ts: 20260826-145500
agent: leader
action: gate-plan-r3
tree: 51feb4411
verdict: PASS
---

# 方案 r3 核对：PASS，进闸门 1

被核对产物：`planner/20260826-134000-plan-r3.md`
sha256 `025d33e625872f32d5e9c632ed3bf73cc916387e844e132b87eb779dc7b342ac`，154,058 字节。

裁决：**PASS**。r2 的 RETURN（PG10/PG11）已被裁决并修复，我逐个复算确认；本轮我另发现一处
非阻塞缺陷（PG15，§4），随卡片带进 stage 2，不构成 RETURN。

---

## 1. 事实核对（全部自己重算，不采信报告文字）

| # | 项 | 我跑的 | 结果 |
|---|---|---|---|
| 1 | 远端 = 本地 | `git rev-parse HEAD` vs `git ls-remote origin refs/heads/feature/email-template-customization` | 两者均 `0c44058841059ffcec581afb82dbac5a749eef1d` |
| 2 | BASE 是祖先 | `git merge-base --is-ancestor f74a7106… HEAD` | yes |
| 3 | 工作树 clean | `git status --porcelain` | 空 |
| 4 | 无业务代码改动 | `git diff --stat BASE..HEAD -- . ':(exclude)multica-ledger'` | 空。方案阶段不应产生任何非 ledger 改动，成立 |
| 5 | 产物 sha256 与声明一致 | `sha256sum` | `025d33e6…b342ac`，与卡片交接声明一致 |
| 6 | front matter | 读文件头 7 行 | `ts/agent/action/tree/verdict` 齐全，`verdict: FROZEN` |
| 7 | 账本 append-only | `sha256sum` r1 / r2 | r1 `e70497a2…`、r2 `13daf92c…`，与前两轮记录逐字节相同，未被改写或删除 |
| 8 | `index.md` 有对应行 | `grep -c '20260826-134000'` | 1 |
| 9 | `index.md` 头部分区 | `git diff` 该文件 | 规划本轮只追加了自己的表格行，未动头部。M-L4 的分区问题本轮未复发 |
| 10 | 上一阶段结论未被推翻 | 读 §0.1–0.3 | PG1–PG9 的裁决原文保留、未被 r3 的新结论覆盖 |

**关于 front matter 的 `tree`。** `7f0664f629…` 是提交 `20517654d`（我的重跑分录）的 tree，
即规划落第一笔 r3 之前 HEAD 的 tree——与 r1/r2 两轮的记法一致，是**账本状态戳**。
方案推理所绑定的**代码树**是 BASE 的 `57e81be9…`，而第 4 项已证明 BASE..HEAD 之间不存在任何
非 ledger 改动，所以方案的全部结论仍绑定在当前真实代码上。该项 PASS。

**产物形态的机械验证（本轮新增，是 20260826-130731 那条处置的收益）。**
r3 由 `cp` r2 后定点编辑得到，所以我用 `diff -u r2 r3` 直接读出改动集：**19 处 hunk，
+708 / −70 行**。未列名的章节是文件复制的结果，逐字节不变——这比「原样带过来」这种承诺强，
是可机械验证的。§14 的「r2 → r3 变更清单」与这 19 处 hunk 的位置一一对上，无未申报的改动。
13 个分步提交（`step 1/13` … `13/13`）＋ 一次自纠提交，增量落盘按要求执行。

---

## 2. 数字全量复算（本轮的核心，全部独立重跑，不读方案的记录）

我按 M14 的要求做：**不看 §8.1 的登记结果去核对，而是自己写探针跑一遍，再和 r3 对。**
探针在仓库外 `probe2/` 运行，仓库工作树保持 clean。口径与 INV11 §5.1 一致：
`len("Subject: " + mime.QEncoding.Encode("utf-8", s))`。

| 断言 | r3 写的 | 我跑出来的 | 判定 |
|---|---|---|---|
| 纯 CJK 在 998 之下的最大 rune 数 | 91（996 octet） | `CJK T=998 -> n=91 line=996` | ✅ |
| **PG10 错值溯源** | 「81 来自阈值 900，不是口径错」 | `CJK T=900 -> n=81 line=893` | ✅ **与 r2 §12 探针 D 原文逐字相同**，根因确认 |
| 三种 CJK 文字一致 | 中/日/韩 均 91 | `中 91/996`、`語 91/996`、`한 91/996` | ✅ |
| 中日韩 + 每 8 字一空格 | 99（998 octet） | 按 §12 R1 记的构造（每第 8 个 rune **换成**空格）：`runes=99 line=998` | ✅（详见 §5 M-L11） |
| 纯 4 字节 emoji | 67（994 octet） | `emoji T=998 -> n=67 line=994` | ✅ |
| 纯 ASCII 200 rune | 209 octet | `ASCII 200 rune line=209` | ✅ |
| 量化台阶 | 89→978、90→987、91→996、92→**1018**（+22） | 同上，逐个复现 | ✅ T23 的台阶断言可执行 |
| **T24b 的运行期推导** | `é`×122 + `a`×76 = 198 rune → 998；+1 → 999 | 我实现了 §0.5 描述的确定性双参数枚举：`é/a -> ok=true w=122 n=76 runes=198 plus1=999` | ✅ **推导确实收敛，且第一个命中组合确定** |
| 同上，emoji 组 | `😀`×56 + `a`×136 = 192 rune → 998 | `😀/a -> ok=true w=56 n=136 runes=192 plus1=999` | ✅ |
| **PG11 finding 成立** | 纯 CJK 在 200 rune 内不可命中 998 | `中/a -> ok=false`（枚举全空间） | ✅ finding 成立，(b) 的采纳有依据 |
| **单调性前提**（§5.1，r2 从未陈述） | 非单调步 = 0 | 5 字符集 × 全前缀：`non-monotonic prefix steps = 0` | ✅ T24a 性质③的良定义成立 |
| 控制字符九码位 | `unicode.IsControl` 全 true | U+0000/0009/000A/000B/000C/000D/007F/**0085**/**009F** 逐个：全 true | ✅ 含 r2 里渲染成空白的那两个 |
| **PG13：r2「301 runes (line=379)」** | 任何阈值下复现不出；T=900 得 731、T=998 得 814 | `T=900 -> runes=731 line=900`、`T=998 -> runes=814 line=996` | ✅ **与 r3 给的数逐个相同**；301/379 确实复现不出，删除正确 |
| **PG12：`text/template`** | `server/cmd/multica/help.go:6` 有生产用法 | `grep -rn` 全仓：`help.go:6` 确有 `import "text/template"`，且是 server 生产代码里唯一一处；`html/template` 零处 | ✅ r2 的 R9「两者都没有」确为错，且这是一条会被上游 review 一眼证伪的代价陈述 |
| **PG14：三处行号** | 结构体 `:502-514`、`handleConn` `:544-638`、EHLO `:570-577` | 逐个 `awk` 编号读出：`502 type testSMTPServer struct {` / `514 }`；`544 func (s *testSMTPServer) handleConn` / `638 }`；`570 writeLine("250-test-smtp Hello")` … `577 writeLine("250 OK")` | ✅ 三处全对。P29 写集从 `:502-624` 扩到 `:502-638` 是**有实际后果的订正**，少的那 14 行正是 `handleConn` 的收尾 |

**十六项全部复现。** 没有一项是靠读方案得出的。

### 2.1 我认错：`email.go:227-238` 规划是对的，我上一轮的订正是错的

规划在 §0.6 明确不采纳我的 `:227-238 → :229-238`，并要求我「按证据指出，不按结论改」。
我按它的要求读了字节：

```
227	switch {
228	case smtpHost != "":
229		tlsLabel := "starttls"
233		fmt.Printf("EmailService: SMTP relay %s:%s (%s) from=%s\n", ...)
234	case client != nil:
236	default:
238	}
```

**`227` 是 `switch {`，`238` 是 `}`，`229` 是分支内部的一个局部赋值。**
R12 断言的是「无条件打印一行」，这个性质由三分支穷尽（含 `default`）保证，
必须由 `227`/`238` 这对括号界定；从 `229` 起引会把论证本身丢掉。
**我上一轮的订正是错的，规划的不采纳成立，`:227-238` 保留。**

这条本身不重要，重要的是它暴露了我的一个坏习惯：我上一轮是**数着行号**给出订正的，
没有回头确认那个范围是否覆盖被断言的性质。行号引用的判据不是「起点是不是那一行代码」，
是「这个范围能不能撑起它被用来支持的那句话」。记进 §5 M-L12。

---

## 3. 语义判断（七项判据逐条对 r3）

| 判据 | 结论 | 依据 |
|---|---|---|
| 需求分层是否分开写 | PASS | §1.1 硬需求 / §1.2 既有约束（R1–R15）/ §1.3 合理推导，三层分列且各带出处。r3 只改了 R1/R9/R12/R14 四行内容，分层结构未动 |
| 影响面是否追到最终落点 | PASS | §4.2 P01–P29 覆盖 caller / consumer / 被改函数 / 凭据落点 / 身份来源 / 部署面 / 文档面 ×4 语言 / 测试 harness，每条标「修改 / 不修改但纳入验收」。本轮 P03、P29 的范围订正让写集更准而非更松 |
| 不变量矩阵是否成立 | PASS（且比 r2 强） | INV1–INV12 各有「现状 / 本次约束 / 观测点 / 验收用例」四列。新增 §5.1 把 INV11 的**度量 + 阈值 + 前提**三者写死，并把单调性从隐含依赖提升为显式前提——这正是 PG10 的根因所在 |
| **正反验收是否可执行** | **PASS（r2 在此 FAIL，本轮修复）** | T24 原来要求构造一个恰好 998 octet 的主题，在方案自己的字符集下不可达。r3 拆为 T24a（四条不动点性质，20000 × 5 字符集违例 0）+ T24b（运行期确定性推导，推不出则 `t.Skip`）。**我独立跑通了这套推导**（§2），它确实收敛。T23 也从「~81 附近」改成精确 91/996/1018 三条断言 |
| 可证伪条件是否写得出 | PASS | §9 十条，其中第 10 条直指 INV11 自身的收益可能接近零，且 §2 的 Q5a 把这条质疑摆到了发起人面前而不是藏起来 |
| `work_partition` 是否合法 | PASS | 单一单元 U1，`no_split_rationale` 给了五条具体理由（代码面单文件、文档面不能先于代码定稿、测试与实现须同 Run、harness 与被测耦合、无 migration/无跨模块边界）。写集不相交（只有一个单元）、无环（无 `depends_on`）、责任唯一、覆盖全部标「修改」的 path。P29 的行号订正落在 U1 已有的 `email_test.go` 内，切分不受影响 |
| Council 提名 | PASS | L1（SMTP 传输层与信头文法）/ L2（凭据落点与日志面）/ L3（信任边界与上游可接受性），锚点互异，**L3 承担反对者**，三条 rationale 都引具体 path 或 invariant（`email.go:292`/`:296-307`/`:328`、`handler/auth.go:340`→`email.go:353`、§7.2 的信任边界断言）。名额 3，合规 |

### 3.1 我自己做的 INV × T 可达性交叉核对

PG10/PG11 是我上一轮用这个办法找出来的，本轮照做：对每条带具体数字的断言，问「在方案自己
规定的管线与字符集里，这个观测点到得了吗」。

- T23（996 / 91 / 1018 / 209）：到得了，四个值我全部复现。
- T24a（四条性质）：性质③依赖单调性，前提已被 §5.1 显式陈述并实测，我复算非单调步 = 0。
- T24b：推导可收敛（é/a 命中），且允许 Skip 的理由成立——「998 是否可命中」确实是 Go 编码器
  的性质而非我方代码的性质。**这条设计比我上一轮建议的「把构造串写进测试」好**，我采纳规划的判断。
- T22（九码位）：九个 `IsControl` 全 true，表可执行。
- T21（文档面）：改为分字符集表述后，四语言文档要写的三个数（91 / 更长 / 200）我全部复现。

**未发现新的不可达断言。**

---

## 4. 本轮我发现的一处缺陷：PG15（非阻塞，随卡片带进 stage 2）

§0.4 与 §5.1 的分字符集表里有两行我**复算不出来**：

| 行 | r3 写的 | 我跑的 | 状态 |
|---|---|---|---|
| Emoji（混入 3 字节符号 U+2728 ✨） | 71 rune → 998 | 遍历全部 `😀`×w + `✨`×(71−w) 组合，**无一命中 998** | 复现不出 |
| §12 R2 的 `mixed` | 151 rune → 998 | 同上，构造未知 | 复现不出 |

**根因与 PG13 同型**：这两个数出自 §12 探针 R2 的 **B3 随机采样**（「200k 次随机采样」），
而 `emoji` / `mixed` 这两个字符集的**具体码位构成没有记录**，只有一个标签。
按 r3 自己在 §12 新立的规矩——「标不出『构造』的旧输出一律删除，不留在文档里当证据」——
这两行不合它自己的规。

**为什么不 RETURN**：这两个数**不承载任何决策**。它们不进 T21 的文档文案、不进 T23/T24 的断言、
不进 Q5a/Q5b 的背景、不改变任何裁决。emoji 这一族真正承载文档的是**纯 4 字节的 67**，
我已精确复现；`mixed` 只出现在探针输出里。PG11 的结论（「998 在方案自己使用的字符集下不可达」）
我用**确定性枚举**独立证过，不依赖 B3 的随机采样。

**处置**：不返工，作为约束带进 stage 2 的实现卡片——

1. T21 的四语言文档文案**只许用 91 / 99 / 200 / 67** 这四个数，它们都可由 §12 记录的构造复算；
2. 若实现或验证需要引用 71 / 151，**必须先补出字符集的码位构成再引**，否则删除；
3. 这条与「不得写一个孤零零的数字」是同一条纪律的两面：一个数字要么有构造要么不出现。

---

## 5. 流程摩擦

**M-L9（取代 M-L5，规划的 M14 反驳成立）。**
我上一轮记的 M-L5 是「把检查方的判据交给被检查方自查，会降低检查方的实际投入」，
建议不要提前给检查表。规划反驳：本轮起作用的变量不是投入多少，是**检查方跑没跑**——
PG10 要在 998 阈值下重跑枚举才看得见，PG11 要真去构造那个 998 才知道构造不出来，
**任何形式的「读一遍 + 打勾」，投入再大也产不出这两条**。

**我接受这条反驳，M-L5 按此修订。** 本轮是它的第二次实证：我这一轮读方案**没读出任何东西**，
唯一的新发现（PG15）来自跑探针；而我唯一一条靠「读 + 数行号」给出的订正（`:229-238`）
恰好是错的。正确表述是：

> **核对方必须用产物中记录的构造独立复算每一个具体数字，不看产物记录的结果。**
> 复算通过才算核对过；「读一遍觉得合理」不构成核对。

补一条规划没说、但本轮成立的观察：**提前给检查表并没有害处，它的收益是 §8.1 那张数字出处表**——
正因为规划把每个数字登记了出处与构造，我的复算才是逐条可定位的而不是漫无目的的重跑。
所以对策是「检查表照给，但核对方必须跑」，不是「不给检查表」。

**M-L10（新）：我的 Gate 检查表里没有「这个数字第三方能不能复算」这一项，而它是本链路两次
finding 的共同形态。** PG13（r2 的 301/379）与 PG15（r3 的 71/151）是同一种病：
一个数字被写进文档，但没人——包括作者自己——能再跑出来。我的 `templates/gate.md` 事实核对
清单查的是 sha256、tree、clean、push、index 行，**全是产物层面的完整性，没有一项查内容的可复算性**。
**建议正本给 gate.md 的事实核对加一项**：
「产物中每一个进入断言或文档面的具体数字，核对方必须能用产物自带的构造独立复算；
复算不出的一律降级为非证据（删除或标注），不得留在文档里。」

**M-L11（新）：核对方复现不出来，第一反应应当是「我用的构造对不对」，不是「这是个 finding」。**
我第一次跑「中日韩 + 空格」得到 98 rune / 992 octet，与 r3 的 99/998 不符，
我当时已经准备把它写成 finding。回去读 §12 探针 R1 才发现构造写得很清楚：
**每第 8 个 rune「换成」空格，而我做的是「插入」空格。** 按记录的构造重跑，一次命中 99/998。

这条有两层意思。一是**记录构造确实有用**——它让一次分歧在一步之内收敛，这正是 r3 新立那条
纪律的直接收益，我拿自己当了实验对象。二是**核对方的复现失败不是证据**，它先是一个关于
核对方自己的假设。区分办法很机械：构造记了没有？记了的话我照做了没有？两问都过之后
才轮到怀疑产物——PG15 正是两问都过（构造根本没记）才成立的。

**M-L12（新）：行号订正的判据是「范围能否撑起它支持的那句话」，不是「起点是不是那行代码」。**
§2.1 的认错。我上一轮是数着行号提的订正，没回头问那个范围被用来证明什么。
`:229-238` 在字面上不算错——229 确实是代码——但它丢掉了「三分支穷尽 ⇒ 无条件」这个论证。
**建议正本在 finding 模板里要求：引用行号的 finding 必须写出「该范围支持的断言是什么」**，
写不出来就说明这条 finding 还没想清楚。

**M-L13（新）：规划有一条不同意上游 finding 的意见，本轮只能靠「我恰好读到了 §0.6」才被处理。**
这是规划的 M15。它写在方案正文里，没有任何结构化字段承载，我若跳过那一节就会漏掉一条
**被检查方明确拒绝的 finding**——而那条恰好是我错了。这是运气，不是流程。

---

## 6. 对规划三条正本建议的裁决

规划在 §13 提了三条，都在我的职责范围内，逐条给结论：

**M15 —— 返工处置补 `REJECTED` 取值：采纳。**
返工卡片目前是单向的：我写 finding，规划逐条处置，「我认为这条不成立」无处可放。
本轮的证据是压倒性的——**唯一一条被拒绝的 finding，恰好是我错了**。一个只能承载「接受」的
流程，会系统性地把检查方的错误固化进产物。
**本 Gate 起我按此执行**：返工卡片的每条 finding 必须有显式处置取值
`ACCEPTED` / `REJECTED`（附证据）/ `DEFERRED`（附理由与去向），核对时**逐条检查，
`REJECTED` 必须被核对方明确确认或推翻**，不得沉默通过。本轮我在 §2.1 明确确认了那一条。
**升级给正本**：把这三个取值写进 `multica-engineering-delivery` 的返工处置定义。

**M16 —— cp + 定点编辑法必须配一份自指内容检查表：采纳。**
规划说得对：`cp` 得到的文件里，所有自指内容会**静默地**变错，不报错，只是变成一份自称是 r2
的 r3。本轮它列了 front matter / preamble / §0 导览 / §13 引言 / §14 清单五处并逐个改对。
**固化为清单**：① front matter 的 `ts`/`action`/`tree`；② preamble 的「本文取代 X」与 sha256；
③ 目录/导览里的轮次划分；④ 摩擦节的「本轮新增 N 条」计数；⑤ 交接节的产物路径与变更清单标题；
⑥ 全文 `grep -n '本轮\|本卡片\|r[0-9]'` 逐个确认指向。
**升级给正本**：与 M-L7（增量落盘）合并成一条完整的「大体量产物生产方法」。

**M17 —— 按范围执行时撞见的范围外事实错误，一律修订并单独成节标注：采纳。**
两个替代方案都更差：静默修订让核对方无法区分授权与越权；放着不管是明知有错还交出去。
r3 的 §0.7 就是正确形态——PG12/PG13/PG14 全部修订，单独成节，一眼看得出哪些是卡片要求的、
哪些是自行扩大的。**我本轮的核对因此变得可能**：我知道该往哪里多看。
**升级给正本**，并补一句规划没说的边界：**扩大范围只限于「事实错误」（数字、行号、引用错），
不含路线、裁决、切分等语义判断**——后者撞见了要报 finding 给调度路由，不能自行改。

---

## 7. 结论与去向

- 事实核对 10/10 PASS；数字复算 16/16 PASS；语义判据 7/7 PASS。
- r2 的 RETURN 事由（PG10 错值、PG11 判据不可达）已裁决并修复，我独立复算确认。
- 规划自查追加的 PG12/PG13/PG14 全部属实，我逐条复现。
- 我上一轮的一条订正（`email.go:229-238`）**是错的**，规划的不采纳成立（§2.1）。
- 本轮我新发现 PG15（两个不可复算的数字），**非阻塞**，作为约束带进 stage 2（§4）。

**verdict: PASS → 进人工闸门 1**，父 Issue HD-51 置 `in_review`，@ 发起人 dongsjoa，
提交 Q5a / Q5b 两问与 §9 可证伪条件、UNVERIFIED 清单（U1–U8，其中 U7 分量本轮上升）。

**HD-53（stage 2 实现）保持 `blocked`。** stage barrier 提示可以提升 stage 2，
但 HD-53 的前置条件是**闸门 1 放行**，不是 stage 1 全部 `done`。未放行前提升等于绕过闸门。
