---
ts: 20260826-063943
agent: leader
action: intake + 风险分级 + Discovery dispatch
tree: —
verdict: —
---

## Intake

Issue HD-47（`b9e8d88d-45b3-4d6c-a832-cb22decafb88`），Source Contract 见 Issue 正文与上游
`multica-ai/multica#7568`。

### Base 重新读回

Issue 声明 base 为 `8fda4f22d5429e8ece1fc56fdc7ef10cf4fd49d2`，读回 live `upstream/main` 后已前进：

- 真实 base：`620f99d8ce168ee56bfcfacf359b3bcca30f21bf`
- base tree：`9fb8974cdc4038f9c42a99f3fbf1613729f89def`
- 漂移量：1 commit，`git diff --stat 8fda4f22d upstream/main -- server/internal/service/email.go
  server/internal/service/email_test.go .env.example` 为空 → 目标面无漂移。

后续所有 Stage 一律以 `620f99d8` 为 BASE，不使用 Issue 正文里的旧 SHA。

### 缺陷坐标在 live base 上复核

`server/internal/service/email.go` @ `620f99d8`：

| 行 | 内容 | 角色 |
|---|---|---|
| L114 | `func resolveFromEmail(smtpHost string) string` | `SMTP_FROM_EMAIL` → `RESEND_FROM_EMAIL` 回退顺序（不变量 3） |
| L259 | `return fmt.Errorf("SMTP_FROM_EMAIL or RESEND_FROM_EMAIL is required when SMTP_HOST is set")` | 空值现有错误（不变量 2） |
| L292 | `encodedSubject := mime.QEncoding.Encode("utf-8", subject)` | `Subject:` 走 RFC 2047 的对照点 |
| L309 | `if err = c.Mail(s.fromEmail); err != nil` | 信封，缺陷 1 |
| L319 | `headers := "From: " + s.fromEmail + "\r\n"` | 头部裸拼，缺陷 2 |

`.env.example` L263–264 的文档措辞（"envelope/header sender"）与 L279 `SMTP_FROM_EMAIL=` 亦在
范围内。Issue 正文引用的行号与 live base 完全一致，Source Contract 未失效。

## 风险分级：HIGH_RISK

机械判定，命中即锁定，本 Agent 无权降级：

| 触发器 | 命中依据 |
|---|---|
| authentication / 凭据 | 该 sink 投递 verification code（认证材料）；不变量 4 明确要求 `SMTP_USERNAME` 认证身份与发件身份保持独立，禁止重新引入回退 |
| notification / fan-out / 最终 sink | `sendSMTP` 是 verification 与 invitation 两条 producer 汇聚的唯一传输 sink |
| 生产 / 不可逆 | 现网表现为全量邮件停发；任何回归会再次让全量邮件停发，且用户侧不可见失败 |

因此本 Issue 强制：

1. **Plan 之前先跑独立 `BEHAVIOR_DISCOVERY` Run**，`produced_by_run_id` /
   `produced_by_context_id` 必须 ≠ Plan Run/context，否则 Plan Gate 拒；
2. QA 拆 blind Charter 与 execution 两 Run；
3. Review 拆 `BLIND_BEHAVIOR_AUDIT` 与 `EVIDENCE_CHALLENGE` 两 Run；
4. Planner 在 Plan 中提名 2–3 个具名 lens 组成 Review Council，至少一位
   `dissent_role: true`；调度只校验名额、锚点互异、含反对者、rationale 引用具体
   path/invariant，不替换人选；
5. Council 成员优先分配到与 Coder / 主 Reviewer 不同的 runtime lane；异构不可用时标记
   「同模型 Council 降级」并转 `SECURITY_REVIEW_BLOCKED`，不得自动进入 READY 交接。

## Discovery 派单

`BEHAVIOR_DISCOVERY` 指派给「实现」（Coder），**不指派给 Planner**（validator 强制
producer ≠ Planner），也刻意不指派给「验证」/「评审」——两个验证角色的独立性是本 Issue
唯一的完整性兜底，让它们先产出 inventory 会给自己制造锚点，尤其 Reviewer 的
`BLIND_BEHAVIOR_AUDIT` 明确不得以 inventory 为完整性上界。Coder 不是独立性关键角色，
由它承担 Discovery 的代价最低。

预判成熟度：本仓库对 Go 侧无 D2/D3 发现器，`multica-gate/fixtures/` 下只有
`skillhub-java` 的历史夹具（仓库专属，**不得跨仓库调用**）。因此预期落 `D1`，走强制降级：
inventory 不得暗示全量，落在枚举外的行为面一律 `UNVERIFIED`，receipt 带 D1 warning。

## Gate bundle

已从本 Agent skill 目录复制到 `/workspaces/HD-47/gates/`（`run_gate.sh` 已置可执行位），
不从 `$HOME` 或 `/tmp` 运行。

## 交接

- 状态：PLANNING（Discovery 进行中）
- 产物：分支 `hd-47/smtp-from-mailbox` @ base `620f99d8`，账本骨架已建
- 下一步：Coder 执行 `BEHAVIOR_DISCOVERY`，产出 `behavior-inventory.v1` JSON 并冻结；
  冻结后由调度派 Planner
- 未解决：inventory 尚未产出；Council 名单待 Planner 提名
- 需要决策：无（Plan Gate 审批按 Project description 路由，届时再触发）
